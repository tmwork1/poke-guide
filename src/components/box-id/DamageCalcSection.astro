---
interface Props {

	pokemonId: string;
	moveAdoptionBySpecies: Record<string, Record<string, Record<string, number>>>;

}

const { pokemonId, moveAdoptionBySpecies } = Astro.props;
import SecondHeader from '../SecondHeader.astro';
import '../../styles/second-header.css';
import '../../styles/box-damage-page.css';
import '../../styles/box-add-button.css';
import '../../styles/box-damage-card.css';
---


<section id="opponent-notes-section" class="damage-calc-area" data-owned-pokemon-id={pokemonId}>



	<p id="damage-calc-engine-status" class="status-text damage-calc-engine-status" data-state="idle">

		<span id="damage-calc-engine-status-text">準備中…</span>

		<button id="damage-calc-engine-reload-button" type="button" class="btn-ghost" hidden>再試行</button>

	</p>



	{/* 狭い画面ではこの操作列(耐久調整ボタン)を、/box等と同じ「app-header直下に
	    張り付くSecondHeader」として表示する(以前はカード一覧の右下に浮くフローティング
	    ボタンだったが、追加ボタンと2つ並んで浮く見た目が煩雑だったため撤廃。位置調整は
	    box-damage-page.cssの.damage-toolbar-header側で行う。900px以上では従来どおり
	    張り付かない通常の行に戻す)。 */}
	<SecondHeader ariaLabel="ダメージ計算の操作" class="damage-toolbar-header">
		<div id="damage-rows-toolbar" class="damage-rows-toolbar"></div>
	</SecondHeader>

	<div id="damage-rows-list" class="damage-rows-list">

		<p>読み込み中…</p>

	</div>

</section>



<script type="application/json" id="damage-calc-move-adoption-data" set:html={JSON.stringify(moveAdoptionBySpecies)} />



<script>
	import "../../lib/box-id/damage-calc";

</script>
