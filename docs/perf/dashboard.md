# パフォーマンス計測ダッシュボード

再現性のあるパフォーマンス評価・改善サイクルの管理表。運用は以下の4ステップの繰り返し。

1. 画面遷移・ボタンクリック・画面を閉じる等の代表的な操作に目標タイムを設定する
2. `tests/perf/scenarios/*.perf.spec.ts` で実測するシナリオを整備する
3. 目標タイムからの超過具合(超過率)で課題の優先度を整理する(このファイルの表、自動生成)
4. 優先度順に改善対応し、`npm run test:perf` で再計測する

## 再計測のしかた

```bash
npm run dev             # 別ターミナルで起動しておく(起動済みならスキップ可、webServerが自動検出する)
npm run test:perf       # tests/perf/scenarios 配下を実行し、このファイルの表を自動更新する
```

計測対象は `astro dev`(開発サーバー)。本番ビルド(`astro preview`)は `build:master-data` を含め
起動コストが高く、素早く繰り返し計測する用途に向かないため、開発サーバーを対象にしている。
絶対値は本番より遅くなるが、同一条件で毎回計測するため改善前後の相対比較には十分機能する。

1シナリオにつき3回計測し、中央値を採用する(`playwright.perf.config.ts` の `repeatEach: 3`)。

## 目標タイムの目安(RAILモデル準拠)

新しいシナリオを追加するときはこの目安を基準に `targetMs` を設定し、外れる場合は `note` に根拠を書く。

| 分類 | 内容 | 目標タイム |
|---|---|---|
| page-load | 通常のページ遷移 | 1500ms(許容上限 3000ms) |
| page-load | Pyodide初期化を伴う初回表示(ダメージ計算関連) | 5000ms |
| interaction | ボタンクリック等への即時UI反応(モーダル開閉、フィルタ適用など) | 300ms |
| interaction | API呼び出しを伴う操作(保存・送信など) | 800ms |
| close | モーダルclose・画面離脱時の後処理 | 300ms |

## シナリオの追加方法

`tests/perf/lib/perf.ts` の `perfScenario` / `timeNav` / `timeAction` を使い、
`tests/perf/scenarios/<対象>.perf.spec.ts` に追加する。実装済みシナリオを参照。
計測結果の集計・この表への反映はレポーター (`tests/perf/lib/perf-reporter.ts`) が自動で行うため、
シナリオ側はファイルI/Oを意識しなくてよい。

## 計測結果

<!-- PERF_TABLE_START -->
| 優先度 | 分類 | 画面/操作 | 目標(ms) | 実測(ms, 中央値) | 超過率 | 備考 |
|---|---|---|---|---|---|---|
| 🔴 要対応 | page-load | バトルデータ | 1500 | 88489 | 58.99x | 既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🔴 要対応 | page-load | バトルデータを表示 | 1500 | 63965 | 42.64x | 既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🔴 要対応 | page-load | データ: 上位チーム | 1500 | 63844 | 42.56x | 既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🔴 要対応 | page-load | 上位チームの表示 | 1500 | 32593 | 21.73x | /ranked-teams から /data/top-builds へのリダイレクト後を計測。既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🟢 達成 | page-load | 相性チェックを表示 | 1500 | 836 | 0.56x |  |
| 🟢 達成 | interaction | すばやさ表: 並び順切替 | 300 | 143 | 0.48x |  |
| 🟢 達成 | page-load | すばやさ表 | 1500 | 693 | 0.46x |  |
| 🟢 達成 | page-load | データ: すばやさ表 | 1500 | 687 | 0.46x |  |
| 🟢 達成 | page-load | 上位チームを表示 | 1500 | 644 | 0.43x |  |
| 🟢 達成 | page-load | ボックス一覧を表示 | 1500 | 629 | 0.42x |  |
| 🟢 達成 | page-load | ホーム | 1500 | 542 | 0.36x |  |
| 🟢 達成 | page-load | チーム詳細の表示 | 1500 | 533 | 0.36x |  |
| 🟢 達成 | page-load | チーム一覧の表示 | 1500 | 442 | 0.29x |  |
| 🟢 達成 | page-load | 検索 | 1500 | 402 | 0.27x |  |
| 🟢 達成 | page-load | 個体詳細を表示 | 5000 | 719 | 0.14x | Pyodide のダメージ計算エンジン初期化を含む |

_最終計測: 2026-08-31T14:53:35.726Z (15シナリオ, 計43試行の中央値。タイムアウト等で一部シナリオは試行回数が少ない場合がある)_
<!-- PERF_TABLE_END -->
