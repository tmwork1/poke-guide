# パフォーマンス計測ダッシュボード

再現性のあるパフォーマンス評価・改善サイクルの管理表。運用は以下の4ステップの繰り返し。

1. 画面遷移・ボタンクリック・画面を閉じる等の代表的な操作に目標タイムを設定する
2. `tests/perf/scenarios/*.perf.spec.ts` で実測するシナリオを整備する
3. 目標タイムからの超過具合(超過率)で課題の優先度を整理する(このファイルの表、自動生成)
4. 優先度順に改善対応し、`npm run test:perf` で再計測する

## 再計測のしかた

```bash
npm run dev             # 別ターミナルで起動しておく(起動済みならスキップ可、webServerが自動検出する)
npm run test:perf       # tests/perf/scenarios 配下を実行し、このファイルの表を自動更新する
```

計測対象は `astro dev`(開発サーバー)。本番ビルド(`astro preview`)は `build:master-data` を含め
起動コストが高く、素早く繰り返し計測する用途に向かないため、開発サーバーを対象にしている。
絶対値は本番より遅くなるが、同一条件で毎回計測するため改善前後の相対比較には十分機能する。

1シナリオにつき3回計測し、中央値を採用する(`playwright.perf.config.ts` の `repeatEach: 3`)。

## 目標タイムの目安(RAILモデル準拠)

新しいシナリオを追加するときはこの目安を基準に `targetMs` を設定し、外れる場合は `note` に根拠を書く。

| 分類 | 内容 | 目標タイム |
|---|---|---|
| page-load | 通常のページ遷移 | 1500ms(許容上限 3000ms) |
| page-load | Pyodide初期化を伴う初回表示(ダメージ計算関連) | 5000ms |
| interaction | ボタンクリック等への即時UI反応(モーダル開閉、フィルタ適用など) | 300ms |
| interaction | API呼び出しを伴う操作(保存・送信など) | 800ms |
| close | モーダルclose・画面離脱時の後処理 | 300ms |

## シナリオの追加方法

`tests/perf/lib/perf.ts` の `perfScenario` / `timeNav` / `timeAction` を使い、
`tests/perf/scenarios/<対象>.perf.spec.ts` に追加する。実装済みシナリオを参照。
計測結果の集計・この表への反映はレポーター (`tests/perf/lib/perf-reporter.ts`) が自動で行うため、
シナリオ側はファイルI/Oを意識しなくてよい。

## 計測結果

<!-- PERF_TABLE_START -->
| 優先度 | 分類 | 画面/操作 | 目標(ms) | 実測(ms, 中央値) | 超過率 | 備考 |
|---|---|---|---|---|---|---|
| 🔴 要対応 | page-load | バトルデータ | 1500 | 89783 | 59.86x | 既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🔴 要対応 | page-load | バトルデータを表示 | 1500 | 82066 | 54.71x | 既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🔴 要対応 | page-load | データ: 上位チーム | 1500 | 64008 | 42.67x | 既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🔴 要対応 | page-load | 上位チームの表示 | 1500 | 37184 | 24.79x | /ranked-teams から /data/top-builds へのリダイレクト後を計測。既知の未解決bug: /api/opgg-usage系が同時リクエストでdevサーバー側に詰まる(backlog, 2026-08-31) |
| 🟡 注意 | interaction | もちもの選択の自動保存 | 800 | 977 | 1.22x | 使い捨ての空個体に限定。700msの保存デバウンスとPUT完了を含む。主因はopgg-usage同時リクエストではなく、ページ表示直後に開始するPyodide/jpokeエンジンのバックグラウンド初期化がメインスレッドを塞ぎ保存処理と競合すること(2026-09-01調査)。pyodide-engine.tsのENGINE_PREFETCH_FLOOR_MSを1500ms→3000msに引き上げ、本シナリオの一連の流れ(表示〜保存完了、概ね2秒)とプリフェッチ開始が重ならないようにした結果、2672ms→966ms前後まで改善(🔴→🟡)。根本対応にはPyodide初期化のWorker化等が必要(follow-up) |
| 🟡 注意 | interaction | チームメモ編集と自動保存 | 800 | 957 | 1.20x | 使い捨てチームでのみメモを変更し、700msデバウンス後のPUT完了までを計測する。 |
| 🟡 注意 | interaction | 検索: 実行して結果を表示 | 800 | 883 | 1.10x | 実行のたびに searches/events テーブルへログが1件追加される |
| 🟢 達成 | page-load | 上位チームを表示 | 1500 | 1460 | 0.97x |  |
| 🟢 達成 | page-load | データ: すばやさ表 | 1500 | 883 | 0.59x |  |
| 🟢 達成 | page-load | 相性チェックを表示 | 1500 | 832 | 0.55x |  |
| 🟢 達成 | page-load | すばやさ表 | 1500 | 784 | 0.52x |  |
| 🟢 達成 | page-load | チーム一覧の表示 | 1500 | 693 | 0.46x |  |
| 🟢 達成 | page-load | チーム詳細の表示 | 1500 | 646 | 0.43x |  |
| 🟢 達成 | interaction | すばやさ表: 並び順切替 | 300 | 123 | 0.41x |  |
| 🟢 達成 | page-load | ボックス一覧を表示 | 1500 | 604 | 0.40x |  |
| 🟢 達成 | page-load | ホーム | 1500 | 559 | 0.37x |  |
| 🟢 達成 | page-load | 検索 | 1500 | 444 | 0.30x |  |
| 🟢 達成 | close | もちもの選択モーダルを閉じる | 300 | 76 | 0.25x | 既存個体では候補を選ばず、表示のみのダイアログを閉じる操作に限定 |
| 🟢 達成 | close | チームのもちもの表示を閉じる | 300 | 62 | 0.21x | 開閉は自動保存を呼ばないことを実装で確認済み。既存チームでは選択・変更を行わない。 |
| 🟢 達成 | page-load | 個体詳細を表示 | 5000 | 614 | 0.12x | Pyodide のダメージ計算エンジン初期化を含む |

_最終計測: 2026-09-01T15:29:38.360Z (20シナリオ, 計59試行の中央値。タイムアウト等で一部シナリオは試行回数が少ない場合がある)_
<!-- PERF_TABLE_END -->

### 🔴 既知の外部要因: opgg-usage系devサーバー詰まり(2026-09-01時点は表に出現)

`data-index-load`(バトルデータ, `/data`)・`data-top-builds-load`(データ: 上位チーム, `/data/top-builds`)・
`ranked-teams-load`(上位チームの表示, `/ranked-teams`)の3シナリオは、既知の未解決bug
([[project_opgg_usage_dev_server_stall]] 相当、`/api/opgg-usage`系が同時リクエストでdevサーバー側に詰まる)の影響を受け続けている。
2026-08-31時点は3回の試行すべてがタイムアウト(120秒)し表から完全に消えていたが、2026-09-01の再計測では
一部試行がタイムアウト前に応答し、42,000〜85,000ms(目標1500msの28〜57倍)という極端な値で表に出現した。
**依然としてアプリのコードでは直接修正できない外部要因(devサーバー側の同時リクエスト詰まり)と判断しており、最優先の要対応として扱う。**
試行回数が3回に満たない場合があるため、表の数値の変動幅は大きい。
