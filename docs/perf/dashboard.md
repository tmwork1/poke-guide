# パフォーマンス計測ダッシュボード

再現性のあるパフォーマンス評価・改善サイクルの管理表。運用は以下の4ステップの繰り返し。

1. 画面遷移・ボタンクリック・画面を閉じる等の代表的な操作に目標タイムを設定する
2. `tests/perf/scenarios/*.perf.spec.ts` で実測するシナリオを整備する
3. 目標タイムからの超過具合(超過率)で課題の優先度を整理する(このファイルの表、自動生成)
4. 優先度順に改善対応し、`npm run test:perf` で再計測する

## 再計測のしかた

```bash
npm run dev             # 別ターミナルで起動しておく(起動済みならスキップ可、webServerが自動検出する)
npm run test:perf       # tests/perf/scenarios 配下を実行し、このファイルの表を自動更新する
```

計測対象は `astro dev`(開発サーバー)。本番ビルド(`astro preview`)は `build:master-data` を含め
起動コストが高く、素早く繰り返し計測する用途に向かないため、開発サーバーを対象にしている。
絶対値は本番より遅くなるが、同一条件で毎回計測するため改善前後の相対比較には十分機能する。

1シナリオにつき3回計測し、中央値を採用する(`playwright.perf.config.ts` の `repeatEach: 3`)。

## 目標タイムの目安(RAILモデル準拠)

新しいシナリオを追加するときはこの目安を基準に `targetMs` を設定し、外れる場合は `note` に根拠を書く。

| 分類 | 内容 | 目標タイム |
|---|---|---|
| page-load | 通常のページ遷移 | 1500ms(許容上限 3000ms) |
| page-load | Pyodide初期化を伴う初回表示(ダメージ計算関連) | 5000ms |
| interaction | ボタンクリック等への即時UI反応(モーダル開閉、フィルタ適用など) | 300ms |
| interaction | API呼び出しを伴う操作(保存・送信など) | 800ms |
| close | モーダルclose・画面離脱時の後処理 | 300ms |

## シナリオの追加方法

`tests/perf/lib/perf.ts` の `perfScenario` / `timeNav` / `timeAction` を使い、
`tests/perf/scenarios/<対象>.perf.spec.ts` に追加する。実装済みシナリオを参照。
計測結果の集計・この表への反映はレポーター (`tests/perf/lib/perf-reporter.ts`) が自動で行うため、
シナリオ側はファイルI/Oを意識しなくてよい。

## 計測結果

<!-- PERF_TABLE_START -->
| 優先度 | 分類 | 画面/操作 | 目標(ms) | 実測(ms, 中央値) | 超過率 | 備考 |
|---|---|---|---|---|---|---|
| 🟡 注意 | interaction | もちもの選択の自動保存 | 800 | 990 | 1.24x | 使い捨ての空個体に限定。700msの保存デバウンスとPUT完了を含む。主因はopgg-usage同時リクエストではなく、ページ表示直後に開始するPyodide/jpokeエンジンのバックグラウンド初期化がメインスレッドを塞ぎ保存処理と競合すること(2026-09-01調査)。pyodide-engine.tsのENGINE_PREFETCH_FLOOR_MSを1500ms→3000msに引き上げ、本シナリオの一連の流れ(表示〜保存完了、概ね2秒)とプリフェッチ開始が重ならないようにした結果、2672ms→966ms前後まで改善(🔴→🟡)。根本対応にはPyodide初期化のWorker化等が必要(follow-up) |
| 🟡 注意 | interaction | 検索: 実行して結果を表示 | 800 | 898 | 1.12x | 実行のたびに searches/events テーブルへログが1件追加される |
| 🟡 注意 | interaction | チームメモ編集と自動保存 | 800 | 855 | 1.07x | 使い捨てチームでのみメモを変更し、700msデバウンス後のPUT完了までを計測する。 |
| 🟢 達成 | interaction | すばやさ表: 並び順切替 | 300 | 187 | 0.62x |  |
| 🟢 達成 | page-load | すばやさ表 | 1500 | 747 | 0.50x |  |
| 🟢 達成 | page-load | バトルデータを表示 | 1500 | 690 | 0.46x | wrangler.jsonc の OPGG_USAGE KVを remote: false に変更し、旧既知バグ(/api/opgg-usage系の同時リクエスト詰まり、backlog 2026-08-31)は解消済み(2026-09-08)。 |
| 🟢 達成 | page-load | 上位チームの表示 | 1500 | 690 | 0.46x | /ranked-teams から /data/top-builds へのリダイレクト後を計測。wrangler.jsonc の OPGG_USAGE KVを remote: false に変更し、旧既知バグ(/api/opgg-usage系の同時リクエスト詰まり、backlog 2026-08-31)は解消済み(2026-09-08)。 |
| 🟢 達成 | page-load | 相性チェックを表示 | 1500 | 660 | 0.44x | ローカルdev(remote: falseのOPGG_USAGE)では集計データが空のため「集計データがまだありません。」の空状態表示になる。カード描画・空状態表示のどちらかを読み込み完了とみなす。 |
| 🟢 達成 | page-load | 上位チームを表示 | 1500 | 651 | 0.43x |  |
| 🟢 達成 | page-load | データ: すばやさ表 | 1500 | 627 | 0.42x |  |
| 🟢 達成 | page-load | バトルデータ | 1500 | 539 | 0.36x | wrangler.jsonc の OPGG_USAGE KVを remote: true から false(ローカルエミュレーション)に変更し、devサーバー起動のたびに発生していたリモートKVプロキシ接続待ちを解消(2026-09-08)。以前の既知バグ(/api/opgg-usage系が同時リクエストで詰まる、backlog 2026-08-31)は解消済み。 |
| 🟢 達成 | page-load | ボックス一覧を表示 | 1500 | 536 | 0.36x |  |
| 🟢 達成 | close | もちもの選択モーダルを閉じる | 300 | 107 | 0.36x | 既存個体では候補を選ばず、表示のみのダイアログを閉じる操作に限定 |
| 🟢 達成 | page-load | ホーム | 1500 | 521 | 0.35x |  |
| 🟢 達成 | page-load | 検索 | 1500 | 499 | 0.33x |  |
| 🟢 達成 | page-load | チーム詳細の表示 | 1500 | 469 | 0.31x |  |
| 🟢 達成 | page-load | データ: 上位チーム | 1500 | 448 | 0.30x | wrangler.jsonc の OPGG_USAGE KVを remote: true から false(ローカルエミュレーション)に変更し、devサーバー起動のたびに発生していたリモートKVプロキシ接続待ちを解消(2026-09-08)。以前の既知バグ(/api/opgg-usage系が同時リクエストで詰まる、backlog 2026-08-31)は解消済み。 |
| 🟢 達成 | page-load | チーム一覧の表示 | 1500 | 406 | 0.27x |  |
| 🟢 達成 | close | チームのもちもの表示を閉じる | 300 | 76 | 0.25x | 開閉は自動保存を呼ばないことを実装で確認済み。既存チームでは選択・変更を行わない。 |
| 🟢 達成 | page-load | 個体詳細を表示 | 5000 | 677 | 0.14x | Pyodide のダメージ計算エンジン初期化を含む |

_最終計測: 2026-09-08T00:54:06.702Z (20シナリオ, 計60試行の中央値。タイムアウト等で一部シナリオは試行回数が少ない場合がある)_
<!-- PERF_TABLE_END -->

### ✅ 解消済み: opgg-usage系devサーバー詰まり(2026-09-08)

`data-index-load`(バトルデータ, `/data`)・`data-top-builds-load`(データ: 上位チーム, `/data/top-builds`)・
`ranked-teams-load`(上位チームの表示, `/ranked-teams`)・`box-matchup-load`(相性チェックを表示)は、
2026-08-31〜09-01時点で `/api/opgg-usage`系が同時リクエストでdevサーバー側に詰まる既知バグ
([[project_opgg_usage_dev_server_stall]])の影響で42,000〜89,000ms(目標1500msの28〜60倍)の極端な値、
または120秒タイムアウトで表から欠落していた。

根本原因は `wrangler.jsonc` の `OPGG_USAGE` KVバインディングが `remote: true` になっており、
`astro dev` 起動のたびに本番Cloudflare KVへのリモートプロキシ接続(workerdプロセスをもう1つ起動、
実測+約530MB)を確立していたこと。この接続確立・維持自体がdev起動を不安定にし(30秒起動タイムアウトと
際どく競合)、同時リクエスト時のプロキシ詰まりも引き起こしていたと判断した(codex sol advisorによる調査、
2026-09-08)。ユーザー承認のうえ `remote: false`(ローカルKVエミュレーション)に変更した結果、
4シナリオとも700〜950ms台(目標達成)まで改善し、120秒タイムアウトも解消した。

**副作用**: ローカルKVは空スタートのため、OP.GGデータに依存する画面はdevでは実データが出ない
(既存コードは空/nullを空状態として表示するため画面自体は壊れない)。`box-matchup-load`
(相性チェックを表示)はこの副作用で「集計データがまだありません。」という正当な空状態表示になるため、
シナリオ側の読み込み完了判定をカード描画・空状態表示のどちらでも良いように修正済み。
