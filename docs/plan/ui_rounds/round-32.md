# UI改善ラウンド 32

[← 索引](../ui_plan.md)

## ラウンド32 の要件(2026-07-30 ユーザー指示第15弾)

対象は **`/box`(ボックス一覧)と `/box/[id]`(個体編集)**。**ユーザー指示は3レビュアーの講評より常に優先する。**

このラウンドは3つの要素からなる:
1. ユーザー指示第15弾(下記 32-B / 32-L / 32-D / 32-R)
2. **ユーザー要求による「UI統一感レビュアー」の新設**(下記 32-U。共通指示「同じUI領域でもフォント・サイズが微妙に統一されていなかったりする。UIの統一感をレビューする専門のsub agentを立ち上げてレビューさせる」)
3. **ラウンド32のメタレビュー**(fable。ラウンド30の申し送りにより、3の倍数のこのラウンドで実施)

### ヘッダーについて(2ラウンド越しの未回答が解決した)

ラウンド31でユーザーの箇条書きの「個体編集画面 ヘッダー」節が空欄だった件は、**このラウンドで「指示なし・変更不要」とユーザーが回答した**(今回も空欄だったため改めて確認した)。**ヘッダーは今後も指示があるまで変更しない。** round-31.md の積み残し5番はこれで解決。

### ユーザーからの追加指示(実装中に届いたもの)

> 個体編集の左パネルのポケモンアイコンサイズ変更はなし。上側の余白調整は実施

→ **32-L1 から「ポケモン画像を少し大きくする」を削除した**(下記 32-L1 参照)。`.species-sprite-img` は 144px、`.species-icon-box` は 160px のまま。

---

### 現状の実測値(1920×1080 ライト、Coordinatorが `getBoundingClientRect()` / `getComputedStyle()` で実測)

**フィクスチャ**(→ `pitfalls.md`「現在のフィクスチャ」)

| ID | 何のケースか |
|---|---|
| `c8680844-dd43-42a4-bdf1-f3de11fe3267`(メガリザードンX) | ダメージ計算カード4枚。攻撃/防御が混在。**ダメージ計算・右サイドの検証はこれ** |
| `8f743377-7cff-4fa7-b42e-8dae8515107f`(ハバタクカミ「カミ様」) | ダメージ計算0枚。ニックネーム・テラス・持ち物・技4つあり。**左サイドの検証に向く** |
| `42ccb527-f21d-4f08-904f-05331b30cdea`(フシギバナ(キョダイ)) | 持ち物なし・テラスなし・特性あり。**「持ち物なし」「テラスなし」の検証はこれ** |
| `f7e594b4-26df-4c3a-bd07-84dc34a1a976`(ディンルー) | 技0件 |

> 🔴 **このラウンドの開始時、メガリザードンXの `tera_type` が基準値「ほのお」から `null` にズレていた**(ラウンド27でも同じ現象)。Coordinatorが `PUT /api/owned-pokemon/:id` で「ほのお」に復元し、`GET` で13フィールドすべてが基準表と一致することを確認済み。**テラスバッジ関連の検証をする前に、必ず `tera_type` が「ほのお」であることを確認すること**(nullだとバッジが `hidden` になり「バッジが消えた」と誤診する)。

**`/box` カード(`.card-pokemon` 257.34×257.34px / y=147.1)**

| 対象 | 実測 |
|---|---|
| `.card-sprite-img` | 113×113 / (621.5, 156.1) |
| `.card-tera-badge` | `.card-badge-row` の中。**テラス未設定の個体では `hidden`** |
| `.card-name` | 239.34×15.75 / y=273.1 / **13.12px / 700** |
| `.card-item-row` | 239.34×18 / y=293.6。中身は `.card-item-badge`(18px)+ `.card-item-name`(**12px / 600 / `rgb(91,100,114)`**)で**中央寄せ** |
| `.card-moves-grid` | 239.34×34.72 / y=317.2 |
| `.card-stat-grid` | 239.34×**32.52** / y=364.7 ← **32-B1 で削除する対象** |
| カードの `title` 属性 | `とくせい: かたいツメ 持ち物: こだわりハチマキ わざ: フレアドライブ / がんせきふうじ / げきりん / おにび` ← **32-B4 で中身を入れ替える対象** |

**`/box/[id]` 左サイド(`.panel-left` x=256 / w=440 / y=64.7 / `padding: 24px`)**

| 対象 | 実測 |
|---|---|
| `.species-icon-box` | 160×160 / (305.5, 88.7) ← **パネル上端 64.7 との差 = 24px(`padding-top`)** |
| `.species-sprite-img` | **144×144** / (313.5, 96.7) ← **サイズは変更しない(ユーザー追加指示)** |
| `.type-badge-img` ×2 | 20px / **(633, 141.1) と (655, 141.1)** → **2つの間隔は 2px** |
| `.item-image` | **24×24** / (504.6, 169.9)。親は `.item-image-badge` |
| 持ち物名テキスト | **18px**(31-L2 で 12→18px に拡大した) |
| 種族 `input.field-control` | 194.7×37.59 / (276, 256.7) / **15.04px** |
| `.field-grid.move-fields-grid` | 399×81.58 / y=409.5 / **margin 16px 上下** → bottom **491.08** |
| `.stat-table-header` | 399×26.56 / **y=507** / 12.48px / 600 → **技との gap 15.92px** |
| `.stat-table` | 399×273.64 / y=507 → bottom **780.64** |
| `.memo-input` | 399×128 / **y=780.7** → **ステータスとの gap 0.06px(ほぼ密着)** |
| `.stat-nature-btn` | 24×24。選択中は `::before` に **`drop-shadow` ×4 = `rgb(31,36,48)`(黒)** の縁取り(31-L8) |

**`/box/[id]` ダメージ計算カード(`.card-damage` 779.22×223.98 / x=738.4)**

| 対象 | 実測 |
|---|---|
| `.sprite-icon`(相手画像) | **112×135.91** / (967.4, 98.5) |
| `.damage-row-build` | 340×212.42 / (749, 92.9) |
| `.damage-row-tera-field` | 202.42×25.84 / (758.6, 208.5) ← 「テラスなし」表示 |
| `.damage-row-columns-wrap` | **408.44**×68.5 / (1098.6, 92.9) ← 技カード列 |
| `.damage-row-total-result` | **408.44**×24.28 / (1098.6, 281) / 14.72px |
| 削除ボタン `.damage-row-delete-button` | 28×28 / **(1480.6, 97.7)= カード右上** ← **32-D3 で移動する対象** |
| `.damage-column-order-label` | 17.91×61.41 / 12.8px |
| カード右端 | 738.4 + 779.22 = **1517.62** |

**`/box/[id]` 右サイド(`.panel-right` x=1560 / w=360 / y=65 / 内側 327px)**

| 対象 | 実測 |
|---|---|
| `.damage-detail-selection-heading` | 327×40.19 / y=79.4(上端固定、31-R1) |
| `.damage-detail-selection-icon` ×2 | **28×28** / x=1577 と x=1631.6 |
| `.damage-detail-selection-arrow` | テキストの「→」(`<span>`) ← **32-R1 で作り直す対象** |
| 天候 `.damage-detail-field-row` | 見出しの直下(パネル上部) ← **32-R2 で最下部へ移す** |
| フィールド `.damage-detail-field-row` | 天候の直下 ← 同上 |
| 攻撃側 `.damage-detail-group` | 327×**99.92** / y=310.8。中身は ランク/状態異常/テラスタル → **じゅうでん → 急所 の順で別段** |
| 防御側 `.damage-detail-group` | 327×63.55 / y=485.7 |
| その他の状態 `.damage-detail-group.damage-detail-volatile-wrap` | 327×96.31 / y=590.4(のろい/やどりぎのタネ/しおづけ/バインド/アクアリング/ねをはる/タールショット/ちいさくなる/きょけんとつげき) |
| 区切り線 | `#damage-detail-panel-body .damage-detail-sides` 系の `border-top`。**29-R2 で攻撃側=`--color-danger-strong` / 防御側=`--color-primary-strong` に色付けした**(`src/pages/box/[id].astro:2824-2840` 付近) ← **32-R3 で撤回する対象** |

---

## 要件(ユーザーの言葉を引用したうえで具体化する)

### `/box`(ボックス一覧)— 担当ファイル `src/pages/box/index.astro`

- **32-B1** > 「ステータスをカードから削除止し、カードを小型化する」(原文ママ。「削除し」の誤字と解釈する)
  `.card-stat-grid`(239.34×32.52)を**カードから消す**。**DOMは残してCSSで見せない方針**(→ `pitfalls.md`「要素を『消す』指示が来ても、DOMは残してCSSで見せない」)。
  消したぶんカードを**小型化する**。**`.card-pokemon` の正方形は維持すること**(グリッドの行が揃わなくなる。257.34px という値自体は今回に限り変更してよいが、**縦横が等しいこと**は守る)。**縮小後の1画面あたりの表示体数を実測して報告すること**(密度は「1画面に何個入るか」で測る → `pitfalls.md`)。

- **32-B2** > 「アイテムアイコンをポケモンアイコンの右側(テラスタル画像の上)に配置」
  `.card-item-badge`(18px、現在は `.card-item-row` の中)を、**公式絵の領域(`.card-art`)の右側**へ移す。**同じ列にテラスバッジ(`.card-tera-badge`)を置き、アイテムアイコンがその上**になるように縦に並べる。
  - **持ち物なし / テラスなしの個体で、空のバッジ枠が出ないこと**(→ `pitfalls.md`「`[hidden]` は詳細度で負ける」。この2クラスは**過去に4回同じ事故を起こしている**)。`42ccb527`(持ち物なし・テラスなし)で必ず確認すること。
  - アイコンが公式絵に**重ならない**こと。カード右上の `.card-actions`(お気に入り/削除)とも重ならないこと。

- **32-B3** > 「特性をアイテムの左側に表示」
  カードに**特性名**を出す(現在カード上には無く、`title` 属性にしか入っていない)。`.card-item-row` を **[特性名] [持ち物名]** の2列**左右振り分け**にする(アイコンは 32-B2 で `.card-art` 側へ移るので、この行はテキスト2つになる)。
  - **特性・持ち物のどちらか一方だけ設定されている個体でも列が崩れないこと**(`42ccb527` = 特性あり・持ち物なし で確認)。
  - **文字12px下限**(20-G3)。1行で省略(`text-overflow: ellipsis`)し、折り返さないこと。

- **32-B4** > 「カードのホバー表示にはステータスやメモなど、カード上に表示されていない情報だけ表示する」
  カードの `title` 属性を作り直す。**現在は「とくせい / 持ち物 / わざ」= 32-B2・32-B3 の後はすべてカード上に見えている情報**なので重複している。
  - **新しい `title` の中身**: **実数値(H/A/B/C/D/S)** と **メモ**。どちらも空なら `title` 自体を出さない。
  - `aria-label`(「◯◯ を編集」)は**変えないこと**(スクリーンリーダーの読み上げが壊れる)。

### `/box/[id]` ヘッダー

- **指示なし・変更不要**(ユーザー確認済み。上記「ヘッダーについて」参照)。

### `/box/[id]` 左サイド — 担当ファイル `src/pages/box/[id].astro`

- **32-L1** > 「上側とヘッダーの間の余白を少し減らし、~~ポケモン画像を少し大きくする~~」
  **🔴 後半は撤回。ユーザーの追加指示「個体編集の左パネルのポケモンアイコンサイズ変更はなし。上側の余白調整は実施」により、画像サイズは 144px のまま変更しない。**
  `.panel-left` の `padding-top: 24px`(パネル上端 64.7 → `.species-icon-box` 88.7)を**少し減らす**。減らした値と、減らした後の `.species-sprite-img` が **144×144 のままである**ことを実測して報告すること。

- **32-L2** > 「タイプ画像はアイテム表示と同じ位置で左揃えし、2タイプ間の距離を少しあける」
  `.type-badge-img` ×2(現在 x=633 / 655、間隔 **2px**)を、**`.item-image` の x=504.6 に左揃え**する(= 左端を揃える)。あわせて**2つのタイプアイコンの間隔を広げる**(2px → 6px 程度)。
  - **単タイプの個体でも位置が変わらないこと**(左揃えなので1つ目の位置は不変になるはず)。**実測値(2つの x と間隔)を報告すること。**

- **32-L3** > 「アイテム画像＋名前表示のサイズを、種族名などと揃える」
  現在 `.item-image` **24px** + 持ち物名 **18px**。**種族名の入力欄(`.field-control` = 15.04px)に揃える。**
  - **これは 31-L2「アイテム画像+文字を大きくする」を、ユーザー自身が部分的に撤回するもの**(→ round-31.md)。31-L2 が守った「画像と文字の比 1〜1.5倍」(28-L3)は引き続き守ること。
  - 画像は**アイコン規格20px**(28-L1〜L6)に戻すのが自然。**文字は12px下限を割らないこと。**
  - **「こだわりハチマキ」で `scrollWidth === clientWidth`(省略なし)を実測して報告すること。**

- **32-L4** > 「技とステータスの間の間隔をさらに広げて、アイテムと技の間のスペースと揃える」
  **実測では既に 16px / 15.92px でほぼ同値**(31-L7 で揃えた)。**にもかかわらずユーザーには不揃いに見えている。** 原因は、持ち物→技が**入力欄の枠と枠の間**なのに対し、技→ステータスは**入力欄の枠と文字ラベル(`.stat-table-header`、`padding-bottom: 5.6px`)の間**で、**視覚的な余白の見え方が違う**こと。
  - **両方を「さらに広げた」うえで、視覚的に等価に見えるようにすること。** 単に数値を合わせるだけでは 31-L7 の再演になる。
  - **報告には (a) 持ち物→技 (b) 技→ステータス の実測値**に加えて、**`--scale 3` の拡大クロップをライト/ダーク両方で撮って目視確認した結果**を書くこと。
  - **技0件の個体(`f7e594b4`)でも間隔が保たれること**を確認する(31-L7 で確認済みの回帰ポイント)。

- **32-L5** > 「ステータスとメモの間の間隔を広げる」
  `.stat-table` の bottom 780.64 と `.memo-input` の y=780.7 で **gap 0.06px = ほぼ密着**している。**広げる。** 32-L4 で決めた間隔と揃えるか、それより広くする(メモは別の情報ブロックなので、同値以上が妥当)。**実測値を報告すること。**

- **32-L6** > 「特性補正の三角形の縁取りの色は、塗りつぶし色と同系色で強調するデザインにする」
  現在 `.stat-nature-btn[aria-pressed="true"]` の `::before` に `drop-shadow` ×4 で **`rgb(31,36,48)`(黒)** の縁取りが付いている(31-L8)。これを**塗りつぶし色と同系色**に変える。
  - 塗り: 上昇 A▲ = `rgb(201,67,44)`(danger系) / 下降 C▼ = `rgb(29,111,209)`(primary系)。
  - **縁取りはその同系のより濃い/深い既存トークンにする。新色は作らないこと**(→ `pitfalls.md`「配色」)。
  - **これは 31-L8 の「黒縁」を撤回するもの。** ただし 31-L8 の要件「非選択の ▲▼ と一目で区別できること」は**引き続き満たすこと**。**ライト/ダーク両方で `--scale 3` の拡大クロップを撮り、目視で確認して報告すること**(ダークで同系色の縁取りが背景に沈まないか)。

### `/box/[id]` ダメージ計算カード — 担当ファイル `src/pages/box/[id].astro`

- **32-D1** > 「相手ポケモンのアイコンを6%ほど小さくする」
  `.sprite-icon`(相手側、現在 **112×135.91**)を **6%縮小 → 幅 105.3px 前後**。**縮小後の実測値を報告すること。**
  - ⚠️ **`.sprite-icon` は `src/styles/global.css` の共有クラス**(→ 31-D3 の教訓、`pitfalls.md`「打ち消したつもりで1階層上を打ち消している」)。**`global.css` は編集せず**、`#opponent-notes-section` 配下に限定したページscopedのセレクタで縮小すること。**左パネルや他ページの `.sprite-icon` が巻き添えで縮んでいないことを確認して報告すること。**

- **32-D2** > 「テラスなし表示は左寄せにしてplaceholderのように薄く表示」
  `.damage-row-tera-field`(202.42×25.84 / x=758.6)の**未選択時**の表示を、
  (a) **左寄せ**にする(現在は中央寄せ)、(b) **placeholder のように薄い文字色**にする(`--color-text-muted` 系の既存トークン。**新色は作らない**)。
  - **テラスが選択されている個体(`c8680844` = ほのお、`8f743377` = フェアリー)では通常の文字色のままであること。** 薄いのは**未選択のときだけ**。
  - 31-L5・31-D4 で左パネル側のテラス選択欄を左揃えにしてあるので、**そちらと見た目を揃えること。**

- **32-D3** > 「技カードの幅を少し減らし、右側の余剰スペースの最下部 (すなわち累計結果の右側)に削除ボタンを配置」
  (a) `.damage-row-columns-wrap` と `.damage-row-total-result`(ともに **408.44px**)の幅を**少し減らす**。
  (b) 空いた右側の**最下部 = 累計結果(`.damage-row-total-result`、y=281)の右側**に、削除ボタン(`.damage-row-delete-button`、28×28、現在はカード右上 (1480.6, 97.7))を移す。
  - **31-D2 で「hover時に赤背景+文字」「常時表示(`opacity: 1`)」にした挙動は維持すること。**
  - **31-D2 の副作用で入れた `.damage-row-columns > .damage-column:first-child .damage-column-move-row` の `padding-right: 40px`(削除ボタンが1発目の「ヒット」ラベルと重なるのを避けるための措置)は、ボタンが右上から居なくなるので不要になる可能性が高い。** 不要になったら**消すこと**(残すと技名欄が理由なく狭いままになる)。消したかどうかを報告すること。
  - **技列を複数(3列以上)持つカードでも、削除ボタンが技カードや累計結果と重ならないこと**を実測すること(→ round-31.md 積み残し1番。**このラウンドで一緒に解消する**)。
  - `.damage-row-total-result` / `.damage-column` / `.damage-row-columns-wrap` は**改名禁止**(JSとE2Eが参照)。

### `/box/[id]` 右サイド — 担当ファイル `src/pages/box/[id].astro`

- **32-R1** > 「ヘッダの画像を少し大きくし、矢印を太くて見やすいデザインに変更」
  `.damage-detail-selection-icon` ×2(現在 **28px**)を**少し大きく**する(32〜36px程度)。
  `.damage-detail-selection-arrow`(現在テキストの「→」)を**太くて見やすいデザイン**にする。
  - **矢印の向きが `direction`(攻撃/防御)に追随する挙動は 31-R1 で実装済み。壊さないこと**(攻撃カードで「→」、防御カードで「←」)。SVGに置き換える場合は**両方向を確認**すること。
  - **ヘッダー全体の高さが増えることで、下の設定群が押し下げられて右パネルに収まらなくならないこと**を確認する。

- **32-R2** > 「天候・フィールド設定を一番下に移動」
  `.damage-detail-field-row`(天候・フィールド、現在は選択見出しの直下)を、**右パネルの一番下**へ移す。
  - 移動後の順序: **選択見出し → 攻撃側 → 防御側 → その他の状態 → 天候 → フィールド**(「一番下」の指示に忠実に)。
  - **天候・フィールドは攻撃側/防御側のどちらにも属さない「場」の設定**なので、**攻撃側/防御側のグループの中に入れないこと。**
  - **DOMの移動になるため、天候・フィールドのボタンのイベントハンドラ・`aria-pressed` の同期・保存経路が壊れていないこと**を確認する(実際にクリックして確認する場合は、**使い捨て個体で行うか、確認後に必ず元の値へ戻すこと** → `pitfalls.md`)。

- **32-R3** > 「横棒は色付けしない」
  29-R2 で攻撃側=`--color-danger-strong` / 防御側=`--color-primary-strong` に色付けした**区切り線(`border-top`)を、無彩色の `--color-border` に戻す**(`src/pages/box/[id].astro:2824-2840` 付近)。
  - **⚠️ 見出しの文字色(赤/青)と形状マーカー `.damage-detail-side-icon` は残すこと。** 29-R2 は「攻撃側/防御側が色でも判別できるように」という WCAG 1.4.1 対応であり、**線の色だけがユーザーの指示対象**。線まで含めて全部無彩色にすると識別性の要件を壊す。
  - **これは 29-R2 の一部を撤回するもの。**

- **32-R4** > 「攻撃側の2段目は、急所,じゅうでんの順に同じグループとして配置」
  攻撃側 `.damage-detail-group`(現在 h=99.92px)の中で、**じゅうでん → 急所 の順に別段になっている**ものを、**「急所 → じゅうでん」の順で同じ1グループ(同じチップ行)**にまとめる。
  - 1段目は現状どおり ランク / 状態異常 / テラスタル(31-R3)。**2段目が「急所, じゅうでん」**になる。
  - **327px幅に2つが省略・はみ出し・折り返しなしで収まることを実測すること。** 収まらない場合も**公式用語を短縮しないこと**(28で3件差し戻された経緯)。
  - 「急所」は 31-R2 で攻撃側に移したもの。**移動先グループが変わるだけで、攻撃側から出さないこと。**

- **32-R5** > 「防御側は、かべをひらがな表記にし、他の揮発状態と同じグループで配置する。」
  (a) 「**壁**」→「**かべ**」(ひらがな)。**これは 31-R4(「リフレクター・ひかりのかべ」→「壁」)をさらに変更するもの。**
  (b) 「かべ」チップを、**`.damage-detail-group.damage-detail-volatile-wrap`(「その他の状態」= のろい/やどりぎのタネ/しおづけ/バインド/アクアリング/ねをはる/タールショット/ちいさくなる/きょけんとつげき)と同じグループ**に入れる。
  - **`title` 属性の説明文(「リフレクター/ひかりのかべを技の分類に応じて自動選択して壁を張る」)は残すこと**(31-R4 で「可視ラベルが短いぶん補足が要る」と判断済み。ラベルがさらに短くなるので必要性は増している)。
  - **チップの並び順の中でどこに置いたか**と、**そのグループが1行に収まるか(327px)**を報告すること。

### 32-U(共通)UI統一感レビュアーの新設 — ユーザー要求

> 「同じUI領域でもフォント・サイズが微妙に統一されていなかったりする。UIの統一感をレビューする専門のsub agentを立ち上げてレビューさせる」

- **32-U1**: **4人目のレビュアーとして「UI統一感レビュアー」(sonnet)を新設する。** 役割は「**同じ意味・同じ格の要素なのに値が微妙に違っている(規格の揺れ)を、実測値の証拠つきで列挙する**」ことに限定する。見た目の好き嫌い・使いやすさは既存3レビュアーの担当。
- **32-U2**: `.claude/skills/ui/references/agent-prompts.md` に**統一感レビュアーのテンプレートを常設**し、`SKILL.md` の役割分担表・Step 2 を「3レビュアー」から「4レビュアー」に改める。**メタアドバイザー(fable)の提案を踏まえて、既存3レビュアーの観点リストから統一感レビュアーへ移す項目・削る項目も同時に決める**(観点の重複を残すと同じ指摘が多重に上がる)。
- **32-U3**: このラウンドで**実際に統一感レビュアーを走らせ**、その指摘を下の「統一感レビューの指摘」節に記録する。**優先度A(同じ行・同じ視線の中で見比べられる揺れ)は 32-B / 32-L / 32-D / 32-R の実装と同じラウンドで直す。**

---

## この指示で撤回される過去の判断(後から読むエージェントが混乱しないための一覧)

| 撤回されるもの | 出典 | 撤回する要件 |
|---|---|---|
| アイテム画像24px + 持ち物名18px に拡大する | 31-L2 | **32-L3**(種族名の 15.04px に揃える。画像は20px規格へ) |
| 性格補正の選択中マーカーの縁取りを黒(`rgb(31,36,48)`)にする | 31-L8 | **32-L6**(塗りと同系色にする) |
| 削除ボタンをダメージ計算カードの右上に置く | 〜31-D2 | **32-D3**(累計結果の右側=右下へ) |
| 攻撃側/防御側の区切り線を赤/青に色付けする | 29-R2(線の部分のみ) | **32-R3**(線は無彩色に戻す。**文字色と形状マーカーは維持**) |
| 「リフレクター・ひかりのかべ」→「壁」 | 31-R4 | **32-R5**(→「かべ」。ひらがな表記) |
| 天候・フィールドを右パネル上部に置く | 25-R3 以降の構成 | **32-R2**(一番下へ) |
| カードの `title` に「とくせい/持ち物/わざ」を出す | 〜31 | **32-B4**(カード上に出るようになるので、実数値とメモに入れ替え) |

---

## 統一感レビューの指摘(2026-07-30 UI統一感レビュアー・新設1回目 / Coordinator裁定つき)

**32-U3 に基づき、優先度Aはこのラウンドで実装する。** レビュアーは設計どおり「どちらの規格に揃えるか」の裁定をしていないので、**裁定はCoordinatorが下した**(下表の【裁定】)。

### 優先度A(同じ行・同じ視線の中で見比べられる揺れ)

- **32-U-A1 `/box` ツールバーで「お気に入りのみ表示」ボタンだけ他より高く、上端も浮いている**
  【実測】同じ `#box-toolbar` 行: `#search-input` と `#sort-select`(ともに `.field-control`)が **40.39px / y=67.27**、`#favorite-filter-toggle`(素の `<button>`)が **45.19px / y=64.88**。**高さ +4.8px、上端 -2.39px。**
  【出どころ】`src/styles/global.css:195` の `body { line-height: 1.6 }` に対し、`input[type=text]/select/textarea`(`global.css:474-487`)は `line-height: var(--line-height-snug)`(1.4)を明示しているが、**`button` 基底(`global.css:812-830`)には `line-height` の指定が無く body の 1.6 を継承している**。padding も input/select 側は `var(--space-2) var(--space-3)`(8px/12px)、button は `0.55em 1.1em`(16pxで8.8px/17.6px)と別式。`favorite-filter-toggle` 自体(`src/pages/box/index.astro:122-135`)は色しか上書きしていないので、ズレは丸ごとこの基底差に由来する。
  【裁定】**採用。`global.css` の `button` 基底で直す**(ページ側で個別に打ち消さない)。「行内に input と button が並ぶ」構成は他ページにも起こりうる**汎用の欠陥**で、31-L4 の真因(`<button>` だけ `input,select` の `min-height` の対象外)と**同じ系統の2件目**。ただし**全ページに波及する**ので、**専任エージェントに全ページのボタン高さ回帰確認までやらせる**(→ 実施結果節)。

- **32-U-A2 右パネル「ランク / 状態異常 / テラスタル」の横並び行で高さ・文字サイズが3段階に割れている**
  【実測】攻撃側・防御側どちらの `.damage-detail-chip-row` でも同じ:
  | 要素 | 高さ | フォントサイズ |
  |---|---|---|
  | ランク `input[type=number]` | 24.28px | 13.12px(0.82rem) |
  | 状態異常 `select` | 27.44px | 12.48px(0.78rem) |
  | テラスタル `button`(`.is-text-only`) | **28.375px** | **12px(0.75rem)** |
  【出どころ】`src/pages/box/[id].astro:3090-3097`(rank input)/ `:3130-3143`(ailment select。**コメントに「ボタン側の実測28.375pxに近づけて…差が3.9px→0.4px程度まで縮む」とあり、select→button の調整は行われたが rank-input だけ取り残された**)/ `:2958-2982`(icon-btn 基底)。
  【裁定】**採用。テラスタルボタンの 28.375px / 12px に3つとも寄せる**(select が既にそこを目標に調整された実績があるため、残る rank-input を合わせれば揃う)。**32-R3/32-R4 と同じ行を触るので、`[id].astro` 担当の実装者に追加で渡す。**

### 優先度B(同一画面内の離れた場所の揺れ)

- **32-U-B1 左パネルのステータス見出し行で「種族値/努力値/実数値」と「残りN」のサイズ・太さが違う**
  【実測】同じ `.stat-table-header` 行の中で、「種族値」等が **12.48px / 600**、`.ev-remaining`(「残り2」)が **12px / 700**。
  【出どころ】`src/pages/box/[id].astro:475-485`(`.stat-table-header { font-size: 0.78rem; font-weight: 600 }`)/ `:514-519`(`.ev-remaining { font-size: var(--font-size-xs); font-weight: 700 }`)。
  【裁定】**採用。共通トークン `var(--font-size-xs)`(12px)を持つ `.ev-remaining` 側を基準にし、`.stat-table-header` の直値 `0.78rem` をトークンに寄せる**(独自の直値より共通トークンを優先する)。**太さの 600 / 700 差は残す**(「残り」は可変の警告値で、階層差として正当)。

- **32-U-B2 ダメージ計算カードの努力値グリッドで、数値入力だけ 0.02rem 大きい**
  【実測】行ラベル「努力値/実数値」・列見出し「H/A/…」・実数値がいずれも **13.76px(0.86rem)**、**数値入力欄だけ 14.08px(0.88rem)**。
  【出どころ】`src/pages/box/[id].astro:2091-2096`(input `0.88rem`)vs `:2011-2015` / `:2021-2027` / `:2103-2107`(いずれも `0.86rem`)。**26-D4 で「+1.6px」を一律適用したときの改定前の差(0.02rem)がそのまま残った。**
  【裁定】**採用**(差は0.32pxで視認できないが、修正コストがほぼゼロで、同種の残骸を残すと次の一律改定でまた増幅する)。`0.86rem` に揃える。

- **32-U-B3 右パネルのチップ行の gap が 8px と 5.6px に割れている**
  【実測】`.damage-detail-chip-row` = `gap: 0.5rem`(8px)、`.damage-detail-icon-group`(天候・フィールド)= `0.35rem`(5.6px)、`.damage-detail-volatile-group`(その他の状態)= `0.35rem`(5.6px)。
  【出どころ】`src/pages/box/[id].astro:3118-3123` / `:2941-2945` / `:3172-3176`。
  【裁定】**採用。多数派(2対1)の 0.35rem に寄せる。** 32-R4(急所+じゅうでんを1行に)で 327px の幅を使うので、**詰まる方向の変更は好都合**。

### 優先度C(別ページ間の揺れ)

- **32-U-C1 「持ち物(アイコン+名前)」の文字ウェイトが `/box` と `/box/[id]` で逆転している**
  【実測】`/box` の `.card-item-name` = 12px / **600** / muted、`/box/[id]` の `.item-name-display` = 18px / **400**(既定)。アイコンは 18px と 24px。
  【裁定】**ウェイトのみ採用**(600 に揃える)。**サイズとアイコン比は揃えない**(箱の制約が違う。→ `00-foundation.md`「箱の制約が先」)。**⚠️ 32-L3 で持ち物名を 18px → 種族名と同じ 15.04px に変更するので、同じ実装者がまとめて行うこと。**

### Coordinatorの実測検証で新たに見つかった揺れ(第2波で実装する)

- **32-B5 `/box` でアイテムバッジ(18px)とテラスバッジ(25px)を縦に並べたことで、サイズ差が露出した**
  【実測】32-B2 の実装後、`.card-item-badge` = **18×18 / x=683.8**、`.card-tera-badge` = **25×25 / x=680.3**。**同じ列に縦に並んだことで、寸法差 7px と左端の 3.5px ズレが直接見比べられる状態になった**(横並びだった頃は離れていて気づかれなかった)。
  【裁定】**採用。確立規格の20pxに両方を揃える**(→ 28-L1〜L6「アイコン規格20px」)。**これは 32-B2 の実装が生んだ新しい揺れ**で、統一感レビュアーが監査した時点(実装前)には存在しなかった。**「指示された部分だけ規格が変わり、指示されなかった部分との差が開く」の実例がラウンド内で発生した**ことになる。

### ユーザー判断待ち(実装しない。次のメタレビューで棚卸しする)

- **32-U-U1 性格補正の表現方式が2種類ある**: 左パネルは **24×24px のクリック可能な三角形ボタン**、ダメージ計算カードの努力値グリッドと `/box` カードは **実数値の直後に付く Unicode の ▲▼**。前者は編集UI・後者は読み取り専用という**役割の違いで正当化できる**ため、統一感レビュアーも優先度を付けずに報告のみとした。**揃えるべきかはユーザーの好みの問題なので確認する。**

### 統一感レビュアーが監査して「逸脱なし」と確認した項目(記録)

- 数値グリッド本体値のサイズ差(左パネル16.8px / ダメージ計算カード13.76px / `/box`カード12.8px)は `00-foundation.md` で「箱の制約による従属変数」と確定済み。**再提起しない。**
- `.damage-row-total-result`(0.92rem)と `.damage-column-result`(0.89rem)の 0.48px 差は 23-D5 の明示判断(累計側をわずかに大きくして階層を作る)。**揃えない。**
- 攻撃側=赤系 / 防御側=青系 の色分けは意図的な識別(29-R2)。

---

## レビュー観点の改訂(ラウンド32時点。メタレビュー = fable の提案をCoordinatorが精査して反映)

**ラウンド30の申し送りどおり、3の倍数のこのラウンドでメタレビューを実施した**(前回はラウンド29)。メタアドバイザーはユーザー指示ラウンド(31・32)を「レビュアーが出せていたはずの指摘」として分析し、**穴を7種類に分類**した。反映した内容:

| 変更 | 何を・なぜ |
|---|---|
| **統一感レビュアーを新設**(32-U1) | ユーザーの明示要求。**8〜12項目の講評予算では画面全域のpx監査は構造的に不可能**で、31-L4(4.0pxズレ)・31-L6(行間20.8 vs 12.8px)がユーザー指摘まで残ったのはこれが原因、という分析。切り分けは「**要素と中身の突き合わせ=UI / 要素どうしの突き合わせ=統一感**」 |
| UIから【固定2】(整列・寸法比)【固定5】(部品の規格差)を**統一感へ移管** | 役割の重複を残すと同じ指摘が多重に上がる。(d) 主客転倒だけは判断が要るので固定1へ吸収 |
| UI【固定1】に**小型部品は3倍閾値で判定しない**を追記 | 31-D5(N発目ラベル34×61.4px)・31-D6(ヒット数3.6em)が**28-D2と同じく倍率閾値をすり抜けた**。ラウンド29で固定2は直したが**固定1の閾値を温存したので同型が再発した** |
| UIに新【固定2】**装飾・区切りが情報を運んでいるか**を新設 | ユーザーの一貫した好み(罫線より余白・背景色より透明・文字より絵)が31〜32の指示に繰り返し現れているのに、それを問う観点が無かった。**29-R2でレビュアー発案の色付けが32-R3で撤回されたのは象徴的** |
| UIに新【固定5】**規格そのものの妥当性**を新設 | 統一感レビュアー新設で生まれる失敗モード(**全員が「揃っているか」だけ見て規格自体の欠陥を誰も言わなくなる**)への対策 |
| UX【固定1】に**画像・アイコンの二重表示**を追記 | 31-L3(テラス画像が公式絵領域と選択欄の2箇所)が「テキストはUX・視覚はUI」の**切り分けの隙間**に落ちていた |
| UXに新【固定3】**常時表示情報の優先順位と退避**を新設 | **最大の穴。** 32-B1(ステータスをカードから削除)・32-R2(天候を最下部へ)のような「情報を退避・降格させる」提案が3レビュアーから一度も出ていない。プレイヤー視点は追加専業、UIの「過剰」は装飾の過剰、UXには情報の取捨選択の観点が無かった。**「機能追加は原則提案しない」+ 肥大する「維持すべき点」が、削る提案を萎縮させるラチェットとして働いていた** |
| UX【固定3】(ワイヤーフレーム差分)を**条件付きに降格** | ラウンド30で全画面の照合が完了し「未記録逸脱0件」。差分が新たに生まれるタイミングだけ回せばよい |
| UX【固定4】に**ホバー/フォーカス**を追加 | **静止スクリーンショットには写らないので一度もレビューされていなかった。** 31-D1/31-D2、32-B4 はいずれもホバーの問題 |
| プレイヤー視点の並び順に**操作頻度**と**追認の根拠水準**を追加 | ラウンド29で右パネルの並びを「妥当と追認」したが、ユーザーが31-R2・32-R2で二度覆した。**追認に指摘と同じ根拠水準を求めていなかった** |
| プレイヤー視点【固定3】(極端ケースの実データ集計)を**条件付きに降格** | 直近2ラウンドで新規の成果が無く、集計コストが重い |
| `SKILL.md` Step 2.5 に**レビュアーの新提案自体に「強調の層数」を適用する**を追加 | 29-R2 → 32-R3 の再発防止 |
| `SKILL.md` Step 7 に**「ユーザー判断待ち」キューの棚卸し**を追加 | **観点ではなくプロセスの穴。** テラスバッジの浮きは 27-B2で指摘 → 29で再確認 → **提示されないまま滞留** → 31-B1でユーザーが自分で指示、という経路をたどった。round-22 の U-1〜U-4 も10ラウンド以上未回答 |
| `SKILL.md` に **Step -1(worktree/ブランチの分離)** を新設 | 下記「実装中に見つかった運用事故」参照。**メタレビューとは独立したユーザー指示** |

**採用しなかった提案**: 特になし(統一感レビュアーの出力を「事実列挙型」に限定し裁定をCoordinatorに残す設計、報告閾値の明示は**そのまま採用した**)。

---

## 維持すべき点(壊さないこと)

- **育成ルール(IV=31固定 / 努力値0〜32スケール / Lv50固定 / 合計上限66)は仕様。バグ扱いしない。**
- **`.damage-row-total-result` / `.damage-column` / `.damage-column-result` / `.damage-row-columns-wrap` / `.damage-row-build` / `.damage-row-footer` / `.card-damage` / `#opponent-notes-section` / `.damages` / `.move` / `.pokemon-name-row` は改名禁止**(同ファイル内JSと `tests/e2e/damage-calc.spec.ts` が参照)。
- **`PUT /api/owned-pokemon/:id` は全項目上書き契約。** SSRで読んだ `level` / `tags` / `is_pinned` を `#edit-form` の `data-*` から載せ直す仕組みを壊さないこと。
- **2カラム化の閾値 1200px を下げないこと。**
- **文字12px下限**(20-G3)/ **アイコン規格20px**(28-L1〜L6。32-R1 のヘッダー画像はユーザー明示指示による例外)。
- **`.card-pokemon` は正方形**(32-B1 で寸法自体は変わるが、縦横が等しいことは維持)。
- **公式用語を幅の都合で短縮しない**(32-R5 の「かべ」はユーザー自身の明示指示なので例外)。
- **31-R1 の矢印の `direction` 追随**(32-R1 で矢印を作り替えても壊さない)。
- **29-R2 の攻撃側/防御側の識別**(32-R3 で線の色は外すが、文字色と形状マーカーは残す)。

---

## ラウンド32 の実施結果(2026-07-30)

実装は**3体のsonnetサブエージェントに分割して委譲**した(第1波: `src/pages/box/index.astro` と `src/pages/box/[id].astro` を並列 / 第2波: `src/styles/global.css` + `index.astro` → その後 `[id].astro`)。**検証はすべてCoordinatorが自分で実測・目視した。**

### 実施済み(実測値はすべてCoordinatorが再測定した値)

#### `/box`

| # | 結果 |
|---|---|
| 32-B1 | ✅ `.card-stat-grid` は `display: none`(DOM保持)。カードは **257.34 → 218.3px の正方形**(4枚とも縦横一致)。`.box-grid` の `minmax(220px→210px)`。**40体cloneで1画面27枚**(実装者が180pxまで詰めるとバッジがアクションボタンと衝突するため210pxに調整) |
| 32-B2 | ✅ `.card-item-badge` を `.card-badge-row`(`.card-art` の右、`left:100%`)へ移し、テラスの上に配置。`top: 60px` で `.card-actions`(y=156.1〜216.1)の直下。**公式絵・アクション・カード名のいずれとも重なりなし** |
| 32-B3 | ✅ `.card-item-row` を **[特性名] [持ち物名]** の2列 `space-between` に。両方 **12px / 600**、4個体すべて `scrollWidth === clientWidth`(省略なし)。**持ち物なしの個体でも列が崩れない** |
| 32-B4 | ✅ `title` を実数値+メモに作り替え(例: `実数値: H153 A200▲ B131 C135▼ D105 S136`)。**両方空なら `title` 自体を出さない。`aria-label` は不変** |
| 32-B5 | ✅ アイテム18px・テラス25px → **両方20px、x も一致**(680.3 / 1383.16)。480px以下も 14/18px → **16px に統一** |

#### `/box/[id]` 左サイド

| # | 結果 |
|---|---|
| 32-L1 | ✅ `padding-top` 24 → **16px**(パネル上端64.69 → アイコン箱80.69)。**`.species-sprite-img` は 144×144 のまま**(ユーザー追加指示) |
| 32-L2 | ✅ `.top-block-icon-corner` を `flex-end` → `flex-start`。タイプバッジ **x=495 / 521(間隔6px)**、`.item-image` も **x=495** で完全一致 |
| 32-L3 | ✅ アイテム画像 24 → **20px**(`var(--icon-size-sm)`)、持ち物名 18 → **15.04px = 種族名入力欄と同値**。「こだわりハチマキ」で `scrollWidth === clientWidth === 120` |
| 32-L4 | ✅ 持ち物→技 **24px** / 技→ステータス **32px**。**実測を同値にせず、技→ステータス側を広く取った**(入力欄の枠どうし vs 枠と文字ラベルで、視覚的な余白の見え方が違うため。31-L7 の再演を避ける狙い)。**技0件のディンルーでも 24/32px** |
| 32-L5 | ✅ ステータス→メモ **0.06px → 28px**(32-L4 の 24px より広く、別ブロックであることを示す) |
| 32-L6 | ✅ 縁取りを塗りと同系色に。ライトは A▲ `rgb(178,58,38)` / C▼ `rgb(26,126,161)`(塗り `rgb(201,67,44)` / `rgb(29,111,209)` と別値)。**ダークは下記 32-L6-dark で追加対応** |

#### `/box/[id]` ダメージ計算カード

| # | 結果 |
|---|---|
| 32-D1 | ✅ 相手アイコン 112 → **105.27px**(6%減)。`global.css` は非改変で `#opponent-notes-section` 限定。**左パネルの `.species-sprite-img` は144pxのまま** |
| 32-D2 | ✅ 未選択時のみ `.is-value-empty` で `--color-text-muted`(`rgb(91,100,114)`)+ 左寄せ。**選択済みは通常色 `rgb(31,36,48)`** |
| 32-D3 | ✅ 技カード列と累計結果を `max-width: calc(100% - 40px)` で **408.44 → 368.44px**。削除ボタンを `top` → `bottom` 基準にして**カード右下(累計結果の右、y=275.67)**へ。**31-D2 の副作用で入れた `padding-right: 40px` は不要になったので削除**。技列3列でも重なりなし |

#### `/box/[id]` 右サイド

| # | 結果 |
|---|---|
| 32-R1 | ✅ 見出しアイコン 28 → **32px**、矢印をテキスト「→」から**インラインSVGシェブロン(stroke-width 2.5)**へ。`.is-reversed` の `scaleX(-1)` で **`direction` 追随を維持**(攻撃カードで右、防御カードで左を確認) |
| 32-R2 | ✅ 順序が **選択見出し(79.4)→ 攻撃側/防御側(139.6)→ 天候(442.2)→ フィールド(520.1)**。天候・フィールドは攻撃側/防御側のどちらにも入れていない。クリック→`aria-pressed` 切り替え→復元も実機確認 |
| 32-R3 | ✅ 区切り線を `--color-border`(`rgb(227,230,238)`)に。**見出し文字色(赤/青)と形状マーカーは維持** |
| 32-R4 | ✅ 2段目が **急所 → じゅうでん の順**(`buildCriticalRow` → `buildCriticalButton` に改名し独立行を廃止)。327px内に収まり折り返し・省略なし |
| 32-R5 | ✅ 「壁」→「**かべ**」。`.damage-detail-volatile-group` の**先頭**へ。全10チップとも `scrollWidth === clientWidth`。**`title` の説明文は維持** |

#### 統一感レビュー由来(32-U)

| # | 結果 |
|---|---|
| 32-U-A1 | ✅ `global.css` の `button` 基底に `line-height: var(--line-height-snug)` を追加。**それだけでは3.2pxしか埋まらず**、縦paddingの `0.55em`(8.8px)→ `var(--space-2)`(8px)も揃えた(`.btn-primary` 等の2px枠補正式も追随)。**`#box-toolbar` の3要素が h=40.39 / y=64.88 / line-height 22.4px / padding 8px で完全一致** |
| 32-U-A2 | ✅ チップ行3要素が **すべて 25.98px / 12px**(攻撃側・防御側とも)。**目標値は 32-U-A1 の副作用で 28.375 → 25.98px に動いており、実装者に測り直させた** |
| 32-U-B1 | ✅ `.stat-table-header` を直値 `0.78rem` → `var(--font-size-xs)`。`.ev-remaining` と**ともに12px**。太さ 600/700 の階層差は維持 |
| 32-U-B2 | ✅ 努力値グリッドの入力欄 `0.88rem` → `0.86rem`。**行ラベル/列見出し/実数値/入力欄の4要素すべて 13.76px** |
| 32-U-B3 | ✅ `.damage-detail-chip-row` の gap `0.5rem` → `0.35rem`。**3つのチップ行がすべて 5.6px** |
| 32-U-C1 | ✅ `.item-name-display` に `font-weight: 600`。**サイズは 32-L3 の 15.04px を維持**(アイコン比も揃えない、の裁定どおり) |
| 32-L6-dark | ✅ ダークで A▲ の縁取り色が塗りと完全一致していた件を、**新色を作らず** `--color-danger-soft`(既存の半透明トークン、`global.css:156`)へ差し替え。6倍拡大クロップで縁取りが視認できることを確認 |

**共通**: `npx astro build` 成功 / `npm test` **298/298 pass**(件数の増減なし)/ 横スクロールなし / コンソールエラー **0件** / 1920・1440・1280・900・390px で破綻なし / 2カラム化の閾値1200pxは未変更。

### 実装中に見つかった実バグ・未然に防いだ事故

- **【実装者が発見・回避】32-R5 で「かべ」を揮発状態グループに入れた結果、選択状態を読み直すクエリが「かべ」ボタンまで拾って空文字列を混入させるところだった。** `[data-volatile-value]` の絞り込みを追加して回避(かべボタンにはこの属性を付けていない)。**気づかなければ「チェックを入れても再読み込みで消える」種類のバグとして出荷されていた。**
- **【実装者が発見・修正】32-B2 のバッジ移動が 390px 幅でカード名と重なっていた**(desktop寸法のバッジ46.2pxが、アクション下端〜カード名間の安全域34.9pxに収まらない)。`@media (max-width:480px)` でバッジを縮小して解消。**1920pxだけ見ていたら通過していた。**
- **【Coordinatorが発見・復元】ラウンド開始時、メガリザードンXの `tera_type` が基準値「ほのお」から `null` にズレていた**(ラウンド27に続き**2回目**)。`PUT` で復元し、`GET` で13フィールドが基準表と一致することを確認。**nullのままだとテラスバッジが `hidden` になり「バッジが消えた」と誤診するところだった。**
- **【Coordinatorの誤検知(記録として残す)】** 右パネルの「その他の状態」ラベルが `display: none` になっているのを回帰と疑ったが、**ラウンド28のユーザー指示(28-R9)「その他の状態、の表記を削除」による既存の仕様**だった(読み上げは `role="group"` + `aria-label` で維持)。**ソースのコメントに経緯が明記されていたので、ソースを読んだ時点で解決した。**

### 🔴 運用事故: 別プロセスが同じワーキングツリーでコミットした(ユーザー指摘)

**実装エージェント2体が走行中に、別セッションが同じワーキングツリーで `git commit` を実行し、作業途中の変更が丸ごとコミット `e82db52`(「UI改善ラウンド24-32 + チーム編成機能の追加」)に巻き込まれた。** ラウンド32はその時点で未完成だった。

- **ユーザー指示: 「/loop 作業はworktreeとブランチを分けて実施して。指示書の手順にもそのように記載して」**
- **対応**: `SKILL.md` に **Step -1「作業用のworktreeとブランチを分ける」** を新設(`EnterWorktree` / `git worktree add` の手順、`npm install` が要ること、dev serverのポート、**サブエージェントにworktreeの絶対パスを渡すこと**、**DBはworktree間で共有されるのでデータ事故の注意は一切軽くならないこと**)。`pitfalls.md` にも事故として記録した。
- **このラウンドでは途中適用を見送った**(実装エージェントは起動時のCWDに固定されるため、走行中に移すと担当ファイルを見失う)。**次のラウンドから Step -1 を最初に実行する。**

### 積み残し(次に着手する項目)

1. **技3列のカードは技列側の余白が広めに空く**(32-D3 で幅を 368.44px に減らしたぶん。実装者は「『少し減らす』の範囲内」と判断)。
2. **右パネル下部の大きな余白**(31-R1 で 29-R1 の中央寄せを撤回した帰結。ユーザーの明示指示による。**29-R1 を復活させないこと**)。天候・フィールドを最下部へ移した(32-R2)ことで中身の終端は 570px 付近まで下がったが、1080pxのパネルにはまだ空きがある。
3. **ラウンド30の積み残し B-4**(`/box/[id]` の `backHref` を `?from=/team/<id>` で切り替える)は未着手のまま。
4. **`.card-actions` が固定60pxで、`/box` のカードをこれ以上詰めるにはここの見直しが要る**(32-B1 の実装者の申し送り)。

### ✅ ユーザー判断待ちの棚卸し(Step 7。**2026-07-30 にユーザーが5件すべて回答した**)

**`SKILL.md` Step 7 に新設した「ユーザー判断待ちキューの棚卸し」を初回実施した。round-22 の U-1〜U-4 は10ラウンド以上滞留していたが、提示したその場で全件決着した。** → **滞留の原因は判断の難しさではなく、提示していなかったことだった。**

| # | 項目 | 出典 | **ユーザーの回答** |
|---|---|---|---|
| U-1 | `/search` の「最近注目されているもの」が常に空セクション | round-09.md B-5 | **DOMごと削除する** → ラウンド33で実装 |
| U-2 | トップバーの「ログイン中: 」接頭辞 | round-12.md C-2 | **接頭辞を削除する**(メールアドレスだけ表示。移設先がログアウトボタンの真上なので文脈で伝わる)→ ラウンド33で実装 |
| U-3 | 努力値の「残りN」に上限66の分母を出すか | round-17.md | **現状維持**(「残り18」のまま)。**2度変更して現在の形に落ち着いており、分母を出すと入力制限のない仕様と矛盾して見える。以後この項目は再提起しない** |
| U-4 | `/moves` の変化技で下部に約534pxの空白 | round-19.md | **今は手を付けない**(情報量を増やさない限り解けない。他に優先度の高い項目が無くなってから検討する) |
| 32-U-U1 | 性格補正の表現方式が2種類ある(左パネル=クリックできる三角形ボタン / 他=実数値直後のUnicode ▲▼) | 32-U(統一感レビュアー) | **現状維持**(「操作するボタン」と「読むだけの表示」の役割差。揃えると「押せるように見えるが押せない」要素が増える)。**統一感レビュアーはこの2種を逸脱として再提起しないこと** |

**U-3 / U-4 / 32-U-U1 は「現状維持」で決着したので、以後のレビューで再提起しないこと**(→ 各レビュアーテンプレートの「計画書に『撤回済み』『再提起しない』と記録された項目を再提起しないこと」に該当する)。
