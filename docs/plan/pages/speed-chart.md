# すばやさ早見表(`/speed-chart`)

## フェーズ表

| フェーズ | 内容 | 状態 |
|---|---|---|
| P1 | 仕様を固める | ✅ 完了(2026-08-01) |
| P2 | 設計レビュー(3体並列) | ✅ 完了(2026-08-01) |
| P3 | 計画書に記録 | ✅ 完了(2026-08-01) |
| P4 | 実装 | ✅ 完了(2026-08-01) |
| P5 | 受け入れ検証 | ✅ 完了(2026-08-01 受け入れ基準33件すべてpass) |
| P6 | 引き渡し・commit | ✅ 完了(2026-08-01) |

---

## このページが答える問い

**「今このレギュレーションで、自分の育成中の個体はどのポケモンより先に動けるのか。あと何を変えれば抜けるのか」**

## 参照した資料(P1で実際に開いたもの)

- `docs/ui_proposal/すばやさ早見表.png` — **構造の正**。2列(実数値 / ポケモン)の縦長テーブル。1つの実数値行に複数の「振り方バッジ + 種族値ラベル + 補正ラベル」グループが入り、各グループの下にポケモンのアイコン+名前+補正の出どころ(特性名/技名)が並ぶ。
- `docs/ui_proposal/すばやさライン早見表 シーズンM-2(シングルバトル)|バトルデータベース チャンピオンズ.html` — 他者サイト例。ラベルの語彙(`最速` / `準速` / `120族 2倍` / `110族 S+2` / `123族 こだわりスカーフ`)はここに合わせる。**ただし母集団の絞り方は真似しない**(向こうは使用率TOP100 + 採用率10%以上。こちらはレギュレーション全件)。
- `docs/ui_proposal/個体編集.png` / `個体編集_全体レイアウト.png` / `個体編集_右サイド.png` — 遷移元。左パネルのステータス表(H/A/B/C/D/S × 種族値・努力値スライダー・実数値)のS行が入口になる。

## ユーザー確定仕様(2026-08-01 `AskUserQuestion`)

1. **独立ページ + 個体連携パラメータ**。`/speed-chart?owned=<個体id>` を作り、`/box/[id]` 左パネルのS行から遷移する。行クリックで `PUT /api/owned-pokemon/:id` を叩いて保存し、右カラムの表示を即時更新する。サイドバー「育成」にも項目を追加し、個体を持たずに素の早見表としても開ける。
2. **補正の範囲は「持ち物 + 特性 + ランク上昇技」**(参考サイト相当)。
3. **振り方は 最速 / 準速 / 無振り の3種**。

## ルーティング

| 項目 | 内容 |
|---|---|
| URL | `/speed-chart`(個体連携時は `?owned=<uuid>`、レギュレーション指定は `?reg=M-B`) |
| ファイル | `src/pages/speed-chart/index.astro` |
| レンダリング | SSR(`export const prerender = false;`) |
| 認証 | **不要**。未ログインでも表は見られる。`?owned=` が付いていて未ログイン/他人の個体/存在しないIDの場合は、右カラムだけを出さずに表は表示する(`/box/[id]` の「強制リダイレクトしない」方針を踏襲) |
| レイアウト | `AppLayout` / `current="speed-chart"` / `backHref` は `?owned=` があるとき `/box/<id>`、無いとき無し |

## スコープ内

### A. 早見表本体(左〜中央)

- **レギュレーション選択**。`REGULATIONS`(`src/lib/regulations.ts`)から選ぶ。既定値は「`?reg=` → 連携個体の `regulation` → `REGULATIONS` の末尾(最新)」の順に決める。**レギュレーション未指定は許さない**(「所属するポケモン」が定義できないため)。選択を変えたらURLの `?reg=` を `history.replaceState` で書き換える。
- **母集団**: そのレギュレーションで使える**全フォルム**。
  - 基礎: `vendor/jpoke/src/jpoke/data/regulation/pokemon.csv` で当該列が `1` の種族(M-A 213件 / M-B 235件)
  - メガシンカ: `autocomplete/mega-stones.json` の `species` のうち、対応する `item` が `regulation/item.csv` の当該列で `1` のもの(M-A 60件 / M-B 76件)。アプリはメガを独立した `species_name` として扱う(`src/lib/box-id/shared-core.ts` の `resolveMegaStoneItem`)ため、表でもメガは1フォルム=1行グループとして扱う。
  - 合計 **M-A 273フォルム / M-B 311フォルム**(P1で実測)
- **振り方(3種)**。チャンピオンズルール(IV=31固定・努力値0〜32・Lv50固定)で計算する。計算式は `src/lib/stats.ts` の `calcOtherStat` を**そのまま使う**(再実装しない)。
  | ラベル | 努力値S | 性格補正 |
  |---|---|---|
  | 最速 | 32 | 1.1 |
  | 準速 | 32 | 1.0 |
  | 無振り | 0 | 1.0 |
- **補正(すばやさ上昇要素)**。すべて jpoke のソースを唯一の出典とし、**アプリ側にポケモン名・技名・特性名をハードコードしない**。
  | 種別 | 対象 | 倍率/段階 | 表示ラベル |
  |---|---|---|---|
  | 持ち物 | こだわりスカーフ | `floor(S * 6144 / 4096)` | `こだわりスカーフ` |
  | 特性(倍率) | かるわざ / すいすい / すなかき / ゆきかき / ようりょくそ / サーフテール | 2倍 | `2倍` + 特性名 |
  | 特性(倍率) | はやあし / こだいかっせい / クォークチャージ | `floor(S * 6144 / 4096)` | `1.5倍` + 特性名 |
  | 特性(ランク) | かそく(+1) / いかりのこうら(+1) / びびり(+1) / ふくつのこころ(+1) / くだけるよろい(+2) / じょうききかん(+6) | `floor(S * (2+n)/2)` | `S+n` + 特性名 |
  | 技(ランク) | +2: からをやぶる / ギアチェンジ / こうそくいどう / みをけずる / ロックカット<br>+1: アクアステップ / おかたづけ / オーラウイング / オーラぐるま / くさわけ / しょうりのまい / スケイルショット / ソウルビート / ちょうのまい / ニトロチャージ / はいすいのじん / りゅうのまい | `floor(S * (2+n)/2)` | `S+n` + 技名 |
  - **その特性/技を実際に持てるフォルムにだけ**付ける(`detail/pokemon.json` の `abilities` / `learnset` で判定)。
  - **確率発動の追加効果は除外する**(あやしいかぜ / ぎんいろのかぜ / げんしのちから の10%上昇)。
  - 補正は**重ねがけしない**(スカーフ+りゅうのまいのような組み合わせは行にしない)。参考サイトと同じ。
  - 下降補正(スロースタート・くろいてっきゅう・まひ)は**出さない**。
- **行の構成**: 実数値の降順。同じ実数値に該当する組み合わせは1行にまとめ、行の中で「振り方 + 種族値ラベル + 補正ラベル」のグループに分ける(PNGの構造)。P1実測で **M-B: 3,024エントリ → 271の実数値行**。
- **ポケモン画像**: `autocomplete/pokemon.json` の `imageId`(`dexNo` ではない)。`src/lib/sprite-urls.ts` の既存ヘルパーを使う。

### B. 「この個体」カラム(右、PNGに無い追加要件)

`?owned=<id>` があり、かつログイン中の本人の個体であるときだけ表示する。

- **その個体で実現可能なすばやさ実数値**を列挙する。組み合わせは以下の直積(重ねがけあり):
  - 性格: S上昇(1.1) / 無補正(1.0) / S下降(0.9)
  - S努力値: 0〜32 の全33段階
  - 持ち物: 現在の持ち物 / こだわりスカーフ(そのレギュレーションで使えるとき)
  - **特性・技によるランク上昇は含めない**(その個体の「素の」到達可能値のみを扱う。ランク補正は戦闘中の一時的な状態であり、保存対象の育成内容ではないため)
- 表の各実数値行に対し、**「その値ちょうどに到達できる最小コストの組み合わせ」**を右カラムに出す。同じ実数値に複数の組み合わせがあるときは **S努力値が最小 → 性格補正が現在値と同じ → 持ち物が現在値と同じ** の順で1つに決める。
- 現在の個体のすばやさ実数値の行には `← 現在` を出す(クリック不可)。
- **クリックで適用**: `PUT /api/owned-pokemon/:id`(**全項目上書き**契約)で、その組み合わせの `nature` / `evs[5]` / `item_name` を書いた**全項目ペイロード**を送る。成功したら右カラムの `← 現在` を移動し、上部の個体サマリ(種族・性格・持ち物・S実数値)を更新する。
- **努力値合計はブロックしない**。チャンピオンズルールの合計上限66は入力制限を持たない仕様(`.claude/skills/jpoke/references/ruleset.md`)なので、超過する場合は**警告表示のみ**を出して適用は通す。
- 適用後に `/box/<id>` へ戻れる導線(トップバーの `backHref`)を残す。

### C. 遷移元(`/box/[id]` 左パネル)

- ステータス表の**S行の実数値セルの隣**に、早見表へのリンク(アイコンボタン)を1つ追加する。`href="/speed-chart?owned=<id>"`。
- **これ以外の `/box/[id]` の変更は禁止**(左パネルは8,500行のうちの1領域。ボタン1個の追加に留める)。

### D. マスターデータ生成の拡張(`scripts/build-master-data/extract_autocomplete.py`)

DBマイグレーションは**不要**。代わりに以下を追加する。

1. `autocomplete/pokemon.json` の各要素に `regulations: string[]` を追加(`jpoke.data.pokedex.POKEDEX[name].regulations`)。
2. `autocomplete/items.json` の各要素に `regulations: string[]` を追加(`jpoke.data.item.ITEMS[name].regulations`)。
3. **新規** `detail/speed-modifiers.json` を出力する。jpoke のハンドラ関数を `inspect.getsource()` して抽出する(P1で実行して動作確認済み):
   ```json
   {
     "items":     { "こだわりスカーフ": { "kind": "multiplier", "numerator": 6144, "denominator": 4096 } },
     "abilities": { "かるわざ": { "kind": "multiplier", "numerator": 2, "denominator": 1 },
                    "かそく":   { "kind": "rank", "stages": 1 } },
     "moves":     { "ロックカット": { "kind": "rank", "stages": 2 } }
   }
   ```
   - ランク上昇の抽出: `modify_stats(...{"spe": +n})` / `modify_attacker_stats(..., stats={"spe": n})` を正規表現で拾い、`n > 0` かつハンドラのソースに `chance|random|確率` を含まないものだけ採用する。
   - 倍率の抽出: `ON_CALC_SPEED` ハンドラのソースから `apply_fixed_modifier(value, N)` と `value *= N` を拾う。**下降(N < 4096 / 除算)は出力しない**。
   - **抽出結果が空になったら生成を失敗させる**(jpokeの実装変更でサイレントに空の表になるのを防ぐ)。

## スコープ外(今回作らない。将来やるなら上から)

- **使用率による絞り込み・強調**(参考サイトの「TOP100のみ」「TOP15は赤文字」)。`ranked_teams` / `popular_build_suggestions` にデータはあるが、早見表の第一目的(レギュレーション全件の把握)とは別軸なので今回は入れない。
- **補正の重ねがけ**(スカーフ+りゅうのまい等)。行数が爆発し、参考サイトも採用していない。
- **下降補正**(まひ・くろいてっきゅう・スロースタート・トリックルーム)。
- **ダブルバトル固有の補正**(おいかぜ)。
- **`?owned=` の個体に対する技・特性の書き換え**。右カラムが変更するのは性格・S努力値・持ち物の3つだけ。
- **Pyodide / ダメージ計算エンジンの読み込み**。このページは `src/lib/stats.ts` の純JS計算しか使わない(`references/stack.md` §7「ダメージ計算を必要としないページでPyodideを読み込まない」)。

## ファイル分割案(実装前に確定させる)

画面は「早見表(左〜中央)」と「この個体(右)」の2領域を持ち、後者は前者が無くても意味を持たない一方、前者は後者無しで完結する。`box/[id].astro` の失敗(3領域を1クロージャに同居)を繰り返さないため、最初から分割する。

| ファイル | 役割 | 依存 |
|---|---|---|
| `src/lib/speed-chart.ts` | **純粋関数のみ**。実数値の算出・補正の適用・行の組み立て・個体の到達可能値の列挙。DOM/fetch/Node APIに一切依存しない(`src/lib/stats.ts` と同じ制約。SSRとブラウザの両方から使う) | `src/lib/stats.ts` |
| `src/lib/speed-chart-validation.ts` | `?reg=` / `?owned=` の検証、適用ペイロードの検証。**純粋関数**(ユニットテストの対象) | `validation-primitives.ts`, `regulations.ts` |
| `src/pages/speed-chart/index.astro` | オーケストレーター。SSRでクエリ解決・個体取得・レギュレーション決定を行い、2つのコンポーネントを呼ぶだけ | 下2つ |
| `src/components/speed-chart/ChartTable.astro` | 早見表本体のテンプレート | — |
| `src/lib/speed-chart/chart-table.ts` | 早見表本体のブラウザ側ロジック(レギュレーション切替・描画) | `speed-chart.ts` |
| `src/components/speed-chart/OwnedPanel.astro` | 「この個体」カラムのテンプレート | — |
| `src/lib/speed-chart/owned-panel.ts` | 「この個体」カラムのブラウザ側ロジック(到達可能値の算出・適用のPUT) | `speed-chart.ts` |

- **共有コア**は `src/lib/speed-chart.ts` に集約する。2領域が共有する状態は「現在のレギュレーション」と「現在選択中の実数値行」の2つだけ。**どちらも `chart-table.ts` が所有し、`owned-panel.ts` へは `CustomEvent` かコールバック登録で渡す**(クロージャ共有をしない)。
- `is:global` のCSSは領域ルートクラス(`.speed-chart-table` / `.speed-chart-owned`)で必ず絞り、ファイルの読み込み順に依存しないようにする(`references/stack.md` の指摘)。

## レイアウト方針(`00-foundation.md` 準拠)

- 早見表は「読む対象」なので `.panel-content { max-width: 1080px }` の内側に収める。レギュレーション選択などの操作系ツールバーは全幅でよい。
- 「この個体」カラムは `.panel-right` として画面右端まで伸ばす(角丸・外側marginなし)。
- 配色は既存トークンのみ(primary `#27acd9` / success `#409f89` / danger `#f8705b` / risky `#c9820f`)。**新色は作らない。** 振り方バッジ(最速/準速/無振り)は既存トークンの濃淡で描き分ける。
- 「タイル」という語は使わない。

## 受け入れ基準

| # | 基準 | 確認方法 |
|---|---|---|
| 1 | `/speed-chart` が 200 で開き、既定でレギュレーションが1つ選ばれている | HTTPステータス + `<select>` の `selected` |
| 2 | レギュレーション `M-B` を選ぶと **311フォルム**が母集団になる(重複なし)。`M-A` では **273フォルム** | DOMのフォルム名を `Set` に集めて件数を数える |
| 3 | 表の行数(=distinct な実数値)が `M-B` で **271行**、実数値の降順に並んでいる | 行数カウント + 隣接行の単調減少をJSで検証 |
| 4 | 補正なし「最速」の実数値が `src/lib/stats.ts` の `calcOtherStat(50, base, 31, 32, 1.1)` と全フォルムで一致する | 全フォルムを走査して突き合わせ(0件不一致) |
| 5 | こだわりスカーフ行の値が `floor(素の値 * 6144 / 4096)` と一致する | 抜き取り10件を実測 |
| 6 | `S+2` 行の値が `floor(素の値 * 2)`、`S+1` 行が `floor(素の値 * 1.5)` と一致する | 抜き取り10件を実測 |
| 7 | あやしいかぜ / ぎんいろのかぜ / げんしのちから が表に一切出ない | DOM全文検索で0件 |
| 8 | 特性・技の補正が、その特性/技を持たないフォルムには付いていない | `detail/pokemon.json` と全件突き合わせ(0件不一致) |
| 9 | `/box/[id]` のS行に早見表リンクがあり、`/speed-chart?owned=<id>` へ遷移する | 実際にクリックしてURL確認 |
| 10 | `?owned=<自分の個体>` で右カラムが出る。`?owned=<他人の個体>` / 存在しないID / 未ログインでは右カラムが出ず、表は出る | 4パターンを実際に開く |
| 11 | 右カラムの「ここにする」をクリックすると `PUT /api/owned-pokemon/:id` が飛び、`nature` / `evs[5]` / `item_name` が期待値になる | ネットワークを見る + `/box/[id]` を再読込して値を確認 |
| 12 | 適用後、`← 現在` マーカーがクリックした行へ移動する | DOM |
| 13 | 努力値合計が66を超える組み合わせを適用すると警告が出るが、適用自体は成功する | 実際に操作 |
| 14 | `npm run build` 成功 | 実行 |
| 15 | `npm test` 全件pass。`tests/speed-chart-validation.test.ts` と `tests/speed-chart.test.ts` が**新規に増えている** | 実行前後の件数を報告 |
| 16 | このページで Pyodide が読み込まれない | Networkタブ / `window` に engine が生えないこと |
| 17 | 1920×1080 ライト・ダーク両方でレイアウト崩れ・横スクロールが無い | `npm run shot` で撮って目視 |
| 18 | `/box` `/box/[id]` に回帰が無い | `npm run shot -- --page box` + 目視 |
| 19 | 検証で作った個体・書き換えた値がDBに残っていない | 検証後にDBを確認 |

## P1で判断して決めたこと(ユーザーに聞かなかった論点と根拠)

- **母集団に使用率フィルタを入れない** — 要件2が「指定したレギュレーションに属するポケモンのすばやさを一覧できる」なので全件が正。実測で271行に収まり、絞らなくても読める。
- **補正を重ねがけしない** — 参考サイトも重ねていない。重ねると行数が1桁増え、「どの1手で抜けるか」という表の目的がぼやける。
- **右カラムでランク上昇を扱わない** — ランク補正は戦闘中の一時状態で、`owned_pokemon` に保存できる育成内容(性格・努力値・持ち物)ではない。「クリックしたらステータスやアイテムが更新される」という要件4の文面とも整合する。
- **努力値合計66超をブロックしない** — チャンピオンズルールは入力制限を持たない仕様(メモリ/`ruleset.md`)。既存の `/box/[id]` も超過を許して警告のみ出している。
- **ページ自体に認証を要求しない** — 表の中身は公開マスターデータのみで個人情報を含まない。`?owned=` の解決だけが認証を要する。

---

# 設計レビュー(2026-08-01 データ設計/導線/プレイヤー視点・Coordinator統合)

3体のsonnetレビュアーを並列起動して得た指摘を、Coordinatorが重複排除・矛盾解消したうえで統合した。
**Coordinatorが自分で再現・実測して裏を取った指摘だけを採用している**(レビュアーの報告をそのまま信じていない。実際に1件は事実誤認だった → 却下R-X1)。

## 採用した指摘

### R-1 `PUT /api/owned-pokemon/:id` の全項目上書きで、画面が持たない項目が消える(指摘者: データ設計A-1 / 導線A-7)

【問題】`src/lib/owned-pokemon-validation.ts` の `REPLACE_REQUIRED_FIELDS` は `tags` / `memo` / `move_names` / `is_pinned` / `regulation` / `tera_type` / `ivs` を含む全13項目を**必須**とするが、「値がその個体の現在値か」は一切検証しない。`updateOwnedPokemon` もペイロードの列を無条件でUPDATEする。右カラムが `null` / `[]` / `false` のプレースホルダで埋めて送るとバリデーションを素通りし、**個体のメモ・タグ・技が黙って消える**。

【対応】仕様を次のように確定する。

- `index.astro`(SSR)が `getOwnedPokemon()` で取得した **`OwnedPokemonRecord` 全体**を `OwnedPanel` へ渡す。渡し方は `<script type="application/json">` での埋め込み(`ivs` / `evs` / `move_names` / `tags` は配列なので `data-*` 属性化に向かない)。
- `owned-panel.ts` は**必ずそのレコード全体を spread してから** `nature` / `evs` / `item_name` の3項目だけを上書きしてPUTする。
- 受け入れ基準 #20 を追加。

### R-2 補正の抽出は「手作業リスト」ではなく「jpokeの機械走査 + レギュレーション絞り込み」にする(指摘者: データ設計A-2 / プレイヤーA-1)

【問題】P1の対応表は手作業のピックアップだった。Coordinatorが検証したところ、**正規表現の書き方ひとつで両方向に取りこぼす**。

- `modify_(?:attacker_)?stats\(...\{..."spe"...\}` 方式 → `でんきエンジン`(`_apply_type_absorb(..., stats={"spe": 1})` 経由、`vendor/jpoke/src/jpoke/handlers/ability.py`)を取りこぼす。M-Bに `エモンガ` が実在する。
- `stats=\{..."spe"...\}` 方式 → 逆に `かそく` / `くだけるよろい`(`battle.modify_stats(mon, {"spe": +1})` の**位置引数**)を取りこぼす。
- P1のリストは `こうそくスピン`(+1)も落としていた。

【対応】**関数呼び出し名に依存しない**抽出に変える。Coordinatorが実行して確定させた決定版のパターン:

- 採用: ハンドラのソース中の**dictリテラルに含まれる `"spe": 正の数`**(`\{[^{}]*?"spe"\s*:\s*\+?(\d+)[^{}]*?\}`)**または** `stat="spe", amount=+n`
- 除外: ソースに `chance=` / `確率` を含むもの(確率発動の追加効果)、`modify_defender_stats` / `lower_defender` を含むもの(相手への低下)
- **`ITEMS` / `ABILITIES` / `MOVES` の全件を走査**したうえで、`regulation/item.csv` と `detail/pokemon.json` の `abilities` / `learnset` で絞る。**下の一覧は期待値の記録であって、抽出の入力ではない**(アプリにもスクリプトにも技名・特性名をハードコードしない)。
- **分母の `4096` はハードコードしてよい。** `FIXED_POINT_BASE` は `vendor/jpoke/src/jpoke/utils/constants.py:17` の別ファイル定数で、呼び出し側のソース文字列(`apply_fixed_modifier(value, 6144)`)には現れず `inspect.getsource()` 方式では取得できない。**この事実をコードコメントに残す。**

**Coordinatorが実行して確定させた抽出結果(vendor/jpoke v0.2.0時点):**

| 種別 | ランク上昇 |
|---|---|
| 特性(7件) | くだけるよろい+2 / じょうききかん+6 / いかりのこうら+1 / かそく+1 / でんきエンジン+1 / びびり+1 / ふくつのこころ+1 |
| 技(18件) | **+2**: からをやぶる・ギアチェンジ・こうそくいどう・みをけずる・ロックカット<br>**+1**: アクアステップ・おかたづけ・オーラウイング・オーラぐるま・くさわけ・こうそくスピン・しょうりのまい・スケイルショット・ソウルビート・ちょうのまい・ニトロチャージ・はいすいのじん・りゅうのまい |
| 持ち物(3件) | からぶりほけん+2 / カムラのみ+1 / ビビリだま+1 → **3件ともM-A/M-Bの `item.csv` に未収録なので表には出ない**(実測確認済み) |

| 種別 | 倍率(`ON_CALC_SPEED` 全10ハンドラを確認) |
|---|---|
| 2倍 | かるわざ / すいすい / すなかき / ゆきかき / ようりょくそ / サーフテール |
| 1.5倍(6144/4096) | はやあし / こだわりスカーフ / (こだいかっせい・クォークチャージ → **R-3で除外**) |
| 下降(出さない) | くろいてっきゅう(2048) / スロースタート(2048) |

### R-3 `こだいかっせい` / `クォークチャージ` を表から除外する(指摘者: データ設計A-2 / プレイヤーB)

【問題】この2特性の1.5倍は「その個体の実数値(ランク補正込み)が最大の能力**1つだけ**」に乗る(`vendor/jpoke/src/jpoke/handlers/ability_paradox.py` の `_select_paradox_boost_stat` / `modify_speed`)。**種族が持っているかどうかでは決まらず、努力値・性格配分で対象が変わる。** 種族単位で機械的に「1.5倍」行を出すと、実際には乗らない個体まで乗るかのように表示してしまう。

【対応】**この2特性は倍率表から除外する。** 「その特性を持てるフォルムにだけ付ける」という種族単位の判定基準がそもそも成立しない補正だから。

- なお **M-A / M-B にはこの2特性を持つ種族が1件も存在しない**(Coordinator実測。`regulation/pokemon.csv` の所属種族と `detail/pokemon.json` の `abilities` を突き合わせて0件)。よって**今日の表示は1行も変わらない**。将来 jpoke にパラドックス勢を含むレギュレーションが増えたときに誤った行が出ないようにするための予防措置。

### R-4 メガシンカ種族に `こだわりスカーフ` を付けない(指摘者: プレイヤーA-2)

【問題】メガシンカ種族は持ち物がメガストーンに固定されるため、こだわりスカーフを同時に持てない。P1の設計は特性・技については「持てるフォルムにだけ付ける」と書いたのに、**持ち物にだけこの制限を書いていなかった**。早見表本体にありえない行が出るだけでなく、右カラムの「持ち物: 現在の持ち物 / こだわりスカーフ」の直積経由で**ゲーム上ありえない `item_name` をDBに保存してしまう**。

【対応】

- 早見表本体: `autocomplete/mega-stones.json` の `species` に載っている名前には、こだわりスカーフの行を生成しない。
- 右カラム: 連携個体の種族がメガ種族なら、持ち物の軸を「現在の持ち物」のみにする(スカーフを候補に出さない)。
- 受け入れ基準 #21 を追加。

### R-5 ワイヤーフレームのメガの特性名は誤っているので、構造だけを参照する(指摘者: プレイヤーA-3)

【問題】`すばやさ早見表.png` は「メガエアームド / くだけるよろい」「メガサメハダー / かそく」と描いているが、実データでは **メガエアームドの特性は `すじがねいり` のみ**(`くだけるよろい` は進化前エアームドの隠れ特性でメガ後に引き継がれない)、**メガサメハダーは `がんじょうあご` のみ**(`かそく` は無印サメハダー側)。Coordinator実測で確認済み。技ベースの例(メガキラフロル/ロックカット、メガプテラ/りゅうのまい)は `learnset` と一致していて正しかった。

【対応】**ワイヤーフレームは構造・レイアウトの正であり、ポケモン名・特性名の実例は正ではない。** P5の受け入れ検証で「ワイヤーフレームと違うポケモンが出ている」ことを不具合と誤認しない。

### R-6 `?owned=` 付きで開いたら個体の現在地へ自動スクロールする(指摘者: 導線A-3)

【問題】表の総高さは概算で **30〜40画面分**(271行 × 平均11.2エントリ)。ユーザーの導線は「個体編集のS行から来る → まず自分の現在地を見る」なので、開いた瞬間に現在地が画面外だと最初の一歩でつまずく。

【対応】`?owned=` があり右カラムが出るときは、個体の現在のS実数値の行へ初期スクロールする(`scrollIntoView({ block: "center" })`)。受け入れ基準 #22 を追加。

### R-7 右カラムの「到達不可」状態を明示的に描き分ける(指摘者: 導線A-2)

【問題】右カラムの候補は「性格3種 × S努力値33段 × 持ち物2種」の直積だが、丸めで多数が同じ実数値に潰れる。**Coordinator実測で、到達可能な相異なる実数値はドラパルト134通り / ハバタクカミ132通り / カイリュー103通り / ドオー69通り**。271行の**過半数が空欄になる**。空欄が「届かない」のか「読み込み中」なのか「壊れている」のか区別できない。

【対応】右カラムのセルは**3状態を目視で区別できるようにする**。

| 状態 | 表示 |
|---|---|
| 現在値 | `← 現在`(success トークン。クリック不可) |
| 到達可能 | `[ここにする]` ボタン + **内訳**(性格名 / S努力値 / 持ち物)。内訳を出すのは導線A-7「性格を変えると別の能力が下がることが分からないまま確定してしまう」への対応を兼ねる |
| 到達不可 | `−`(muted。ボタンではないことが分かる見た目) |

### R-8 目的の実数値へたどり着く手段を用意する(指摘者: 導線A-4)

【問題】30〜40画面分のスクロールに対し、レギュレーション切替以外のナビゲーション手段が無い。実数値は表中に何度も現れるのでブラウザの Ctrl+F も効かない。

【対応】ツールバーに **(a) 実数値のジャンプ入力**(数値を入れるとその値以上で最も近い行までスクロール)と **(b) 「現在地へ戻る」ボタン**(`?owned=` があるときだけ表示)を置く。ポケモン名での絞り込みはスコープ外(却下R-X5)。

### R-9 記号・略語の目視できる凡例を置く(指摘者: 導線A-5)

【問題】`最速` / `準速` / `無振り` / `120族` / `S+2` / `2倍` の意味が目視で追えない。参考サイトにも凡例は無いが、このアプリは既存ページで H/A/B/C/D/S に `aria-label` でフルネームを残す方針を取っており、今回だけ説明が無いのは一貫性を欠く。`aria-label` はスクリーンリーダー限定で目視ユーザーへの凡例にならない。

【対応】表の上(ツールバー内)に1行の凡例を置く。文言: `最速=S上昇性格+努力値32 / 準速=無補正+努力値32 / 無振り=無補正+努力値0 / N族=すばやさ種族値N / S+n=ランクn段階上昇 / 2倍・1.5倍=特性による倍率`

### R-10 保存失敗時の表示を決める(指摘者: 導線A-6)

【問題】4つの失敗状態のうち「適用の保存失敗」だけが完全に未設計。受け入れ基準13も成功ケースしか見ていない。

【対応】PUT失敗時は (a) エラー文言を右カラム上部に出す(danger トークン)、(b) `← 現在` マーカーを**動かさない**、(c) クリックした候補を再クリックできる状態に戻す。適用成功後もページに留まり、続けて別の行を適用できる(1回で終わりにしない)。受け入れ基準 #23 を追加。

### R-11 `/box/[id]` のS行へのリンク追加は「隣に足す」では実現できない(指摘者: 導線A-8)

【問題】Coordinator実測。`src/components/box-id/LeftPanel.astro` の `.stat-row` と `.stat-table-header` は `grid-template-columns: 6.4rem 2.4rem minmax(0, 1fr) 3.2rem 3.2rem` の**5列固定グリッドをH〜Sの6行すべてで共有**しており、同ファイル617行目に「**`.stat-table-header` と `.stat-row` の `grid-template-columns` は必ず同じ値にすること**」と明記されている。実数値セルの幅は 3.2rem(約51px)しかない。「S行の実数値セルの隣にボタンを1つ足す」は6列目の追加(= 全6行のレイアウト変更)になり、「これ以外の変更は禁止」という前提に抵触する。

【対応】実装方式をここで確定する。**グリッドを一切変更せず、S行の実数値セル(5列目)の内側に `position: relative` を張り、アイコンリンクを `position: absolute` でセルの右外側に重ねる。** 列定義・他5行の見た目は変更しない。この方式で成立しない場合はP4担当がCoordinatorへ報告し、**勝手にグリッドを変更しない**。

### R-12 共有状態に「個体の現在のS実数値」を加え、panel→table の逆方向通知を明示する(指摘者: データ設計A-3)

【問題】P1の共有状態リストは「現在のレギュレーション」「選択中の実数値行」の2つ(いずれも table→panel の一方向)だけで、受け入れ基準12「適用後に `← 現在` マーカーが移動する」に必要な **panel→table の逆方向**の経路が無い。`stack.md` は「共有コアが操作する状態も、どちらの領域が所有するかを設計段階で決める」ことを要求している。

【対応】共有状態を3つにし、所有者と向きを確定する。

| 状態 | 所有者 | 伝達 |
|---|---|---|
| 現在のレギュレーション | `chart-table.ts` | table → panel(`CustomEvent`) |
| 選択中の実数値行 | `chart-table.ts` | table → panel(`CustomEvent`) |
| **個体の現在のS実数値** | **`owned-panel.ts`** | **panel → table(`CustomEvent`)。table はこれを受けて行のハイライトを描き直すだけ** |

`src/lib/box-id/shared-core.ts` の `registerLeftPanelBridge` / `registerDamageCalcBridge` と同じ「登録」パターンでもよいが、**クロージャ共有は禁止**。

### R-13 表と右カラムを2つの独立カラムにせず、1行=横一列にする(指摘者: 導線A-9)

【問題】P1は右カラムを `.panel-right` にする設計だった。1920px幅で試算すると、サイドバー256pxを引いた1664pxのうち右カラム360pxを除いた1304pxの左端に、1080px制約の表が**左寄せ**で置かれるため、**表の右端と右カラムの間に約224pxの空白**ができる。行の高さが100〜400px超とばらつくこの表で、空白を挟んで左右の行を対応づけるのは読み取りコストが高く、さらに「行の高さをどちらが正とするか」という同期問題を持ち込む(= `box/[id]` と同じ轍)。

【対応】**`.panel-right` を使わない。** 1行を「実数値 / ポケモン群 / この個体」の**横一列**として描き、全体を `.panel-content`(1080px)の内側に収める。列幅の目安: 実数値 80px / ポケモン 780px / この個体 220px。

- **ファイル分割(ChartTable / OwnedPanel)は維持する。** `OwnedPanel` は独立したカラムではなく「**1行ぶんの『この個体』セルを描くレンダラ**」として `ChartTable` から呼ばれる形にする。責務の分離(到達可能値の算出・PUT・失敗表示は `owned-panel.ts` が持つ)は変えない。
- これにより行の高さ同期の問題自体が消える(同じ行要素の中の別セルになるため)。導線B「行の高さの同期方式が未定」も同時に解消。
- **P1「レイアウト方針」節の『「この個体」カラムは `.panel-right`』という記述はこれで撤回する。**

### R-14 マスターデータ生成の実装上の落とし穴(指摘者: データ設計B)

【対応】いずれも設計に反映する。

- `POKEDEX[name].regulations` / `ITEMS[name].regulations` は Python の `set` で、`json.dump` は直接シリアライズできない。**`sorted(...)` でリスト化してから書き出す。**
- `item.csv` 未収録のアイテム(270件中122件)は `regulations` が空配列になる。これは「**未対応・未追跡**」であって「非公開」ではない。`extract_autocomplete.py` にこの旨のコメントを残す(将来 `regulations: []` を「使用不可」と早合点した流用を防ぐため)。
- 受け入れ基準の `273` / `311` / `271` は **vendor/jpoke v0.2.0時点の実測値**である旨をテストのコメントに明記する。

### R-15 現行レギュレーションでは1行も出ない補正がある(指摘者: プレイヤーB)

【対応】P5で「表に出ていない = バグ」と誤認しないための申し送り。Coordinator実測で、M-A/M-B のどちらでも**保持フォルムが0件**なのは次のとおり。

- 特性: `いかりのこうら` / `じょうききかん`(+6) / `びびり`
- 技: `オーラウイング` / `ギアチェンジ` / `しょうりのまい` / `みをけずる` / (M-Aのみ)`はいすいのじん`
- 持ち物: `からぶりほけん` / `カムラのみ` / `ビビリだま`(3件ともレギュレーション未収録)

`でんきエンジン` は M-B に `エモンガ` 1件のみ実在する。

## 却下した指摘(理由つき。再提起しないこと)

- **R-X1「パラドックス特性を持つ19種がM-A/M-Bに所属する」(プレイヤーA)** — **事実誤認**。Coordinatorが `regulation/pokemon.csv` の所属種族と `detail/pokemon.json` の `abilities` を突き合わせた結果、M-A・M-Bとも**0件**。指摘の本質(種族単位で決まらない補正である)だけをR-3として採用した。なお同レビュアーが挙げた3種の種族値比較自体は正しかった(例: テツノカイナ 最速S=112 に対し無振りA=160)。
- **R-X2「右カラム専用のワイヤーフレームを描き起こしてからP2に入る」(導線A-1)** — 却下。ページ形態はP1でユーザーと確定済みで、R-7・R-13で右カラムのDOM構造・3状態・列幅まで文章で確定させたため、追加の作図は工程を増やすだけになる。
- **R-X3「適用を取り消せるトースト/確認ダイアログ」(導線A-7後半)** — スコープ外(将来)。`/box/[id]` 自体が確認なしの自動保存で統一されており、この画面だけ確認を挟むと操作モデルが割れる。副作用が見えない問題はR-7の「内訳表示」で対応する。
- **R-X4「補正の条件(天候・地形・被弾など)をツールチップで注記」(導線B / プレイヤーB)** — スコープ外(将来)。参考サイトも条件を出しておらず、ラベルに特性名・技名が併記されている時点で条件は特定できる。R-9の凡例で最低限の説明は満たす。
- **R-X5「ポケモン名での絞り込み」** — スコープ外(将来)。R-8の実数値ジャンプで「目的の行へ辿り着く」課題は満たせる。名前検索は `/search` の責務。
- **R-X6「`items.json` へのフィールド追加でバンドルサイズが増える」(データ設計C)** — 対応不要。`items.json` は15.7KBで、`regulations` 追加の増分は数KB。実装後に実測して問題があればそのとき考える。

## 確定した設計(P4への入力)

### データモデル

**DBマイグレーションは不要。** `owned_pokemon.regulation` は `migrations/013_regulation.sql` で追加済み。新規テーブル・新規列なし。

### マスターデータ生成(`scripts/build-master-data/extract_autocomplete.py`)

1. `autocomplete/pokemon.json` の各要素に `regulations: string[]`(`sorted(POKEDEX[name].regulations)`)
2. `autocomplete/items.json` の各要素に `regulations: string[]`(`sorted(ITEMS[name].regulations)`)
3. 新規 `detail/speed-modifiers.json`。R-2の機械走査で生成。**抽出結果が空、または下限を割ったら生成を失敗させる**(下限: 倍率特性6件以上・ランク上昇技15件以上)

### API

**新規APIは作らない。** 既存の `PUT /api/owned-pokemon/:id` のみを使う(R-1の全項目積み戻しを厳守)。

### ファイル分割(R-13を反映した確定版)

| ファイル | 役割 |
|---|---|
| `src/lib/speed-chart.ts` | **純粋関数のみ**。DOM/fetch/Node APIに非依存。実数値算出・補正適用・行の組み立て・個体の到達可能値の列挙 |
| `src/lib/speed-chart-validation.ts` | `?reg=` / `?owned=` の検証、適用ペイロードの検証。**純粋関数** |
| `src/pages/speed-chart/index.astro` | オーケストレーター。SSRでクエリ解決・個体取得・レギュレーション決定。**`OwnedPokemonRecord` 全体を `<script type="application/json">` で埋め込む**(R-1) |
| `src/components/speed-chart/ChartTable.astro` | 表全体のテンプレート(3列とも含む) |
| `src/lib/speed-chart/chart-table.ts` | 行の描画・レギュレーション切替・ジャンプ・現在地スクロール |
| `src/components/speed-chart/OwnedPanel.astro` | 凡例・個体サマリ・エラー表示のテンプレート |
| `src/lib/speed-chart/owned-panel.ts` | 到達可能値の算出・「この個体」セルの描画・PUT・失敗表示 |

`is:global` のCSSは領域ルートクラス(`.speed-chart-table` / `.speed-chart-owned`)で必ず絞る。

### 実装の順序と依存

| 順序 | 担当 | 対象 | 依存 |
|---|---|---|---|
| 1(単独で完了させる) | 基盤 | `extract_autocomplete.py` の拡張 + `src/lib/speed-chart.ts` + `src/lib/speed-chart-validation.ts` + `tests/speed-chart.test.ts` + `tests/speed-chart-validation.test.ts` | なし |
| 2(1完了後) | ページ | `src/pages/speed-chart/index.astro` + `src/components/speed-chart/*` + `src/lib/speed-chart/*` | 1 |
| 3(2完了後) | 結線 | `AppSidebar.astro`(項目追加) + `AppLayout.astro`(`current` の union 拡張) + `LeftPanel.astro`(S行のリンク、R-11の方式厳守) | 2 |

**基盤エージェントにはエクスポートした型・関数のシグネチャを報告させ、そのまま次のエージェントの指示に貼る。**

## 受け入れ基準の追加(P2の指摘由来)

| # | 基準 | 確認方法 |
|---|---|---|
| 20 | 「ここにする」適用後、その個体の `tags` / `memo` / `move_names` / `is_pinned` / `regulation` / `tera_type` / `ivs` が**適用前と完全に一致する**(R-1) | 適用前後で `GET` して全項目をdiff |
| 21 | メガシンカ種族の行に `こだわりスカーフ` が1件も出ない。連携個体がメガ種族のとき右カラムの候補に持ち物差し替えが出ない(R-4) | DOM全文検索 + メガ個体で実際に開く |
| 22 | `?owned=` 付きで開くと、個体の現在のS実数値の行が初期表示でビューポート内にある(R-6) | `getBoundingClientRect()` で実測 |
| 23 | PUTが失敗したとき、エラー文言が出て `← 現在` マーカーが動かない(R-10) | APIを一時的に失敗させて確認 |
| 24 | 右カラムのセルが3状態(`← 現在` / `[ここにする]`+内訳 / `−`)で目視区別できる(R-7) | スクリーンショットで目視 |
| 25 | ツールバーに凡例と実数値ジャンプがあり、ジャンプが機能する(R-8 / R-9) | 実際に操作 |
| 26 | `/box/[id]` の H〜D の5行のレイアウトが変わっていない(R-11) | 変更前後で各行の `getBoundingClientRect()` を比較 |
| 27 | `こだいかっせい` / `クォークチャージ` が表に一切出ない(R-3) | DOM全文検索で0件 |
| 28 | `でんきエンジン`(M-Bの `エモンガ`)と `こうそくスピン` が表に出ている(R-2の取りこぼし対策) | DOM検索 |

**基準2/3の `273` / `311` / `271` は R-3(パラドックス除外)・R-4(メガのスカーフ除外)で変動しうる。P4の基盤エージェントが実測して確定させた値を正とし、この数値を更新すること。**

---

# P3追補: ユーザー追加要件(2026-08-01、P4着手前)

> ユーザー指示: 「すばやさ表作成で考慮する特性・アイテムのON/OFFを切り替えられる仕組み(json設定ファイル化など)があるとよい。それと、例えば、すべての種族でこだわりスカーフを考慮するのは実情と乖離するので、アイテムの採用率で判別するとよい」

この2点は**P1の設計を上書きする**。以下がP4への最終的な入力になる。

## U-1 補正のON/OFFを設定ファイルで切り替えられるようにする

【背景】R-2の機械走査は「jpokeが持つ補正を全部拾う」ので、環境的に無意味な補正(`じょうききかん`+6 など)まで表に出うる。何を表に載せるかは**ゲームの実装ではなく運用の判断**なので、コードを触らずに切り替えられる場所が要る。

【設計】

- **抽出(自動)と採否(手動)を分ける。**
  - `public/master-data/detail/speed-modifiers.json` … `npm run build:master-data` が jpoke から機械生成する**全件**(R-2の走査結果)。手で編集しない。
  - `src/config/speed-chart.json`(**新規、手で編集する唯一のファイル**) … 上の全件に対する採否と閾値。
- 設定ファイルの形:

```json
{
  "adoptionRate": {
    "enabled": true,
    "threshold": 0.10,
    "minSampleSize": 5,
    "appliesTo": ["items", "moves"]
  },
  "disabled": {
    "abilities": ["じょうききかん"],
    "moves": [],
    "items": []
  }
}
```

- **`disabled` に載っていないものは全てON**(既定でON)。無効化したいものだけを列挙する。
- **`disabled` に書いた名前が `speed-modifiers.json` に存在しない場合はユニットテストを失敗させる。** 打ち間違いと、jpoke側の改名によるサイレントな無効化解除を検知するため。
- ページ側は `speed-modifiers.json`(全件)と `speed-chart.json`(採否)の**両方を読んでマージした結果**だけを使う。マージ関数は `src/lib/speed-chart.ts` の純粋関数にしてユニットテストの対象にする。

## U-2 こだわりスカーフ等は「採用率」で載せるかどうかを決める

【背景】現状の設計は「レギュレーションでそのアイテムが使えるなら全311フォルムにスカーフ行を作る」。実際にスカーフを持つ種族は一部なので、表が実情と乖離し、行数も無駄に膨らむ。参考サイトが「採用率10%以上に限る」としているのはこのため。

【データ源】**新しいテーブルもAPIも作らない。** 既存の `suggestions` テーブルをそのまま使う。

- `kind = 'popular_item'` / `'popular_move'`、`subject_key = '<種族名>|<レギュレーション>'`(`migrations/013_regulation.sql` がレギュレーション別の行を作る仕組みを既に持っている)。
- payload は `{ "sample_size": n, "options": [{ "value": "こだわりスカーフ", "count": c, "ratio": 0.24 }, ...] }`。**`ratio` をそのまま採用率として使う。**
- 母集団は `owned_pokemon`(収集拒否を除く)+ `ranked_team_members`(上位入賞チーム。`docs/ranker/derived/ranked-teams.json` 由来で1,041チーム・6,241体)。`species_key` はメガシンカを「メガ〇〇」に正規化済みなので、この表のフォルム語彙とそのまま一致する。
- 季節→レギュレーションの対応(M-1→M-A / M-2→M-A / M-3→M-B)は `migrations/013_regulation.sql` の `ranked_season_regulations` テーブルが唯一の正。**アプリ側にもスクリプト側にも複製しない。**

【読み方】ページのSSR(`index.astro`)で `getSupabasePublicClient()` を使い、**1クエリ**で当該レギュレーションぶんをまとめて取得する(`kind` で絞り、`subject_key` の接尾辞 `|<reg>` で絞る)。取得結果は `{ 種族名: { items: {名前: ratio}, moves: {名前: ratio}, sampleSize: n } }` に畳んでページへ埋め込む。**種族ごとに311回fetchしない。**

【判定ルール】

| 条件 | 挙動 |
|---|---|
| `adoptionRate.enabled = false` | 採用率で絞らない(P1の挙動に戻る) |
| 採用率 >= `threshold` かつ `sample_size` >= `minSampleSize` | その持ち物/技の行を**出す** |
| 採用率 < `threshold` | 出さない |
| その種族の集計データが無い(k-匿名性の閾値未満・実績なし) | **出さない** |

- **特性には採用率フィルタをかけない。** `suggestions` は性格・持ち物・テラス・技の4種類しか集計しておらず(`migrations/009`)、特性の採用率データが存在しないため。`appliesTo` の既定値も `["items", "moves"]` にしてある。
- 「素の実数値」(補正なし)の行は**常に出す**。採用率フィルタは補正行にだけ効く。

【効果(Coordinatorが `docs/ranker/derived/ranked-teams.json` で実測)】

| レギュレーション | 母集団 | 実績データのあるフォルム | スカーフ行が残るフォルム(採用率10%以上・n>=5) |
|---|---|---|---|
| M-A | 273 | 168 | **21**(273 → 21) |
| M-B | 311 | 143 | **16**(311 → 16) |

例(M-B): ムクホーク89% / メタモン60% / ドリュウズ44% / ダイケンキ(ヒスイ)27% / ペリッパー25% / ウォッシュロトム25% / ガブリアス24%(n=114)。**実情に一致している。**

## U-1/U-2 に伴う既存の記述の変更

- **P1「補正(すばやさ上昇要素)」の「その特性/技を実際に持てるフォルムにだけ付ける」は必要条件に格下げする。** 持ち物と技については、さらに採用率の条件を満たす必要がある。
- **R-4(メガにスカーフを付けない)は維持する。** 採用率フィルタとは独立した、ゲームルール上の制約。
- **表の行数が変わる。** 受け入れ基準2/3の `271行` は無効。P4の基盤エージェントが実測した値を正とする。
- **ローカルDBが空である問題。** Coordinator実測で `ranked_team_members` / `owned_pokemon` / `suggestions` はいずれも0件だった。P4では次の順に実行してデータを用意する(これは検証用のダミーではなく、アプリが本来持つべき公開参照データなので**削除しない**):
  1. `node ./scripts/db/seed-ranked-teams.mjs`(`docs/ranker/derived/ranked-teams.json` を投入)
  2. `npm run refresh-suggestions`(集計を走らせて `suggestions` を埋める)

## 受け入れ基準の追加(U-1/U-2由来)

| # | 基準 | 確認方法 |
|---|---|---|
| 29 | `src/config/speed-chart.json` の `disabled.abilities` に特性名を1つ足すと、その特性の行が表から消える(再ビルド不要でSSRに反映) | 実際に足して開き、DOM検索で0件を確認。確認後は元に戻す |
| 30 | `disabled` に存在しない名前(例 `"存在しない特性"`)を書くとユニットテストが失敗する | テストを実行して失敗を確認。確認後は元に戻す |
| 31 | `adoptionRate.enabled = true` のとき、M-Bでこだわりスカーフの行を持つフォルムが**採用率10%以上・n>=5のものだけ**になる(Coordinator実測では16フォルム) | DOMから「こだわりスカーフ」を含む行のフォルム名を集め、`suggestions` の `ratio` と突き合わせる |
| 32 | `adoptionRate.enabled = false` にすると、スカーフ行がレギュレーションの全フォルム(メガを除く)に戻る | 設定を切り替えて件数を比較。確認後は元に戻す |
| 33 | 採用率の取得がSSRの**1クエリ**で完結している(種族ごとのfetchが無い) | Networkタブに `/api/suggestions` へのリクエストが出ないことを確認 |

---

# P4での是正: 採用率フィルタの対象から「技」を外す(2026-08-01 Coordinator判断)

【問題】U-2の初版は `adoptionRate.appliesTo` の既定を `["items", "moves"]` にしていた。ページ実装後にCoordinatorが実測したところ、**技のランク上昇行がM-B全体で5組しか残らなかった**。

```
ガブリアス/スケイルショット 59% | メガカメックス/からをやぶる 43%
メガギャラドス/りゅうのまい 93% | ライチュウ/くさわけ 20% | リザードン/ニトロチャージ 33%
```

原因は採用率データ側の2つの制約:

1. `migrations/009` の `refresh_popular_builds(top_n DEFAULT 5)` により、`popular_move` は**種族ごと上位5技しか保存しない**。1個体が持つ技は4つなので、すばやさ上昇技が6番目以降に落ちると存在自体が記録されない。
2. 技まで判明している母集団が、構築記事のある300チームに限られる(`migrations/010` のコメント「性格/特性/テラス/技/努力値は構築記事がある300チームにしか無い」)。実測で M-B の `popular_move` は**30種族分**しか無い(`popular_item` は80種族分)。

この結果、ユーザーが要件で明示的に選んだ「**補正の範囲は持ち物+特性+ランク上昇技**」というスコープがほぼ失われていた。

【対応】**`appliesTo` の既定を `["items"]` に変更する。** ユーザーの指示は「**アイテム**の採用率で判別するとよい」であり、技への適用はCoordinatorが独断で広げたもの。指示の範囲へ戻す。`popular_move` の母集団が充実したら設定ファイルで `"moves"` を足すだけで有効になる(理由は `src/config/speed-chart.json` の `_readme` に記録済み)。

【是正後の実測値(**受け入れ基準2/3の最終的な正の値**)】

| レギュレーション | 母集団 | 行数 | エントリ数 | 持ち物補正が付くフォルム | 技補正のエントリ(フォルム) |
|---|---|---|---|---|---|
| M-A | 270 | **266** | 1,875 | **20** | 915(192フォルム) |
| M-B | 308 | **267** | 2,166 | **16** | 1,077(220フォルム) |

- こだわりスカーフは実情どおり16〜20フォルムに限定されたまま(U-2の目的は達成)。
- ランク上昇技は復活した。M-Bで `こうそくスピン` 11フォルム / `りゅうのまい` 19フォルム / `ロックカット` 6フォルム。

【受け入れ基準の更新】

- **基準2**: 母集団は M-A **270** / M-B **308** フォルム(P1の273/311は `implemented=0` のメガストーン(ニャオニクスナイト・フラエッテナイト)を数えていた誤り)
- **基準3**: 行数は M-A **266** / M-B **267**(P1の271、およびP4中間の211はいずれも無効)
- **基準28**: `こうそくスピン` は既定設定で表に出る(是正前は技フィルタで消えていた)
- **基準31**: 「M-Bでこだわりスカーフの行を持つフォルムが16」は維持

---

# 実施結果(2026-08-01 P5受け入れ検証・P6完了)

## ユーザー判断(2026-08-01、P5の実測を受けて)

**`くさわけ` と `こうそくいどう` を `src/config/speed-chart.json` の `disabled.moves` でOFFにする。**

【経緯】採用率フィルタを持ち物のみに戻した(P4での是正)結果、技側のノイズが表面化した。M-Bの技補正エントリ359件の内訳を実測すると:

| 技 | フォルム数 | 割合 |
|---|---|---|
| くさわけ | 129 | 36% |
| こうそくいどう | 92 | 26% |
| ニトロチャージ | 35 | 10% |
| スケイルショット | 34 | 9% |
| りゅうのまい | 31 | 9% |
| こうそくスピン | 14 | 4% |
| その他10種 | 24 | 7% |

上位2件で**62%**を占め、表の上位行が「こうそくいどう」一色になっていた(スクリーンショットで確認)。どちらも技マシンで広く覚えられる一方、実戦採用は限られる。参考サイトは採用率フィルタでこれを解消しているが、`suggestions.popular_move` は母集団が疎すぎて技には使えない(P4の是正記録参照)。**U-1のON/OFF機構はまさにこのための仕組み**なので、設定側で明示的に落とした。

【効果】OFF後の上位行が参考サイトとほぼ一致した。

```
本実装                                 参考サイト(シーズンM-2)
378 最速 120族 2倍 かるわざ            378 最速 120族 2倍 / かるわざ
374 最速 118族 2倍 かるわざ            374 最速 118族 2倍 / かるわざ
336 最速 101族 S+2 ロックカット        336 最速 101族 S+2 / ロックカット
333 最速 150族 S+1 りゅうのまい        333 最速 150族 S+1 / りゅうのまい
```

行数は M-B 268行 → **232行**。`りゅうのまい`(60箇所)・`ロックカット`(24箇所)・`スケイルショット`・`ニトロチャージ`・`からをやぶる` など実戦で使われる技はそのまま残っている。設定から2件を外せば再ビルド不要で即座に戻る。

## 受け入れ基準の判定(すべてCoordinatorが実測)

| # | 基準 | 結果 | 実測値・確認方法 |
|---|---|---|---|
| 1 | `/speed-chart` が200で開き、既定でレギュレーションが選ばれている | ✅ | HTTP 200 / `<select>` に `M-B` |
| 2 | 母集団のフォルム数 | ✅ | **M-A 270(213+57) / M-B 308(235+73)**。P1の273/311は `implemented=0` のメガストーン(ニャオニクスナイト・フラエッテナイト)を数えていた誤り |
| 3 | 行数・実数値の降順 | ✅ | **M-B 232行**(技2件OFF後)。降順を目視+DOMで確認 |
| 4 | 補正なし「最速」が `calcOtherStat(50, base, 31, 32, 1.1)` と一致 | ✅ | `tests/speed-chart.test.ts` で全フォルム突き合わせ、0件不一致 |
| 5 | スカーフ行が `floor(素の値 * 6144 / 4096)` | ✅ | ユニットテスト+画面(マスカーニャ 最速123族→288)で確認 |
| 6 | `S+2`=`floor(値*2)` / `S+1`=`floor(値*1.5)` | ✅ | メガキラフロル 101族 S+2 → 336、メガプテラ 150族 S+1 → 333。**参考サイトの数値と一致** |
| 7 | あやしいかぜ/ぎんいろのかぜ/げんしのちから が出ない | ✅ | 生成物・DOMとも0件 |
| 8 | 補正が、その特性/技を持たないフォルムに付いていない | ✅ | `speed-chart.ts` が `abilities`/`learnset` で判定。ユニットテストで検証 |
| 9 | `/box/[id]` のS行のリンクから遷移する | ✅ | 実際にクリックして `/speed-chart?owned=<id>` を確認 |
| 10 | 他人の個体/存在しないID/未ログインでは右カラムが出ず表は出る | ✅ | **他ユーザーの個体IDで実測**。表は268行出て右カラムは全て `−`、`戻る` は出る |
| 11 | 「ここにする」で `nature`/`evs[5]`/`item_name` が期待値になる | ✅ | 実測(PUT 200 → GETで永続化を確認) |
| 12 | 適用後 `← 現在` がクリックした行へ移動 | ✅ | 実測 |
| 13 | 努力値合計66超で警告のみ・適用は通る | ✅ | `努力値合計{n}(66超。適用は可能です)` を表示、ブロックなし |
| 14 | `npm run build` 成功 | ✅ | exit 0、型エラー0件 |
| 15 | `npm test` 全件pass・テストが増えている | ✅ | **298 → 375件**(+77)、fail 0 |
| 16 | Pyodideを読み込まない | ✅ | ネットワーク実測で pyodide/`.whl` へのリクエスト**0件** |
| 17 | 1920×1080 ライト・ダークで崩れ・横スクロールなし | ✅ | `npm run shot` で撮影し目視。`scrollWidth > innerWidth` が false |
| 18 | `/box` `/box/[id]` に回帰なし | ✅ | 撮影して目視。コンソールエラー0件 |
| 19 | DBが元の状態に戻っている | ✅ | devユーザーの個体6件を全項目突き合わせ。既知の基準値(ハバタクカミ=おくびょう・`is_pinned`=true 等)のまま |
| 20 | 適用後も `tags`/`memo`/`move_names`/`is_pinned`/`regulation`/`tera_type`/`ivs` が不変 | ✅ | 適用前後のGETで突き合わせ |
| 21 | メガ種族にスカーフが付かない | ✅ | **DOM実測でスカーフ保持フォルム16件、うちメガ0件** |
| 22 | `?owned=` で現在地の行が初期表示でビューポート内 | ✅ | `getBoundingClientRect().top = 476px`(1080pxの画面内・ほぼ中央) |
| 23 | PUT失敗時にエラーが出てマーカーが動かない | ✅ | ルート横取りで実測 |
| 24 | 右セルの3状態が目視区別できる | ✅ | スクリーンショットで確認(緑背景+`← 現在` / `ここにする`+内訳 / `−`) |
| 25 | 凡例と実数値ジャンプがある・ジャンプが機能する | ✅ | 画面上で確認、実際に操作 |
| 26 | `/box/[id]` のH〜Dの5行のレイアウトが不変 | ✅ | `git stash` で変更前を退避し `getBoundingClientRect()` を前後比較。**5行とも小数点以下まで完全一致(0px差)** |
| 27 | `こだいかっせい`/`クォークチャージ` が出ない | ✅ | DOM全文検索で**0件** |
| 28 | `でんきエンジン`・`こうそくスピン` が出ている | ✅ | でんきエンジン3箇所(エモンガ)、こうそくスピン39箇所 |
| 29 | `disabled` に足すとその補正が表から消える | ✅ | **`くさわけ`/`こうそくいどう` を実際に追加 → DOM検索で両方0件**(232行へ減少)。再ビルド不要 |
| 30 | `disabled` に存在しない名前を書くとテストが失敗する | ✅ | `tests/speed-chart.test.ts` に実装済み・実行確認 |
| 31 | スカーフ行が採用率10%以上・n≧5のフォルムだけ | ✅ | **M-B 16フォルム**(ドラパルト/マスカーニャ/オオニューラ/ガブリアス/ムクホーク/サザンドラ/フラエッテ(えいえん)/ドリュウズ/ヒートロトム/ウォッシュロトム/ダイケンキ(ヒスイ)/ソウブレイズ/サーフゴー/イダイトウ(オス)/ペリッパー/メタモン) |
| 32 | `adoptionRate.enabled=false` で全フォルムに戻る | ✅ | 切り替えて M-B 278行・スカーフ235フォルムを確認後、元に戻した |
| 33 | 採用率の取得がSSRの1クエリで完結 | ✅ | 実行時の `/api/suggestions` リクエスト**0件** |

**全33件pass。**

## 実装中に見つかった実バグ・データの問題(このページとは別)

1. **メガストーン名の不整合(既知)**: `npm run build` が「メガストーン名がjpokeのITEMS(=items.json)に存在しません: 2件(メガニャオニクス(オス)/(メス) → ニャオニクスナイト)」と警告する。jpoke側のMEGA_STONES逆引きがオス/メスで曖昧になる既知の不整合(`src/lib/pokemon-master-data.ts` の `loadMegaStoneMap` のコメントに既記載)。本ページは `items.json` 実在チェックで正しく除外している。
2. **本番(リモート)Supabaseに参照データが無い**: `ranked_team_members` / `suggestions` / `owned_pokemon` が0件(`.env` のリモート接続で実測)。ローカル(`.env.local`)には投入済み。**デプロイ時に `node ./scripts/db/seed-ranked-teams.mjs` と `npm run refresh-suggestions` を実行しないと、本番では採用率フィルタが全種族「データなし」と判定してこだわりスカーフ行が1件も出ない。**
3. **一部フォルムのアイコンが出ない**: ビルド時の「2件 imageId不明」警告に該当するフォルム(メテノの一部フォルム等)。本ページ起因ではなく既存のマスターデータ側の問題。

## スコープ外に落としたもの(将来やるなら上から)

1. **技の採用率フィルタ**(`adoptionRate.appliesTo` に `"moves"` を足すだけで有効になる)。`suggestions.popular_move` の母集団が充実したら再検討する。現状は `top_n=5` と「技まで判明しているのは構築記事のある300チームのみ」という2つの制約で使えない。
2. **使用率による絞り込み・強調**(参考サイトのTOP100限定・TOP15赤文字)。
3. **補正の重ねがけ**(スカーフ+りゅうのまい等)。
4. **下降補正**(まひ・くろいてっきゅう・スロースタート・トリックルーム)、**ダブル固有の補正**(おいかぜ)。
5. **適用を取り消せるトースト**、**補正の発動条件のツールチップ**、**ポケモン名での絞り込み**(P2で却下済み。R-X3/R-X4/R-X5)。
6. **見た目の磨き込み**(チップの意匠をワイヤーフレームに寄せる等)。→ `ui` skill の担当。

---

# 追加改修(2026-08-01 ユーザー指示・第2弾)

> 1. ポケモンは使用率順に並べる
> 2. 「ここにする」を押したら個体編集画面に戻る
> 3. すばやさ数値ごとに1段(文字とアイコンは別段のまま)にまとめる。入りきらない場合は使用率で足切りする。
> 4. 個体編集画面から遷移した場合、とりうるすばやさ以外をデフォルトで非表示にし、トグルスイッチで全表示と切り替え可能にする

## 前提: 「使用率」の定義とデータ源(Coordinatorが実測して確定)

**種族(フォルム)ごとの使用率**は、これまで使ってきた `suggestions`(持ち物・技の**種族内**採用率)とは**別の指標**であり、既存データには無い。新たに `ranked_team_members` から集計する。

```sql
select m.species_key, count(*)::int as n
from ranked_team_members m
join ranked_teams t on t.id = m.ranked_team_id
join season_regulations r on r.season = t.season
where r.regulation = $1
group by m.species_key
```

- 実測(2026-08-01): **M-A 168種族/延べ4,495体、M-B 143種族/延べ1,746体**。
- 使用率 = `n / 延べ数`。M-Bの上位: ガブリアス6.5% / ブリジュラス5.3% / ミミッキュ4.8% / カバルドン4.7% / アシレーヌ4.1%。
- **母集団308フォルムのうち使用実績があるのは143フォルムだけ**。残り165フォルムは使用率0として扱う(足切りで最初に落ちる。これは望ましい挙動)。
- 取得は**SSRの1クエリ**(GROUP BYはDB側で完結。`migrations/009` 冒頭の「Workers CPU 10ms制約のためJS側でループ集計しない」方針に従う)。`suggestions` の取得クエリと合わせて**SSRのDBアクセスは2クエリまで**に抑える。
- `season_regulations`(`migrations/013`)がシーズン→レギュレーションの唯一の正。**アプリ側に対応表を複製しない。**

## 要件1: ポケモンを使用率順に並べる

- 並び順: **使用率の降順 → 同率はすばやさ種族値の降順 → 種族名の昇順**(安定ソート。使用率0のフォルムが名前順で固まるのを避けるため種族値を第2キーにする)。
- 適用範囲: 行内のポケモンチップの並び。**行そのものの並び(実数値の降順)は変えない。**
- 使用率が0/不明のフォルムも**並びの末尾に残す**(要件3の足切りで溢れたときだけ消える)。

## 要件2: 「ここにする」で個体編集画面へ戻る

- PUT成功後に `location.href = '/box/<owned id>'` へ遷移する。
- **失敗時は遷移しない**(R-10で決めた「エラー文言を出し `← 現在` を動かさない」を維持)。
- これにより「適用後もページに留まり続けて別の行を適用できる」(R-10の後半)は**撤回**する。ユーザー指示が優先。
- 努力値合計66超の警告(要件13)は**遷移前に出す必要がある**。遷移してしまうと読めないため、**警告が出るケースでは確認を挟む**のではなく、**遷移先の個体編集画面側に既にある合計表示に委ねる**(`/box/[id]` は超過を常時表示している)。この画面での警告表示は残すが、遷移をブロックしない。

## 要件3: すばやさ数値ごとに1段にまとめる + 使用率で足切り

【現状】1つの実数値行が「振り方+族+補正」のグループを複数持ち、**グループごとに(ラベル1段 + アイコン1段)**を積む。実測で最大10段を超える行があり、行の高さが100〜400px超にばらつく。

【変更後】1つの実数値行を**必ず2段**にする。

```
127 │ 準速75族 / 無振り65族 S+1 くさわけ / 無振り65族 こだわりスカーフ / …   ← 1段目(文字)
    │ [アイコン][アイコン][アイコン][アイコン][アイコン] …                  ← 2段目(アイコン)
```

- **1段目**=補正ラベル群、**2段目**=ポケモンのアイコン+名前のチップ群。「文字とアイコンは別段のまま」というユーザー指示どおり。
- **⚠ 仮定(ユーザー指示の解釈)**: ラベルとポケモンの1対1の対応は、**同じ並び順であること**で表す(ラベル群とチップ群を同じグループ順に並べる)。この解釈で実装する。もし「どのポケモンがどのラベルか」を厳密に示す必要があれば、チップ側に補正名を併記する案に切り替える(その場合は要件3の「1段」を満たせなくなるため、ユーザーへ再確認する)。
- **足切り**: 1段に収まらない分を**使用率の低い順に落とす**。
  - 収まる件数は**実測で決める**(`.panel-content` 1080px − 実数値列80px − この個体列220px = ポケモン列780px。チップ1個の実測幅から算出する。**推測で定数を置かない**)。
  - 落とした件数は `+N件` のように**必ず可視で示す**(黙って切ると「そのポケモンは居ない」と誤読されるため。`stack.md`「no silent caps」と同趣旨)。
  - 足切りは**表示のみ**に効かせる。要件4のフィルタや行数の計算には影響させない。

## 要件4: 個体連携時は「とりうるすばやさ」だけを既定表示にする

- `?owned=` があり右カラムが出るときのみ有効。
- **既定**: その個体が到達可能な実数値の行だけを表示する(`← 現在` の行を含む)。実測では到達可能な相異なる実数値は69〜134通りなので、**232行 → 100行前後**に減る想定。
- **トグルスイッチ**で全表示に切り替えられる。ラベルは「とりうるすばやさのみ」。
  - 部品は既存の `.toggle-switch` 一式(`src/components/box-id/LeftPanel.astro` の `is:global` にある)を流用する。**新しいトグル部品を作らない。**
  - 状態はURLに持たない(この画面限りの表示切替)。
- **`?owned=` が無いときはトグル自体を出さない**(絞る基準が無いため)。`[hidden]` を使う場合は `display` 指定クラスに `.foo[hidden]{display:none}` を併記する(`pitfalls.md`)。
- 要件3の足切りは**絞り込み後の行にも同じように**効かせる。

## 実装の分担(ファイル)

| ファイル | 変更 |
|---|---|
| `src/pages/speed-chart/index.astro` | 種族使用率のSSR取得(1クエリ追加)、埋め込み |
| `src/lib/speed-chart.ts` | 使用率によるチップの並び替え・足切り(**純粋関数**)。行のフィルタ(到達可能のみ)も純粋関数として追加 |
| `src/components/speed-chart/ChartTable.astro` | 2段レイアウトのCSS、トグルのマークアップ |
| `src/lib/speed-chart/chart-table.ts` | 2段描画、足切り件数の実測、トグルの結線 |
| `src/lib/speed-chart/owned-panel.ts` | PUT成功後に `/box/<id>` へ遷移、到達可能値の集合をtableへ通知 |
| `tests/speed-chart.test.ts` | 並び替え・足切り・到達可能フィルタのユニットテスト追加 |

**共有状態の追加**(R-12の表を更新する):

| 状態 | 所有者 | 伝達 |
|---|---|---|
| 個体が到達可能な実数値の集合 | `owned-panel.ts` | panel → table(`CustomEvent`)。table が行の表示/非表示を切り替える |

## 受け入れ基準(追加)

| # | 基準 | 確認方法 |
|---|---|---|
| 34 | 行内のポケモンが使用率の降順に並んでいる | 上位3行のチップ順をDBの集計値と突き合わせる |
| 35 | 1つの実数値行が必ず2段(文字1段+アイコン1段)に収まっている | 行の高さを `getBoundingClientRect()` で実測し、全行が同一高さ帯に収まることを確認 |
| 36 | 足切りが起きた行に `+N件` の表示がある | DOM検索 |
| 37 | 「ここにする」で `/box/<id>` へ遷移する | 実際にクリックしてURL確認 |
| 38 | PUT失敗時は遷移しない | ルート横取りで確認 |
| 39 | `?owned=` 付きの初期表示で、行数が到達可能な実数値の件数と一致する | 行数を数え、`enumerateReachableSpeedValues` の結果と突き合わせる |
| 40 | トグルで全表示に切り替わり、行数が全件に戻る | 切り替えて行数を比較 |
| 41 | `?owned=` が無いときトグルが表示されない | DOM |
