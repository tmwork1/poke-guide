# 未作成ページのバックログ(索引)

**このファイルは索引専用。** 各ページの仕様・設計・受け入れ基準・実施結果は `docs/plan/pages/<slug>.md` に書く(このファイル本体に追記しない)。

`new-page` skill の P0(現在地把握)がこの表を読む。**着手・完了したら必ずこの表を更新すること。** 更新を怠ると次回P0で見つけられない。

## 状態

- **進行中: なし**(2026-07-29 時点)
- **直近の完了: チーム(`/team` + `/team/[id]`)** → `docs/plan/pages/team.md`(2026-07-29 P1〜P6完了。受け入れ基準23件のうち22件をCoordinatorが実測でpass判定、1件はdev環境の制約でコード確認のみ)。**見た目の磨き込みは `ui` skill に引き渡し済み**
- 既存ページ(`/box` `/box/[id]` `/search` `/pokemon/[name]` `/moves/[name]` `/share/[slug]`)の**見た目の改善は `ui` skill の担当**。このバックログは対象外。

## バックログ

| 優先 | ページ | URL | 根拠 | ワイヤーフレーム | 状態 | ファイル |
|---|---|---|---|---|---|---|
| 1 | チーム一覧 | `/team` | `00-foundation.md`「ページ構成」の3ページ構成のうち「チーム登録」。`AppSidebar` に `準備中` バッジで枠だけ存在 | `docs/ui_proposal/チーム.png` | ✅ 完了(2026-07-29) | `team.md` |
| 1 | チーム編集 | `/team/[id]` | 同上(一覧とセットで1機能) | `docs/ui_proposal/チーム編集.png` | ✅ 完了(2026-07-29) | `team.md` |
| 2 | ホーム(統計) | `/` | `00-foundation.md`「ページ構成」1.「サイトの入り口。DBの統計情報などを表示する」。**現在 `src/pages/index.astro` は `/box` への302リダイレクト**なので、作るならこの扱いをどうするかの判断が要る | なし | ⬜ 未着手 | — |
| 3 | トレンド | 未定 | `AppSidebar` に `準備中` バッジで枠だけ存在。**何を出すページなのかが未定義**なので、着手前にユーザーへの仕様確認が必須 | なし | ⬜ 未着手 | — |

### 「後回し」方針について
`00-foundation.md`「開発の流れ」に **「ホーム、チーム登録は後回し」** と明記されている。この後回しは「育成登録(`/box` 系)を先に仕上げる」ためのもので、`ui` skill のラウンド19で育成登録側のUI改善ループは完了している。**着手してよい状態になっているが、順序はユーザーの判断で決める。**

## 参照

- `.claude/skills/new-page/SKILL.md` — このバックログを消化する手順
- `.claude/skills/new-page/references/stack.md` — 層構造・規約・落とし穴
- `docs/plan/ui_rounds/00-foundation.md` — ページ構成・レイアウト原則(**現行仕様の唯一の正**)
- `docs/plan/ui_plan.md` — 既存ページのUI改善ラウンドの索引(`ui` skill 側の状態)
