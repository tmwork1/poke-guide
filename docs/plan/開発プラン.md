# Poke-Commons 開発プラン

作成日: 2026-07-23
前提資料: [初期構想.md](./初期構想.md)
参考実装: `../poke-research/`(Astro + Cloudflare Workers + Supabase の稼働実績あり)
計算エンジン: `../jpoke/`(純Python・依存ゼロのポケモンチャンピオンズ対戦ライブラリ)

---

## 1. ゴールとスコープ

初期構想の3機能 + データ蓄積基盤を、以下の順で段階的にリリースする。

1. **ダメージ計算機**(+ 計算ログの匿名蓄積)
2. **育成ビルダー**(保存・共有 + 蓄積)
3. **検索**(+ 検索ログ蓄積)
4. **データ活用**(蓄積データからのサジェスト表示)

対象ゲームは jpoke の対応範囲に合わせ **ポケモンチャンピオンズのシングルバトル**(努力値は 0〜32 のチャンピオンズ形式)。

「Data First」の思想に基づき、**イベントロギング基盤を Phase 1 で最初に作り**、全機能はその上に載せる。

---

## 2. アーキテクチャ

### 2.1 全体構成

```
[ブラウザ]
   │  Astro SSR ページ + フォーム/検索 UI
   │  + Pyodide 上の jpoke(クライアントサイド計算エンジン、静的アセット配信)
   ▼
[Cloudflare Workers: メインアプリ (TypeScript / Astro)]
   │  ├─ ファイルベース API (src/pages/api/**)
   │  ├─ middleware.ts (認証・レートリミット・CSRF)
   │  └─ worker.ts (fetch + scheduled 合成エントリ)
   │
   └──▶ [Supabase (Postgres)]
           マスタ参照キャッシュ・イベントログ(計算条件が中心)・育成データ・集計テーブル

[GitHub Actions (無料枠、定期実行)]
   │  ネイティブ Python + jpoke で damage_calcs のユニーク組み合わせを再計算・検証
   ▼
   Supabase へ検証済み結果を書き込み(suggestions)
```

- **メインアプリ**: poke-research と同じ Astro + `@astrojs/cloudflare` 構成。単一 npm パッケージ(モノレポにしない)。API/SSR のみを担い、ダメージ計算そのものは行わない。
- **計算エンジン**: jpoke をブラウザ内 **Pyodide** で実行(§2.2)。専用 Worker・Service Binding は持たない。
- **結果検証バッチ**: クライアント計算結果は未検証のまま蓄積し、信頼性が必要なサジェスト機能(§2.6)のみ GitHub Actions のネイティブ Python 実行でユニーク組み合わせを再計算して裏取りする。
- **DB**: Supabase (Postgres)。ローカル開発は Supabase CLI (`supabase start`, Docker) を使用(初期構想の指定どおり。poke-research には無い部分なので新規セットアップ)。

### 2.2 jpoke 実行方式 — 決定済み(2026-07-23)、Phase 0 は検証のみ

jpoke はランタイム依存ゼロ・C拡張なしの純 Python で、`numpy`/`pandas` 等の重量ライブラリに依存しないため Pyodide との相性が良い。Cloudflare Workers Free プランは **CPU時間 10ms/呼び出し(HTTP・Cron Trigger とも同一)、Worker バンドルサイズ 3MiB 上限** という制約があり、jpoke のマスタデータ単体で約3MBに達するため案Aは無料プランでは事実上成立しない([Cloudflare Workers Limits](https://developers.cloudflare.com/workers/platform/limits/))。この検証結果を踏まえ、**案Bをデフォルト方式として採用**する。

| 案 | 内容 | 懸念 |
|---|---|---|
| **B (採用)** | ブラウザ内 Pyodide で jpoke をクライアント実行 | 初回ロードサイズ(Pyodide コア約7MB + jpoke データ約3MB ≒ 10〜13MB想定)、低スペック端末での実行速度。ただしブラウザキャッシュ/Service Worker で初回以降は解消、サーバ負荷ゼロ・オフライン計算可 |
| A (非採用) | Python Workers で jpoke を動かす専用 Worker | Free プランの CPU 10ms/バンドル3MiB制限にほぼ確実に抵触。成立させるには Workers Paid(月$5〜)が前提になる |
| C (最終手段) | ダメージ計算コア部分のみ TypeScript へ移植 | 移植工数大・jpoke の 6174 テストとの等価性維持が困難 |

**Phase 0 の役割**: 案Bの実現可否(ロードサイズ・初期化時間・計算結果の一致)を検証する。想定を超えて破綻した場合のみ、Paid プラン前提で案A、それも難しければ案Cを検討する。

### 2.3 マスターデータの流れ

jpoke の `src/jpoke/data/`(pokedex.json / moves.json / learnsets.json / CSV)を**単一の信頼できる情報源**とし、ビルド時スクリプトで以下2種類の静的アセットを生成する。DB にはマスタを複製せず、**イベントログには名前(jpoke の日本語キー)をそのまま記録**する。

1. **オートコンプリート用軽量 JSON**(ポケモン名・技名・特性・アイテム): フォーム入力補助向け
2. **Pyodide 実行用フルパッケージ**(jpoke 本体 + データ一式): ブラウザ内計算エンジン向け。Cloudflare の静的アセット配信はリクエスト数/CPU時間の制約を受けないため、Free プランのままサイズ・帯域の心配なく配信できる

### 2.6 計算結果の信頼性方針 — 決定済み(2026-07-23)

計算をクライアント(Pyodide)で行うため、サーバは計算結果を検証できない。悪意あるクライアントが捏造した結果を送信すると、集合知の集計データ(サジェスト)が汚染されるリスクがある。これに対応するため、初期構想の機能を「計算条件(入力)だけで作れるか」で仕分けて扱いを分ける。

| 分類 | 該当機能 | 扱い |
|---|---|---|
| **入力ベース(大半)** | よく比較される育成/ポケモン、人気の仮想敵、人気の育成、採用率、技構成の傾向、努力値の傾向、最近注目されているもの | `POST /api/events` は**計算条件のみ**を正データとして受け付け、集計もそこからのみ行う。結果の真正性に依存しないため無検証で提供可能 |
| **結果依存(少数)** | 勝率の高い調整、おすすめの耐久ライン | クライアント計算結果(`client_result`)は UI 表示用の未検証値として保存するのみで、集計には使わない。Phase 5 で GitHub Actions の**ネイティブ Python + jpoke**(Pyodide 経由ではないため CPU時間制限なし)による**ユニークな組み合わせ単位のバッチ再計算**を導入し、検証済みの値のみ `suggestions` へ書き込んでから提供する |

Cloudflare Workers 上での検証(Cron Trigger 含む)は Free プランの CPU 10ms/呼び出し制限に阻まれるため選ばない。GitHub Actions(private リポジトリでも月2,000分無料)ならネイティブ実行のため制限を受けない。

### 2.4 データモデル(初期案 — Phase 1 で確定)

```sql
-- 匿名イベントログ (append-only、全機能共通の蓄積基盤)
events (
  id bigint generated always as identity,
  event_type text,          -- 'damage_calc' | 'build_save' | 'search' | 'compare' | ...
  payload jsonb,            -- イベント固有データ (計算条件、検索語 など)
  session_hash text,        -- 匿名セッション識別子 (日次ローテーションのハッシュ、個人特定不可)
  created_at timestamptz default now()
)

-- 型付きテーブル (集計しやすい主要イベントは正規化して二重記録)
damage_calcs ( attacker_name, defender_name, move_name, attacker_build jsonb,
               defender_build jsonb, field jsonb,
               client_result jsonb,      -- クライアント計算結果。UI表示用の未検証値、集計には使わない(§2.6)
               verified_result jsonb,    -- GitHub Actions バッチが埋める検証済み結果(Phase 5)
               session_hash, created_at )

builds ( id uuid, pokemon_name, nature, ability_name, item_name, tera_type,
         evs jsonb, ivs jsonb, move_names text[], share_slug text unique,
         is_public boolean, session_hash, created_at )

searches ( query text, category text, hit_count int, session_hash, created_at )

-- 集計結果 (cron ジョブが書き込み、閲覧 API は読むだけ)
suggestions ( kind text, subject_key text, payload jsonb, computed_at )
  -- kind 例: 'popular_build' | 'common_opponent' | 'ev_trend' | 'trending_search'
```

- **RLS を Phase 1 の最初から全テーブルに適用**(poke-research では後付け対応 `027_enable_rls.sql` になった教訓)。閲覧系は `SELECT` のみ public、書き込みは service_role 経由の API のみ。
- 匿名化ポリシー: IP・UAは保存しない。`session_hash` は日替わりソルト付きハッシュで長期追跡不可にする。

### 2.5 poke-research から流用するパターン

| 項目 | 流用内容 |
|---|---|
| マイグレーション | `migrations/NNN_*.sql` 連番 + `scripts/db/run-migrations.mjs`(`pg` 直結の自前ランナー、適用済み管理テーブル) |
| API 設計 | Astro ファイルベースエンドポイント + `api/_shared.ts` 共通ヘルパー。Hono 等は導入しない |
| 認証 | 管理系 API のみ Basic 認証(`middleware.ts`)。一般ユーザーはログイン不要(匿名) |
| Cron | `worker.ts` に fetch + scheduled 合成エントリ。**Cron Trigger の上限は Free プランでアカウントごと5件**([公式 Limits](https://developers.cloudflare.com/workers/platform/limits/))で、poke-research が3件使用済み。**残り枠内で対処する方針(決定済み 2026-07-23)**: poke-commons は1件に集約し分スロットでジョブ分岐(poke-research の `DAILY_SLOT_JOBS` パターン) |
| テスト | `node --test`(外部フレームワーク無し)+ CI で postgres:16 コンテナへのマイグレーション DDL 検証 |
| デプロイ | Cloudflare Git 連携で main ブランチ自動デプロイ。本番マイグレーションのみ `npm run release` で手動先行適用 |
| 環境変数 | `.env.example` / `.env` / `.env.production` / `.dev.vars` の使い分け + `wrangler secret put` |

---

## 3. 開発フェーズ

各フェーズの実装タスクは **Sonnet サブエージェント**に分割発注する(オーケストレーションは本セッションが担当)。タスクは「仕様 → 実装 → テスト → レビュー」の粒度で切る。

### 進捗サマリー(最終更新: 2026-07-23)

| フェーズ | 状態 | 進捗 |
|---|---|---|
| Phase 0: 技術検証(ゲート) | ✅ 完了 | 3/3 |
| Phase 1: 基盤構築 | ✅ 完了(残課題あり、下記参照) | 7/7 |
| Phase 2: ダメージ計算機 | ✅ 完了 | 5/5 |
| Phase 3: 育成ビルダー | ✅ 完了 | 4/4 |
| Phase 4: 検索 | ✅ 完了 | 3/3 |
| Phase 5: データ活用 | ⬜ 未着手 | 0/5 |

### Phase 0: 技術検証(ゲート) — 完了(2026-07-23)

**目的**: jpoke × ブラウザ Pyodide(案B)の実現可否を実証し、§2.2 の方式を最終確認する。

- [x] **0-1** ブラウザ Pyodide 最小 PoC: jpoke をブラウザにロードし `Battle.calc_damages()` を呼ぶページを作成(`poc/pyodide-jpoke/`)
  - 完了条件: ローカルで計算結果が jpoke ネイティブ実行と完全一致
  - 結果: **クリア**。攻撃側ピカチュウ(でんきショック)/防御側フシギダネ/seed=1 で `[9,9,9,10,10,10,10,10,10,11,11,11,11,11,11,12]` がブラウザ・ネイティブ双方で完全一致
- [x] **0-2** ロードサイズ・体感速度計測(通常回線・モバイル相当スロットリング)
  - 完了条件: 初回ロード数秒程度、ブラウザキャッシュ後は即時実行
  - 結果: **一部未達**。転送量は約6.33MB(想定10〜13MBより小さい)。通常回線の初回ロードは約2.5〜2.6秒で想定通り。**モバイル相当スロットリング(DL1.5Mbps等)では初回ロード約36〜37秒**で想定を超過。2回目ロードはPyodideのWASM初期化が再実行されるため約1.7〜1.9秒に留まり、「即時実行」は単純なブラウザキャッシュだけでは未達
- [x] **0-3** (0-1/0-2 失敗時のみ)案A/案Cの再検討
  - 結果: 致命的な不成立ではないため不要と判断。案Bを維持し、緩和策をPhase 1のタスクとして追加

**ゲート判定: 案B(ブラウザ内 Pyodide でのクライアント実行)を採用として確定。** モバイル低速回線での初回ロードが遅い課題は、Phase 1-7 に以下の緩和策を組み込むことで対応する(致命的な破綻ではないため案A/Cへの切替は不要):
- Pyodideの初期化をページ読み込み時ではなくユーザー操作(計算実行ボタン等)まで遅延させる
- Service Worker で Pyodide コア一式・jpoke wheel をオフラインキャッシュし、かつアプリ内でPyodideインスタンス自体を使い回して再初期化を避ける(SPA的な状態保持、またはIndexedDBでの初期化結果キャッシュ)
- 初回ロード中は進捗が分かるローディングUIを明示する

### Phase 1: 基盤構築 — 完了(2026-07-23)

**目的**: 「共通データベースを育てる入口」の土台。以後の全機能がここに載る。

- [x] **1-1** リポジトリ雛形: Astro + `@astrojs/cloudflare` + `wrangler.jsonc` + TypeScript strict + `worker.ts` 合成エントリ
- [x] **1-2** Supabase ローカル開発環境: Supabase CLI (`supabase start`) + `migrations/` + 自前ランナー + `.env` 系ファイル整備
- [x] **1-3** 初期スキーマ: `events` / 型付きテーブル(`damage_calcs` は `client_result`/`verified_result` を分離) / `suggestions` + **全テーブル RLS**(§2.4)
- [x] **1-4** イベントロギング API: `POST /api/events`(匿名 session_hash 発行・検証、レートリミット。**計算条件を正データとして扱い**、`client_result` は任意付与の未検証値として保存 — §2.6)
- [x] **1-5** マスタデータ生成パイプライン: jpoke → フロント用軽量 JSON(オートコンプリート) + Pyodide 実行用フルパッケージ(§2.3) のビルドスクリプト
  - jpoke は `vendor/jpoke/` にバージョン固定でvendoring済み(§4リスク表の方針どおり)。ビルドスクリプトの既定参照先も `vendor/jpoke` に変更済み
- [x] **1-6** CI: GitHub Actions(`.github/workflows/ci.yml`、`npm test` + postgres:16 マイグレーション検証)
  - Cloudflare Git 連携設定はダッシュボード側の手動設定が必要なため未着手(下記「残課題」参照)
- [x] **1-7** クライアント計算エンジンの組み込み(Phase 0 の PoC を製品化。Pyodide ロード状態管理・ローディング UI・Service Worker / IndexedDB によるキャッシュ。**Phase 0-2 で判明したモバイル低速回線での初回ロード遅延(約36秒)への対応として、Pyodide初期化のユーザー操作トリガー化・Pyodideインスタンスの使い回しによる再初期化回避を必須で含める**)
  - 同一ページ内でのインスタンス使い回しまで実装。ページ再読み込みをまたぐService Worker/IndexedDBキャッシュは未実装、今後の課題として明記

**Phase 1 完了時点の残課題(Phase 2以降 or 別途対応)**:
- [ ] Cloudflare Git連携の実設定(ダッシュボード操作。ビルドコマンドは `npm run build` 一本で完結する状態に整備済み。Pythonビルド環境がCloudflareのビルドイメージで利用可能か要確認)
- [x] Service Worker / IndexedDB によるページ再読み込みをまたいだPyodideキャッシュ(2026-07-23 追加実装)
  - `public/pyodide-sw.js`: Pyodide CDN (`cdn.jsdelivr.net/pyodide/`) と jpoke wheel (`/master-data/pyodide/`) 宛のGETのみを対象にしたcache-first Service Worker。それ以外のリクエストには一切介入しない
  - `src/lib/pyodide-engine.ts` に `registerOfflineCache()` を追加し、`damage-calc-poc` ページ読み込み時に登録(Pyodide自体の初期化トリガーとは独立、遅延初期化方針は維持)
  - IndexedDBでの初期化結果キャッシュは見送り(Service WorkerのCache Storageで要件を満たすため)
- [ ] `damage_calcs` テーブルへの二重記録(Phase 2-3で対応予定)

### Phase 2: ダメージ計算機(最初の公開機能) — 完了(2026-07-23)

- [x] **2-1** クライアント計算: ブラウザ内 Pyodide + jpoke で `Battle.calc_damages()` を実行(サーバ API 呼び出し不要、結果 + 乱数16段階・致死率をその場で算出)
  - `src/lib/pyodide-engine.ts` に `FieldSpec`(天候・地形・防御側の壁)と `calc_lethal()` による1発〜N発致死率(`lethal`)を追加
- [x] **2-2** 計算 UI: 攻撃側/防御側のポケモン・技・努力値(0〜32)・性格・持ち物・特性・テラス・フィールド状態入力、オートコンプリート
  - `/damage-calc`(本番UI)。既存の `damage-calc-poc` は Phase 1-7 の検証用として維持
- [x] **2-3** 計算ログ蓄積: 計算実行ごとに計算条件を `damage_calcs` + `events` へ匿名記録(`client_result` は UI 表示用に任意添付、集計には使わない — §2.6)
  - `POST /api/damage-calcs`(events.ts と同じセッション発行・レートリミット方式)
- [x] **2-4** 「よく比較される育成 / 人気の仮想敵」表示枠: 当面は `suggestions` テーブル読み出しの空枠(Phase 5 で中身が入る)
  - `GET /api/suggestions`(anonクライアント経由の公開閲覧API)+ `/damage-calc` 内の空状態表示
- [x] **2-5** E2E 検証: jpoke のテストケースから代表パターンを抽出し、ブラウザ実行結果と jpoke ネイティブ実行の等価性を回帰テスト
  - Playwright導入。10パターン(通常技/タイプ相性/特性補正/持ち物補正/急所固定/多段技/天候/地形/壁)で `npm run test:e2e` が全件pass。CIに`e2e`ジョブ追加

### Phase 3: 育成ビルダー — 完了(2026-07-23)

- [x] **3-1** 育成 CRUD API + 共有スラッグ(`/builds/[slug]` で閲覧可能な公開 URL)
  - `POST /api/builds`(保存)、`GET /api/builds/:slug`(公開閲覧、RLS経由でis_public=trueのみ)
- [x] **3-2** ビルダー UI: 努力値配分・実数値表示(jpoke の `stats` 計算と一致させる)・技/持ち物/特性選択
  - `/builds/new`。`pyodide-engine.ts` に `calcStats()`(`Pokemon.stats` をそのまま利用)を追加
- [x] **3-3** ダメージ計算機との連携(作った育成をワンタップで計算機に読み込み)
  - `/damage-calc?build=<share_slug>` で攻撃側フォームへ自動反映
- [x] **3-4** 保存・作成イベントの蓄積
  - `POST /api/builds` が `damage_calcs` と同じ二重記録方式で `events`(`event_type='build_save'`)へも記録

### Phase 4: 検索 — 完了(2026-07-23)

- [x] **4-1** 検索 API: ポケモン・技・特性・持ち物の横断検索(マスタ JSON ベース + Postgres 全文検索は必要になったら)
  - `POST /api/search`。マスタJSONをビルド時にバンドルし単純な部分一致検索。`public/master-data/detail/{pokemon,moves}.json` を新設(種族値・技等の詳細表示用、特性/持ち物は説明文データが無いため見送り)
- [x] **4-2** 検索 UI + 詳細ページ(`/pokemon/[name]` など)
  - `/search`(デバウンス検索)、`/pokemon/[name]`・`/moves/[name]`(`getStaticPaths()`でビルド時に全件事前生成、ポケモン1290件・技716件、DB非依存)
- [x] **4-3** 検索ログ蓄積 + 「最近注目されているもの」表示枠
  - `searches`+`events`への二重記録(既存パターン踏襲)、`/search`内で既存の`GET /api/suggestions?kind=trending_search`を再利用

### Phase 5: データ活用(集合知の還元) — 未着手

- [ ] **5-1** 集計 cron ジョブ: 1つの Cron Trigger + 分スロット分岐で「人気の育成 / よく比較される組み合わせ / 努力値傾向 / 注目検索」(入力ベース)を日次集計し `suggestions` へ書き込み
- [ ] **5-1b** 結果検証バッチ(§2.6): GitHub Actions の定期実行でネイティブ Python + jpoke を使い、`damage_calcs` のユニークな組み合わせ(attacker/defender/move/field)を再計算。検証済み結果を `verified_result` / `suggestions` へ書き込み、「勝率の高い調整 / おすすめの耐久ライン」を解放
- [ ] **5-2** 各画面のサジェスト枠に集計結果を接続(Phase 2-4 で用意した空枠を埋める)
- [ ] **5-3** プライバシーレビュー: k-匿名性(表示は n 件以上集まったものだけ)の確認
- [ ] **5-4** 評価スクリプト(`scripts/eval/`): 集計品質・subrequest 消費量の計測(poke-research の eval パターン)

将来(構想の「メタゲーム分析」「AI育成支援」等)は Phase 5 のデータが貯まってから別プランで扱う。

---

## 4. リスクと対応

| リスク | 影響 | 対応 |
|---|---|---|
| ブラウザ Pyodide の初回ロードが重い/低スペック端末で遅い | UX 低下・離脱率上昇 | Phase 0 でロードサイズ・体感速度を実測してゲート判定。ローディング UI・バックグラウンドプリロード・Service Worker / IndexedDB キャッシュで初回以降を高速化。想定を超えて破綻した場合のみ Paid プラン前提の案A、または案C(TS移植)を検討 |
| クライアント計算結果の捏造・改ざん | 集計データ(サジェスト)の汚染 | 入力ベースの機能(大半)は結果に依存しない設計にし、結果依存の機能(勝率の高い調整・耐久ライン)は GitHub Actions によるユニーク組み合わせ単位のサーバ再計算で検証してから提供(§2.6) |
| Cron Trigger アカウント上限(残り2枠) | 集計ジョブが登録できない | 1 Cron + 分スロット分岐に最初から集約(残り枠内で対処する方針で決定済み)。Cloudflare Free プランは HTTP・Cron とも CPU 10ms/呼び出しのため、重い再計算はここに乗せず GitHub Actions へ分離(§2.6) |
| Worker subrequest 上限(50/呼び出し) | 集計ジョブの失敗 | poke-research の教訓どおりバッチ分割・`eval-subrequests` 相当の計測を Phase 5 に組み込み |
| 匿名データのプライバシー懸念 | 信頼毀損 | IP/UA 非保存・日替わり session_hash・k-匿名性表示を設計段階から適用 |
| jpoke 側の仕様変更(努力値形式など) | 計算結果の不整合 | jpoke をバージョン固定で vendoring し、更新は回帰テスト付きで取り込む |
| RLS 後付けの手戻り | セキュリティ指摘対応 | Phase 1 の初期マイグレーションから全テーブル RLS 有効 |

---

## 5. 実行体制(サブエージェント運用)

- **指揮**: 本セッション(タスク分解・発注・レビュー・統合・ゲート判定)
- **実装**: Sonnet サブエージェント。1タスク = 1エージェントを原則とし、並列可能なものは同時発注
- 各タスクの発注書には「対象ファイル・完了条件・テスト方法・poke-research の参照パス」を明記する
- フェーズ末に統合レビュー(必要に応じて別エージェントによるコードレビュー)を実施

## 6. 直近のアクション

- [x] Phase 0-1/0-2: jpoke × ブラウザ Pyodide の最小 PoC・計測 → 完了(2026-07-23)、案B確定
- [x] Phase 1 のタスク発注・実装(1-1〜1-7 全タスク完了、2026-07-23)
- [ ] Phase 1 残課題の解消(Cloudflare Git連携の実設定 — ダッシュボード操作のため引き続き未着手。Service Worker・IndexedDBキャッシュ・`damage_calcs`二重記録はPhase 1/2-3で解消済み)
- [x] Phase 2 のタスク発注・実装(2-1〜2-5 全タスク完了、2026-07-23。2-1を先行実装後、2-2/2-3/2-5を並列発注)
- [x] Phase 3 のタスク発注・実装(3-1〜3-4 全タスク完了、2026-07-23。3-1/3-2を並列発注後、3-3を発注)
- [x] Phase 4 のタスク発注・実装(4-1〜4-3 全タスク完了、2026-07-23。4-1を先行実装後、4-2/4-3を発注)
- [x] Phase 2/3/4 のコードレビュー(セキュリティ・正確性、2エージェント)実施、指摘2件(リクエストサイズ上限・二重書き込み失敗時のレスポンス)を修正済み
- [ ] Phase 5 のタスク発注(データ活用: 5-1 集計cronジョブから着手)
