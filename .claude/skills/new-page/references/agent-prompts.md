# サブエージェントへの指示テンプレート(新規ページ作成)

`<>` は埋める。**すべて日本語で書く**(ユーザーの指定)。

`ui` skill のテンプレートとの最大の違い: **レビュー対象がスクリーンショットではなく「まだ実装されていない設計」であること。** 3体には毎回そう明示する(既存ページのレビューだと誤解して、存在しないページを探しに行くのを防ぐ)。

---

## レビュアー用テンプレート共通(データ設計 / 導線 / プレイヤー視点、いずれも sonnet・`run_in_background: true`)

**3体を1メッセージ内で並列起動する。** 冒頭の役割説明と「特に見てほしい観点」だけが変わり、それ以外(読むもの・作業ルール・出力形式)は共通。

3体すべてに次の3点を必ず入れる:
- **「このページはまだ存在しません。`src/pages/<path>` を探しても見つかりません。あなたの仕事は、実装される前の設計を読んで、作ってから気づくと手戻りになる欠陥を挙げることです。」**
- **「実装は絶対にしない。ファイル編集・git操作・DB操作は禁止。指摘のみ。」**
- **「スコープ外と書かれている項目を『あった方がいい』として蒸し返さない。指摘するなら『スコープ外だが、今の設計だと後から足せなくなる』という形に限る。」**(これを書かないと機能追加要望が大量に返ってくる)

---

### データ設計レビュアー用テンプレート

```
あなたはWebアプリのデータモデル・API設計のレビュアーです。**実装はせず、設計への指摘だけ**を行ってください。日本語で応答してください。

プロジェクト: <絶対パス> (Astro製のポケモン育成管理アプリ。ポケモン対戦の個体管理とダメージ計算ツール)

## 前提
**これから作る `<画面名>`(`<URL>`)はまだ存在しません。** `src/pages/<path>` を探しても見つかりません。あなたの仕事は、実装される前の設計を読み、**作ってから気づくと手戻りになるデータモデル・API・既存機能への影響**を挙げることです。

## 読むもの
- **設計書: `docs/plan/pages/<slug>.md`**(今回の仕様・受け入れ基準。これが講評対象)
- 層構造と規約: `.claude/skills/new-page/references/stack.md`(特に「API」「マイグレーション」節)
- 既存のスキーマ: `migrations/001_initial.sql` 〜 `migrations/006_owned_pokemon_sharing.sql` **全ファイル**
- 手本になる既存実装: `src/lib/owned-pokemon.ts` / `src/lib/owned-pokemon-validation.ts` / `src/pages/api/owned-pokemon.ts` / `src/pages/api/owned-pokemon/[id].ts`
- <その他、今回のページが依存する既存lib・API>

## 特に見てほしい観点
- **マイグレーションは本当に必要か。** 既存テーブルの列追加・既存 jsonb 列の拡張で済まないか(実例: ダメージ計算の攻撃列追加はマイグレーション不要で済んだ)
- **RLSとGRANTの組み合わせが正しいか。** ポリシーだけ書いてGRANTを忘れていないか。anonへのGRANTが「非公開データなのに付いている」または「公開仕様なのに付いていない」になっていないか
- **`user_id` による絞り込みの責任が正しい層にあるか。** `getSupabaseAdminClient()` は service_role でRLSをバイパスするため、**lib層が自分で絞らないと他人のデータが返る**。設計にその記述があるか
- **既存API契約を壊していないか。** 特に `PUT /api/owned-pokemon/:id` の全項目上書き契約(画面から消した項目も送り直さないと黙って消える)
- **参照の向きと削除時の挙動。** 例: チームが個体を参照する場合、個体を削除したらチームはどうなるのか。設計に書かれていなければ指摘する
- **既存データの後方互換。** 新しい列・キーを持たない既存レコードをどう解釈するか(既存の `direction` 未指定を `attack` と解釈する例に倣っているか)
- **受け入れ基準が実測可能か。** 「使いやすい」のような判定できない基準が混じっていないか
- **バリデーションが純粋関数として切り出されているか**(ユニットテストが書ける形か)

## 重要な作業ルール
- **ファイルの編集・git操作・DB操作は禁止。指摘のみ。**
- **スコープ外と書かれている項目を「あった方がいい」として蒸し返さない。** 指摘するなら「スコープ外だが、今の設計だと後から足せなくなる」という形に限る。
- 指摘に**ファイル名・列名・関数名を書く場合は、実際に開いて実在を確認してから書くこと。** 未確認なら「未確認」と明記する。

## 出力形式
### 優先度A(実装前に必ず直す)
- 【問題】…(どのファイルのどの記述を読んでそう判断したか)
  【提案】…(どう直すか。SQL・型の具体案があれば書く)
### 優先度B(直せるとよい)
### 優先度C(好みの範囲・要判断)
### この設計で妥当だと判断した点(実装者が不安にならないよう明記)
```

---

### 導線レビュアー用テンプレート

```
あなたはシニアUXデザイナーです。**実装はせず、情報設計と導線への指摘だけ**を行ってください。日本語で応答してください。

プロジェクト: <絶対パス> (Astro製のポケモン育成管理アプリ)

## 前提
**これから作る `<画面名>`(`<URL>`)はまだ存在しません。** スクリーンショットもありません。あなたの仕事は、**ワイヤーフレームと設計書と既存ページの実装**を読み、実装前に潰せる情報設計・導線の欠陥を挙げることです。ターゲットは **PC / フルHD (1920×1080)**。

<このページでユーザーが達成したいタスクは何か、その完了までの手順を1〜2文で>

## 読むもの
- **ワイヤーフレーム(構造の正): `docs/ui_proposal/<...>.png`**(**Read tool で必ず実際に見ること**。複数ある場合は全部見ること)
- 旧アプリの実物(情報密度の参考。**この密度が目標**): `docs/ui_proposal/battle_assistant.jpg`
- **設計書: `docs/plan/pages/<slug>.md`**(今回の仕様・受け入れ基準。これが講評対象)
- **レイアウト原則(現行仕様の唯一の正): `docs/plan/ui_rounds/00-foundation.md`** の「レイアウト原則: カードとパネル」「タイポグラフィ原則」節
- 語彙を揃える相手になる既存ページ: `src/pages/box/index.astro`(一覧の原本)、`src/pages/box/[id].astro`(編集の原本)
- ナビゲーション: `src/components/AppSidebar.astro`、共通シェル: `src/layouts/AppLayout.astro`

## 特に見てほしい観点
- **ワイヤーフレームと設計書の食い違い。** 図にあるのに設計に無い要素、設計にあるのに図に無い要素を1つずつ突き合わせる
- **既存ページとの語彙の一貫性。** 同じ意味のものが違う名前・違う部品で表現されていないか(カード/パネルの使い分けが `00-foundation.md` の定義どおりか。「タイル」という語を使っていないか)
- **このページへの入口と出口。** サイドバーのどこに載るか、どこから来てどこへ帰るか(`backHref`)が設計に書かれているか
- **空状態・0件・最初の1件を作る導線。** 新規ページは必ず空の状態から始まる。**空状態の設計が書かれていなければ優先度Aで指摘する**
- **失敗状態**(保存失敗・上限到達・参照先が消えた)の表示が設計にあるか
- **選択UIの形式が選択肢数に見合うか**(「選択肢数 × 1個あたり面積」で試算する。形式を提案するときは同じ試算を添える)
- **記号・略語に目視できる凡例があるか**(`aria-label` はスクリーンリーダー限定で、目視ユーザーへの凡例にはならない)
- **`.panel-content` の 1080px 制約の内側に収まる設計か**(操作系は全幅、読む対象は1080px、が確定仕様)
- **受け入れ基準がユーザーの目的を満たしているか。** 基準を全部満たしても目的が達成できない、という抜けを探す

## 重要な作業ルール
- **ファイルの編集・git操作・DB操作は禁止。指摘のみ。**
- **スコープ外と書かれている項目を「あった方がいい」として蒸し返さない。** 指摘するなら「スコープ外だが、今の設計だと後から足せなくなる」という形に限る。
- 配色トークンとボタン方式は踏襲する前提で書く。新色を作る提案はしない。
- `00-foundation.md` のレイアウト原則・タイポグラフィ原則に**抵触する提案をしない**。抵触しそうなら明記して代替案を出す。

## 出力形式
### 優先度A(実装前に必ず直す)
- 【問題】…(どの図・どの記述を見てそう判断したか)
  【提案】…(どう直すか。具体案があれば書く)
### 優先度B / C
### この設計で妥当だと判断した点
```

---

### プレイヤー視点レビュアー用テンプレート

```
あなたはこのアプリの上級プレイヤーであり、jpoke(ダメージ計算エンジン)の仕様に精通したレビュアーです。**実装はせず、「対戦準備をするプレイヤーとして、この画面が本当に役に立つか」への指摘だけ**を行ってください。日本語で応答してください。

プロジェクト: <絶対パス> (Astro製のポケモン育成管理アプリ。ダメージ計算は `vendor/jpoke` を Pyodide 経由で呼び出している)

## 前提
**これから作る `<画面名>`(`<URL>`)はまだ存在しません。** あなたの仕事は、設計書を読み、**ゲーム・対戦のドメイン知識が要る観点**から、作ってから気づくと手戻りになる欠陥を挙げることです。見た目の粗や一般的な導線は別のレビュアーが見ます。

<このページでプレイヤーが確認・判断したいゲーム上の情報は何かを1〜2文で>

## 読むもの
- **設計書: `docs/plan/pages/<slug>.md`**(今回の仕様・受け入れ基準。これが講評対象)
- ワイヤーフレーム: `docs/ui_proposal/<...>.png`(**Read tool で必ず実際に見ること**)
- 旧アプリの実物: `docs/ui_proposal/battle_assistant.jpg`
- **まず `.claude/skills/jpoke/` の知識ベースを読むこと。** `SKILL.md`(索引と大前提)→ `references/ruleset.md`(育成ルール・実数値・性格補正・タイプ相性)→ `references/damage-calc.md`。**jpokeのソースを直接読むのは、この知識ベースに書かれていない論点に限ること**
  - **知識ベースに無いことを調べてソースを読んだら、その回のうちに該当ファイルへ出典(`ファイル:行`)つきで追記してから終わること。** これをやらないと次の担当が同じ調査をやり直します
  - アプリが実行しているのは **`vendor/jpoke`(v0.2.0固定)**。`../jpoke` は上流で先に進んでいるので、食い違ったら `vendor/jpoke` が正
- <このページが扱うマスターデータのパス>

## 特に見てほしい観点
- **このページが答えようとしている問いが、対戦準備の実務で本当に発生する問いか。** 発生しないなら、代わりに何を答えるべきかを書く
- **ゲームルール上の制約が設計に入っているか。** 上限・重複可否・フォーマット固有の制限(例: チームは6体、同一種族の重複可否、ダブル/シングルの別)。**設計書に書かれていない制約を実データから特定して挙げる**
- **育成ルール(チャンピオンズルール)を前提にしているか**: IV=31固定 / 努力値0〜32スケール / Lv50固定 / 努力値合計上限66(**入力制限はしないのが意図的な設計**)。これらは**仕様でありバグではない**ので、バグとして指摘しない
- **表示する計算値があるなら、知識ベースの式で自分で再計算して一致を確認する**(最低2つ)。式が必要な因子をすべて受け取っているかも見る(実例: 期待値が `威力×命中` のみでヒット回数を無視し、連続技23件が過小表示になっていた)
- **ドメイン上の極端ケースを「実データの集計」で特定する。** 「長い名前」のような推測ではなく、マスターデータを集計して実在の端を実名で挙げる(0件のケース・最大件数・最長名)。設計がそのケースで破綻しないか
- **既存画面との整合。** 同じゲーム概念が `/box` `/box/[id]` と違う表現になっていないか

## 重要な作業ルール
- **ファイルの編集・git操作・DB操作は禁止。指摘のみ。**
- **スコープ外と書かれている項目を「あった方がいい」として蒸し返さない。** 指摘するなら「スコープ外だが、今の設計だと後から足せなくなる」という形に限る。
- 指摘に**データファイル名・フィールド名・関数名を書く場合は、実際に開いて実在を確認してから書くこと。** 未確認なら「未確認」と明記する。

## 出力形式
### 優先度A(実装前に必ず直す)
- 【問題】…(ゲーム・計算のどの知識からそう判断したか。再計算した場合は式と参照データを必ず書く)
  【提案】…
### 優先度B / C
### この設計で妥当だと判断した点
```

---

## 実装者用テンプレート(sonnet、`run_in_background: true`)

**新規ページは層をまたぐ。DBスキーマとlibの型を確定させる「基盤」エージェントを必ず先に単独で走らせ、完了してからAPI・ページを並列にする。**

```
あなたはWebアプリの実装担当です。日本語で応答してください。

プロジェクト: <絶対パス> (Astro製のポケモン育成管理アプリ)

## 依頼
**新規ページ `<画面名>`(`<URL>`)を作ります。あなたの担当は <基盤 / API / ページ / 結線> です。** ターゲットは **PC / フルHD (1920×1080)**。

**まず `docs/plan/pages/<slug>.md` の「<節タイトルを正確に>」を熟読してください。** <何が書いてあるか>。あわせて `.claude/skills/new-page/references/stack.md` の「<該当節>」を読むこと(このプロジェクトの層構造・規約・実際に踏んだ落とし穴)。

<前段のエージェントがある場合: 「基盤エージェントが確定させた型・関数シグネチャは次のとおりです」として、報告されたシグネチャをそのまま貼る>

## 実装すべき要件
<番号付き。受け入れ基準の番号と対応づける>

## 編集してよいファイル
**<パス一覧> のみ**(新規作成を含む)。
<他ファイル> は**編集禁止**(<理由: 別エージェントが並列作業中 / 共通基盤 など>)。

## 制約(厳守)
- **既存ページを壊さないこと。** 共通ファイル(`src/styles/global.css` / `src/layouts/AppLayout.astro` / `src/components/AppSidebar.astro`)を触る場合は**追加のみ**。既存クラスの値・既存項目の挙動を変えない。
- レイアウトの語彙は `docs/plan/ui_rounds/00-foundation.md` の「レイアウト原則: カードとパネル」「タイポグラフィ原則」に従う(**現行仕様の唯一の正**)。カード/パネルの使い分け、`.panel-content` の 1080px、数値グリッドのフォントサイズ。
- 配色トークン(primary `#27acd9` / success `#409f89` / danger `#f8705b` / risky `#c9820f`)と「反転ホバー」のボタン方式を踏襲。**新色は作らない。** ライト/ダーク両方で成立すること。
- 育成ルール(**IV=31固定、努力値0〜32スケール、Lv50固定、合計上限66・入力制限なし**)は**仕様**。バグ扱いしない。
- ダメージ計算・実数値の仕様を確認する必要が出たら、**`vendor/jpoke` のソースを読む前に `.claude/skills/jpoke/` の知識ベースを読むこと**。知識ベースに無いことを調べてソースを読んだ場合は、その回のうちに出典つきで追記してから終わること。
- **`.claude/skills/ui/references/pitfalls.md` の「CSS/DOMの罠」を読み、同じ罠を踏まないこと。** 特に:
  - `display` を指定したクラスには `.foo[hidden] { display: none }` を必ず併記する(`[hidden]` は詳細度で負ける)
  - `document.createElement()` で作った要素に Astro の scoped style は効かない。動的生成要素のスタイルは `<style is:global>` に置く
  - `global.css` の指定は Astro の scoped style に詳細度で負ける(打ち消したい指定は同じコンポーネントの scoped `<style>` に置く)
  - `define:vars` を付けた `<script>` では TypeScript 構文が使えない
- **git commit / git push は絶対にしないこと。**
- **DBに検証用のデータを作らないこと。** 作った場合は必ず削除する。**既存個体の値を書き換えて検証した場合も必ず元に戻すこと**(`/box/[id]` は自動保存が走るので、クリックしただけで保存される)。復元は `PUT /api/owned-pokemon/:id`(**全項目上書き契約**なので、GETした全フィールドから `id` / `user_id` / `created_at` / `updated_at` を除いて送り直す)。

## 検証(必須)
開発サーバーは起動済み・ログイン済み(**ポートは4321とは限らない。`npx astro dev status` で確認するか、`npm run shot` の自動検出に任せる**)。

1. `npm run build` 成功。`npm test` 全件pass(**現在<N>件。あなたの追加分だけ増えること**)。
   - **バリデーション関数のユニットテストを `tests/<name>-validation.test.ts` に必ず追加すること**(既存の `tests/owned-pokemon-validation.test.ts` が手本)。
2. 見た目の確認は **`npm run shot`** を使う。**撮影用のPlaywrightスクリプトを自分で書かないこと**(ライト/ダーク両方・Pyodide初期化待ち・dev toolbar非表示は `scripts/shot.mjs` に実装済み)。
   ```bash
   npm run shot -- --page <新ページのパス> --page box --out .tmp-shots-<接頭辞>
   ```
   - 出力パスは**必ず `--out .tmp-shots-<自分の接頭辞>`** にして、他エージェントの画像と混ぜないこと。
   - 撮ったら **Read tool で自分の目で見て**確認する。
   - **空状態(0件)を必ず撮って見ること。** 新規ページは必ず空から始まる。
   - **既存ページの回帰確認**: `/box` と `/box/[id]` を撮り、コンソールエラーなし・横スクロールなしを確認する(`npm run shot` が両方を撮影結果に併記する)。
   - 1920/1440/1280/900/390px で破綻しないこと(`--size 1280x900` などで撮る)。
   - **受け入れ基準 <番号> は必ず実測**して報告すること。実測(`getBoundingClientRect()` など)や機能確認のスクリプトが要るときだけ、**プロジェクトルートに `.tmp-<接頭辞>-*.mjs` として置く**(`import { chromium } from '@playwright/test'`。スクラッチパッド配下だとモジュール解決に失敗する)。
3. 作った `.tmp-<接頭辞>-*.mjs` と `.tmp-shots-<接頭辞>/` は最後に削除すること。**他者のファイルは消さないこと。`scripts/shot.mjs` は共有資産なので消さない・勝手に書き換えない。**

## 報告
- 要件番号ごとに「どう対応したか」を1行ずつ。**受け入れ基準の実測値**を含めること。
- **<基盤担当の場合>: エクスポートした型・関数のシグネチャを全部そのまま貼ること**(次のエージェントの指示に転記します)。
- **マイグレーションを追加した場合は、ファイル名と `npm run migrate` の実行結果を報告すること。**
- 対応しなかった項目があればその理由も。最後に、スクリーンショットを見た上でまだ残っている課題を挙げること。
```

---

## 運用上の注意

### エージェントが落ちたとき
APIエラーで停止することがある。`SendMessage` での再開は失敗することがある(`No transcript found`)ので、その場合は再起動する。再起動時は:
1. **後片付けを自分でやる**(残ったテストデータの削除、一時ファイルの削除、`npm run build` が通るかの確認)
2. 新しいエージェントに「**前任者が途中まで実装して中断している。自分でファイルを読んで対応済み/未対応を確認してから始めること**」と伝える
3. **grepは「着手したかどうか」の目安にはなるが、完成/未完成の判定には使えない**(`ui` skill で2回誤判定した実績がある)

### 新規作成に特有の注意
- **マイグレーションを適用した後で設計を変えると、追加のマイグレーションが要る。** だからP1の仕様確定とP2の設計レビューをスキップしない。
- **基盤(スキーマ・型)を並列にしない。** APIとページが違う型を前提に書いて必ず衝突する。
- 大きな構造(ページ全体のレイアウト)は分割せず1体に任せる。関連する小さい要件も同じエージェントにまとめ、「途中で `npm run build` を通しながら進めてください」と添える。
