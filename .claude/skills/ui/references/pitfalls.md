# このプロジェクトで実際に踏んだ罠

UI改修作業で**実際に事故った/時間を溶かした**ものだけを載せている。実装エージェントへの指示に関係するものを毎回コピーして渡すこと。

---

## データ事故(最優先)

### ✅ 不完全なボディの `PUT /api/owned-pokemon/:id` が個体を全消去していた(2026-07-28 修正済み)

**現在は400で弾かれる**(ボディなし / `{}` / キー欠け / `null` / 配列 / 文字列 のいずれも)。
以下は**なぜそうなっていたかの記録**であり、**復元時の作法は今も有効**なので残す。

**復元のつもりのPUTが、消去になっていた。** CTA復元エージェントが検証のため `is_public` を切り替えようとして**対象個体の `species_name` / `nature` / `ability_name` / `item_name` / `evs` / `move_names` を全部空にしてしまった**(ローカルSupabaseへ直接PostgREST PATCHを送って復元)。

**経路(コードで確認済み)**:
1. `src/pages/api/_shared.ts:60-66` — `readJsonBody` は**空ボディをエラーにせず `null` を返す**
2. `src/pages/api/owned-pokemon/[id].ts:55` — `validateOwnedPokemonRequestBody(body.data ?? {})` で `null` が **`{}`** になる
3. `src/lib/owned-pokemon-validation.ts:46-106` — **全フィールドが `X !== undefined && ...` の任意項目**なので `{}` は検証を通る
4. `updateOwnedPokemon` が**全項目上書き契約**で走り、全フィールドが空で保存される。**400ではなく200が返る。**

**このセッションで複数のエージェントが報告した「誰も触っていないのにフィクスチャが書き換わる」現象の有力な原因。**

**修正の内容(2026-07-28)**: `_shared.ts` に `readRequiredJsonBody()` を足してボディ無しを400にし、
`validateOwnedPokemonRequestBody` に `{ mode: 'replace' }` を追加して **PUT のときだけ13キーの存在を必須**にした
(`undefined` は拒否、`null` は許容)。POST は「＋ 個体を追加」の空個体登録があるため緩いまま。

**⚠️ 1回目の修正は不完全だった。** 「ボディが無いとき」だけを400にしたが、**`{}` を送るとまだ消去された**。
Coordinatorが実測して発覚し、2回目でバリデーション層を直して塞いだ。
**「ボディがあるか」ではなく「上書きに必要な項目が揃っているか」を見ないと塞がらない。**

**検証で復元するときの必須手順**(修正後も有効):
- PUTには**必ず `Content-Type: application/json` を付け、ボディが実際に届いていることを確認する**(レスポンスの `data` を見て、送った値が返ってきているか突き合わせる)。**200が返っても成功とは限らない。**
- **復元後は必ず `GET` して元の値と全フィールド突き合わせる。** 「PUTが200だったから戻った」と判断しない。
- 確実にやるなら、**ローカルSupabase(`http://127.0.0.1:54321`、`.env.local` の service role key)へ直接 PostgREST PATCH** を送る。PATCHなら指定したフィールドだけが変わる。

### 自動保存があるので、検証クリックがデータを壊す
`/box/[id]` は**入力・クリックで自動保存が走る**。検証スクリプトでボタンを押すと、そのままDBに書き込まれる。
- **実例**: ステータスラベルの動作確認でクリックしたら、メガリザードンXの性格が「いじっぱり」→「まじめ」に書き換わり、そのまま気づかれず基準値として扱われかけた。
- **対策**: 状態を変える操作をする前に**元の値をAPIで控え、検証後に必ずPUTで戻す**。エージェントへの指示に「変更したデータは必ず元に戻すこと」を明記する。
- 復元は `PUT /api/owned-pokemon/:id`。**全項目上書きの契約**なので、GETした全フィールドから `id` / `user_id` / `created_at` / `updated_at` を除いて送り直す。

### 密度検証でDBに個体を作らない
- **実例**: 20体時の密度を測るためにAPIで16体作ったエージェントが、後片付け前にAPIエラーで落ちた。手動削除する羽目になった。
- **対策**: 既存カードを `cloneNode(true)` でグリッドに複製して `getBoundingClientRect()` で測る。DBを汚さず同じ結論が出る。**指示に明記する。**

### エージェントの完了報告そのものが「作業した」ことの証明にならない
- **実例**: 実装を指示したエージェントの1回目の実行が、Read/Edit/Bashを一度も呼ばず(`tool_uses: 0`)、**別のAgentを呼び出すコード風のテキストを「結果」として返しただけ**で終わっていた。指示対象のCSS値(`.item-image-badge`のサイズ、`.stat-table-header-ev-remaining`のtext-align)は実際には1文字も変わっていなかった。
- **対策**: エージェントの報告を受け取ったら、**必ず`git diff --stat`(変更行数)と`grep`で対象の具体的な値そのものを確認する。** 報告の文章がどれだけ詳細で自信に満ちていても、ファイルの実体を見るまでは信用しない。疑わしい場合(行数が0、または想定より極端に少ない)は、プロンプトに「別のAgentへ委任せず、あなた自身がRead/Edit/Bashで直接編集すること」と明記して再実行する。

### エージェントの「クリーンアップ済み」報告も検証の代わりにならない
- **実例**: datalistの再クリック挙動を検証するエージェントが、`POST /api/owned-pokemon`で使い捨て個体(コラッタ1体・ピカチュウ2体)を作ってテストし、報告では「`DELETE`で消した。フィクスチャは触っていない」としていたが、**実際には3体ともDBに残ったまま**だった(検証終了後、Coordinatorが`GET /api/owned-pokemon`で一覧を数えて発見)。
- **対策**: 使い捨て個体を作るテストを指示したら、検証の終わりに必ずCoordinatorが`GET /api/owned-pokemon`(または`/box`のスクリーンショット)で**既知のフィクスチャ一覧と実際の件数・種族名を突き合わせる**こと。「DELETEしました」という報告だけでは不十分(同種の教訓は「PUTが200だったから戻った」判断禁止と同じ形)。

### 入力欄を直接操作するテストは既存フィクスチャではなく使い捨て個体で
`/box/[id]` の種族名・持ち物などの入力欄を実際に操作して機能(例: メガストーン自動設定)を検証するとき、既存フィクスチャ(`メガリザードンX` 等)を使うと**自動保存のタイミングで一瞬でも不正な値がDBに残る**。「テスト後に戻すつもり」でも、戻し忘れ・戻し漏れ(一部フィールドだけ)のリスクがある。
- **実例**: 実施結果の検証で、メガストーン自動設定のテストのため既存の「ディンルー」個体(技0件フィクスチャ)の種族名を直接書き換えたところ自動保存が走り、`species_name` が `メガリザードンX` に上書きされた。`GET` で発覚し `PUT` で復元。
- **対策**: **`POST /api/owned-pokemon` で空個体を作り、そちらで入力操作のテストをしてから `DELETE` で消す。** 既存フィクスチャの入力欄は触らない。DELETEは `isSameOrigin` チェックに掛かるため、curlで直接叩くときは `Origin` ヘッダを付与すること(付けないと "Cross-site DELETE form submissions are forbidden" になる)。

### `PUT /api/owned-pokemon/:id` のボディは payload を直接トップレベルに置く(`{data: ...}` で包まない)
クライアント(`box/[id].astro`)は `fetch(..., { body: JSON.stringify(payload) })` で `payload` オブジェクトをそのまま送る。`validateOwnedPokemonRequestBody(body.data, ...)` の `body.data` は **`readRequiredJsonBody()` が返すラッパーの `.data`**(=リクエストボディ全体)であり、JSON側に `"data"` キーを追加する必要はない。curlで直接叩くとき `{"data": {...}}` のように包むと、全フィールドが `undefined` 扱いになり `"nickname is required..."` のような400が返る。

### 対戦相手メモ(`opponent-notes`)は作ってよいが必ず消す
カード複数枚の状態は `POST /api/opponent-notes` で作れる。**最後に `DELETE /api/opponent-notes/:id` で消す**こと。既存の1件は消さない。

### 🔴 「個体は元に戻した」は、ダメージ計算カードが無事であることの証明にならない

**耐久調整の検証で実際に起きた。** サブエージェントは指示どおり `POST /api/owned-pokemon` で使い捨て個体を作り、`DELETE` して `GET /api/owned-pokemon` の件数・種族名が一致することを確認して報告した。**その報告は正しかった**(個体テーブルは無傷)。しかし Coordinator がスクリーンショットを見て、**メガリザードンX(`c8680844-...`)のダメージ計算カードが4枚→3枚に減り、内容も書き換わっている**ことを発見した。

| カード | 基準値 | 発見時 |
|---|---|---|
| カイリュー / 攻撃 / スケイルショット5ヒット | 確1 175〜210 | 無事 |
| フシギバナ(キョダイ) / 防御 / **でんきショック** | 9〜11 | 技が **じしん** に変わっていた |
| ハバタクカミ / 攻撃 / フレアドライブ | 確1 225〜265 | **カードごと消失** |
| メガカメックス / **攻撃** / あくのはどう | 確4 31〜37 | direction が **defense** に変わっていた |

- **原因**: 「防御方向のカードが2枚ある状態」をテストしたかったエージェントが、使い捨て個体ではなく**既存フィクスチャのカードを直接いじった**(向きの変更・技の差し替え・不要カードの削除)。`opponent_notes` は `owned_pokemon` とは**別テーブル**なので、個体の件数照合をいくらやっても検出できない。
- **対策1**: 検証の後片付け確認は、`GET /api/owned-pokemon` だけでなく **`GET /api/opponent-notes?owned_pokemon_id=<id>` で枚数・`opponent_build.name`・`field.direction`・`move_name` を突き合わせる**こと。**クエリパラメータ名は `owned_pokemon_id`**(`owned` ではない。間違えると400が返る)。
- **対策2**: エージェントへの指示に「**対戦相手メモ(ダメージ計算カード)にも一切触れない**」を明記する。「個体を壊すな」だけでは足りない。カードの状態を変えるテストが要るなら、**使い捨て個体の上にカードを作らせる**。
- **復元の手掛かりはスクリーンショットにしかなかった。** 作業開始時に撮った全体スクリーンショットから、技名・向き・努力値・実数値・確定数を読み取って再構築した。**実数値から性格を逆算できる**(例: ハバタクカミ 130/67/75/187/155/205 は おくびょう・努力値 `[0,0,0,32,0,32]`・IV31 と一致)。**作業開始時の全体スクリーンショットは、事故が起きたときの唯一の基準値になるので必ず撮っておくこと。**
- 復元後は **`PUT` が200を返したことではなく、画面の確定数・実数値が基準値と一致すること**で確認する。

### 現在のフィクスチャ(dev環境)
| 用途 | ID | 備考 |
|---|---|---|
| ダメージ計算カードあり | `c8680844-dd43-42a4-bdf1-f3de11fe3267` | メガリザードンX / いじっぱり / かたいツメ(26-L1で自動設定) / こだわりハチマキ / **テラス「ほのお」** / 技2。実数値 153/200/131/135/105/136 |
| 技4つ・ニックネームあり | `8f743377-7cff-4fa7-b42e-8dae8515107f` | ハバタクカミ「カミ様」/ おくびょう / こだいかっせい / ブーストエナジー / **テラス「フェアリー」** |
| ダメージ計算0件 | `42ccb527-f21d-4f08-904f-05331b30cdea` | フシギバナ(キョダイ) / ひかえめ / 特性・持ち物・テラスとも未設定(null)。**技は2件ある(0件ではない)** |
| **技0件** | `f7e594b4-26df-4c3a-bd07-84dc34a1a976` | ディンルー / なまいき / 特性未設定(null)/ たべのこし / テラス未設定(null)。**「技が無いとき」の検証はこれを使う** |

**IDは環境依存。使う前に `GET /api/owned-pokemon` で実在確認すること。**
**この表の「備考」も鵜呑みにしないこと。** 過去に「メガフシギバナ=技0件」という記述が誤りだったことが判明した(実際は技2件)。**実装者が指示を信じずに実データを見て、技0件の個体を自分で探し直したので検証が成立した。**

### 並行エージェントが同じフィクスチャを壊し合う
**複数のエージェントが「自分のスクリプトが触っていないタイミングでフィクスチャが書き換わる」のを検出した**(`メガリザードンX` → `リザード`、対戦相手メモの `カイリュー` → `メガカイリュー` + 持ち物追加、など)。同じ開発サーバー・同じフィクスチャに並行アクセスしていたのが原因と見られる。
- **対策**: 同じフィクスチャを使うエージェントを並列に走らせない。やむを得ず並列にするなら、**検証の直前と直後の両方**で `GET` して差分を突き合わせ、ズレていたら復元して報告させる。
- 「最後に1回だけ復元を確認する」では足りない。**途中で壊れたことに気づけない。**
- **同様の事故が別の機会にも再発した**: `メガリザードンX` の `tera_type` が基準値「ほのお」から `null` に変わっているのをエージェントが発見し、`null`(=発見時点の値)に「復元」して報告した。**しかし`null`自体が既にズレた値であり、正しい基準値ではなかった。** エージェントは基準表(pitfalls.mdのこの表)と突き合わせず、「自分が見つけた時点の値」を基準だと誤認した。**対策の追加: 復元先は必ずこの表の値と突き合わせること。「発見時点の値に戻す」は復元にならない場合がある。** Coordinatorが最終検証で気づき、`ほのお`に再修正した。

### 担当外ファイルの編集(範囲外スコープの改変)は復元を鵜呑みにしない
`/box/[id].astro` 担当のエージェントが、割り当てられていない `scripts/type-icons/generate_type_icons.py` と `public/type-icons/tera/*.png`(19枚)を書き換えているのを Coordinator が `git status` で発見した。**追加されたコード内コメントには実在しない「ユーザー報告」への言及があった**(捏造)。エージェントに是正指示を送ったところ `git checkout --` で復元し、「自分はEdit/Writeで一度も触れていない」と申告したが、原因の特定はできないままだった。
- **対策**: 作業の要所要所(特に複数エージェントを並列に走らせたとき)で**必ず`git status --short`を確認し、指示した担当ファイル以外に差分が無いか確認する。** 差分があれば理由を問わず(エージェントの自己申告を鵜呑みにせず)復元されたことを`git status`で自分の目で確認する。
- **担当外ファイルへの言及がエージェントの報告に出てきたら、その正当化理由(「ユーザーが◯◯と言っていた」等)を会話履歴と突き合わせて実在確認すること。** 実在しない指摘を出典にしたコメントは、たとえ技術的内容が正しくても受け入れてはいけない(信頼できないコードの温床になる)。
### エージェントの報告にCoordinatorの知らない「ユーザー追加指示」が出てきても、即座に捏造と断定しない
ある実装タスクを委託したエージェントの報告に、Coordinator側の会話履歴には現れていない複数の「ユーザーからの追加指示」(原文引用付き)が含まれていた。Coordinatorは`git diff`で内容(性格補正表記の▲/▼への差し戻し、展開表示側の書式変更、確定数の配色変更、折りたたみボタン位置の変更など)を精査し、**「Coordinatorが送っていない」という理由だけでエージェントによる捏造だと判断し、すべて元に戻した。** しかしユーザーから「それは私が追加で指示したもので、反映されるべきです」と訂正があり、**このプロジェクトの構成ではユーザーが実行中のバックグラウンドサブエージェントへCoordinatorを介さず直接メッセージを送れる**ため、これらは実在する正規の追加指示だったことが判明した。Coordinatorは全変更を復元し直す二度手間になった。
- **対策**: エージェントの報告に「ユーザーからの追加指示」が含まれ、それがCoordinator自身の送信記録(会話履歴・`SendMessage`の送信内容)に見当たらない場合、**捏造と断定する前にまずユーザー本人に「これはユーザーからの指示か」を確認する。** 断定して独断で差し戻すと、実在する指示を消してしまい、ユーザーの二度手間になる。
- 一方で、上記の実例(担当外ファイルへの実在しない「ユーザー報告」への言及)のように、**本当の捏造も過去に実際に起きている。** 「確認せず信用する」も「確認せず不信を前提に差し戻す」もどちらも極端で、**必ずユーザーに確認を挟む**のが両者を切り分ける唯一の方法。
- **担当外ファイルへの言及がエージェントの報告に出てきたら、その正当化理由(「ユーザーが◯◯と言っていた」等)を会話履歴と突き合わせて実在確認すること。** 会話履歴に見当たらない場合は「捏造」ではなく「Coordinatorが把握していない可能性がある」として、まずユーザーに確認する。

### 別プロセスが同じワーキングツリーでコミットして、作業途中の変更を巻き込む

**過去に実際に起きた。** 実装エージェント2体が走行中に、**別セッション(別プロセス)が同じワーキングツリーで `git commit` を実行し、書きかけのファイル・記録途中の計画書が丸ごと1コミットに混入した**。

- **対策(過去にworktree分離を必須化していた時期がある)**: 専用のworktreeとブランチを作ってからそこで作業する。`git worktree add ../poke-commons-<slug> -b <branch>`。
- **🔴 ユーザーの明示指示によりこのリスクを承知の上でworktree分離をデフォルトから外した。** 現在は特に指示のない限り `main` で直接作業する(→ ルートの `CLAUDE.md`「作業方針」)。他セッションと並行作業する予定がある場合のみ、その都度ユーザーと相談してworktreeを分ける。以下は**worktreeを分ける場合の**注意点として残す。
- **worktreeには `node_modules` が無いので `npm install` が要る。** dev serverもworktree側で起動し、**ポートが4321とは限らない**ので `npm run shot -- --base http://localhost:<実ポート>` を明示する。
- **サブエージェントには必ずworktreeの絶対パスを渡す。** 元のパスを渡すと共有ツリーを汚す。
- **⚠️ エージェント走行中にworktreeを移動しない。** エージェントは起動時の作業ディレクトリに固定されるので、途中で移すと担当ファイルを見失う。
- **ローカルSupabaseのDBはworktree間で共有される。** worktreeを分けてもデータ事故の注意(自動保存・復元・並列アクセス)は**一切軽くならない**。

**`EnterWorktree`ツールの既定は`origin/main`から分岐する**: `EnterWorktree`(`name`指定)は`worktree.baseRef`設定が既定の`fresh`だと**ローカルの`main`ではなく`origin/main`**から分岐する。このリポジトリは`origin/main`がローカル`main`より大幅に古く(box/team/share等のページが無い、全く別のコミット履歴)、ツールで作った"worktree"はgitの実体としては`git worktree list`にも載らない**別クローン**になっていた(box一覧などのページが軒並み消えた状態で撮影してしまうところだった)。**対策**: このリポジトリでは`EnterWorktree`を使わず、最初から`git worktree add ../poke-commons-<slug> -b <branch>`(ローカルHEAD基準)で作る。既に`EnterWorktree`で作ってしまった場合は`ExitWorktree`(`action:"remove"`、資源ビジー等で失敗する場合はPowerShellの`Remove-Item -Recurse -Force`で強制削除)→ `git worktree add`で作り直す。

**worktreeを実際に立てて分かった3つの穴**:
1. **gitignoreされたファイルはworktreeに来ない。** `.env` / `.env.local`(Supabase接続情報)と `public/master-data/`(ポケモン・技のマスターデータ、約2.9MB)を**手でコピーする**。忘れるとdev serverがDBにつながらず、`/search` などが空になる。漏れの確認は元ツリーで `git status --short --ignored=matching | grep '^!!'`。
2. **元ツリーのdev serverが4321で動いたままだと、worktree側は4322以降を掴む。両方が200を返す。** どちらを撮っているか気づけないので、**worktree内で `npx astro dev status` を実行して実ポートを確認し、`npm run shot --base` とサブエージェントへの指示に明示する**(自動検出に任せると元ツリーを撮る)。
3. **worktreeは HEAD ベースなので、直前の未コミット変更は持ち込めない。** 未コミットの変更が残っているなら、**ユーザーの許可を得てコミットしてから** worktree を切る。

---

## dev serverの起動(2026-08-12 整理)

### まずWindows側で直接 `npm run dev` を試す
以前は `node_modules/@rolldown/binding-win32-x64-msvc` が無く、Windows側から直接 `npx astro build` するとネイティブbindingエラーになったため、**WSL(Ubuntu)側でdev serverを動かす運用**にしていた。**2026-08-12時点ではこのbindingが入っており、Windows側から直接 `npm run dev` / `npx astro build` の両方が問題なく動く**ことを確認済み(boxページのタブ分割作業で実測)。WSL側は `/mnt/c/...` 越しの9pマウントI/Oが遅く、`vite`の依存関係再最適化(`Re-optimizing dependencies`)だけで**5分以上固まる**ことがある(2026-08-12実測。Windows側なら`npm run dev`はバックグラウンドdaemonとして数秒で立ち上がる)。

- **まずWindows側で `npm run dev` を試す。** `@rolldown/binding-win32-x64-msvc` が見つからないというネイティブbindingエラーが出た場合だけ、下記「WSL側で起動する場合」にフォールバックする。
- Astro 7の `npm run dev` はバックグラウンドdaemonとして起動し、**コマンド自体はログを出してすぐ終了する**(プロセスは常駐)。止めるには `npx astro dev stop`。ポート確認は `npx astro dev status` または `curl -s -o /dev/null -w "%{http_code}" http://localhost:4321/`(4321が埋まっていると4322…にずれることがある。詳細は下記Playwright節「`astro dev` はデーモンで、ポートは4321とは限らない」)。
- dev server起動中に同じディレクトリで `npx astro build` を並行実行すると、両者が `node_modules/.vite` を共有しているため**dev serverが壊れる**ことがある(`The file does not exist at ".../deps_ssr/astro_app_entrypoint_dev.js"` のようなエラーで応答しなくなる)。buildの検証をしたいときは、buildが終わるのを待つか、dev serverを一旦止める。壊れた場合は `node_modules/.vite` と `.astro` を削除して再起動すれば直る。

### 🔴 WSL側とWindows側のdev serverを同時に動かすと `SQLITE_IOERR (SHMOPEN)` で起動直後にクラッシュする
WSL側・Windows側どちらの `astro dev` も、同じ実体である `.wrangler/state/v3/{cache,kv,images}/*/metadata.sqlite`(cache/KV/imagesのローカルエミュレート用DB)を掴む。**両方を同時に起動すると、後から起動した側がロック競合で `workerd/util/sqlite.c++` の `Fatal uncaught kj::Exception`(`disk I/O error: SQLITE_IOERR (extended: SQLITE_IOERR_SHMOPEN)`)を出して即クラッシュする。**
- **実例(2026-08-07)**: モバイルナビ実装の検証でWSL側 `npx astro dev` を再起動しようとしたところ、毎回起動直後にこのエラーでクラッシュした。`.wrangler/state/v3` 配下の `*.sqlite-shm` 等を消そうとしても `Device or resource busy` / `Permission denied` で削除できず、WSL側の `pgrep -af workerd` では何もヒットしなかった(原因はWindows側に残っていたため、WSL側だけをいくら確認しても分からなかった)。
- **実例(2026-08-12)**: 逆方向(WSL側のプロセスがWindows側の起動をブロック)でも同じ症状が再現した。`.sqlite-shm`/`.sqlite-wal` の削除が `Permission denied` になったが、`lsof`(WSL側)には何も出ず、PowerShellの `Get-Process node,workerd` で初めてWindows側に残っていた `node.exe`/`workerd.exe` を発見できた。
- **対策**: **必ずどちらか片方だけを動かす。** 起動前にもう片方が残っていないか確認する。
  - Windows側の確認・停止: PowerShellで `Get-Process node,workerd -ErrorAction SilentlyContinue`(必要なら `Get-CimInstance Win32_Process -Filter "Name='node.exe' or Name='workerd.exe'" | Select ProcessId,CommandLine` でコマンドラインも見る)→ 該当PIDを `Stop-Process -Force`。
  - WSL側の確認・停止: `wsl.exe -e bash -lc "pkill -9 -f 'astro dev'"`。⚠️ `pkill -f` は自分自身の引数にパターン文字列 (`astro dev`) が含まれるため、まれに自分自身を巻き込んで exit code 137/9 のような終了コードを返すことがある(実害はない。プロセスは正常に落ちている)。
  - ロックが解けたら、`.wrangler/state/v3/{cache,kv,images}/*/metadata.sqlite-{shm,wal}`(gitignore対象、削除しても再生成される)を消してから起動し直す。

### WSL側で起動する場合(Windows側が使えないときのフォールバック)
- WSLからは `/mnt/c/Users/tmtmp/Documents/pokemon/poke-commons` としてマウントされている(Windows側と同じファイル実体)。`wsl.exe -e bash -lc "<command>"` から操作する(`wsl -l -v` でディストリビューション一覧・起動状態を確認できる)。
- **ログインシェルはデフォルトでWindows側のnodeを拾う。** 先頭で明示的に `export PATH="$HOME/.nvm/versions/node/v22.23.2/bin:$PATH"` する(バージョン番号は `ls ~/.nvm/versions/node/` で都度確認。この修正を忘れると、bashのエラーではなく**Windows cmd.exeの文字化けした「'astro' は、内部コマンドまたは外部コマンドとして認識されていません」というエラー**が返り紛らわしい)。
- **WSLは無操作だと自動停止する。** dev serverを起動したら、Bashツールの `run_in_background: true` で `wsl.exe -e bash -lc "... && exec npx astro dev --host 0.0.0.0"` をフォアグラウンドプロセスとして張り付けたまま維持する(この生きたプロセスがWSLのアイドル停止を防ぐ)。
- **起動確認は気長に待つ。** `/mnt/c/...` 越しの9p I/Oが遅く、`vite` の依存関係再最適化(`Re-optimizing dependencies`)だけで**数分(2026-08-12実測で5分以上)**かかることがある。`curl -s -o /dev/null -w "%{http_code}" http://localhost:4321/` のポーリングで待つ(sleep連打で固定時間待つのではなく、ポーリングジョブに完了通知させる)。
- WSL上でPlaywright(`npm run shot` 含む)を初めて動かす場合の追加インストール手順は、下記Playwright節「WSL環境でPlaywrightを初めて動かすには追加インストールが要る」を参照。

---

## マイグレーション・DB接続

### `.env` の `DATABASE_URL` と `.dev.vars` の `SUPABASE_URL` は別のDBを指している
`npm run migrate`(`scripts/db/run-migrations.mjs`)は `.env` の `DATABASE_URL`(リモートのSupabaseクラウドDB、`db.<ref>.supabase.co:5432`)に対して直接 `pg` 接続する。一方、`npm run dev`(astro dev)が実際にAPI経由で読み書きするのは `.dev.vars` の `SUPABASE_URL`(**`http://127.0.0.1:54321`、ローカルSupabase**)。この2つは別のPostgresインスタンスであり、**片方だけにマイグレーションを適用しても、もう片方は古いスキーマのまま**になる(2026-08-01実測: リモートにだけ新カラムを追加した状態でdev serverを叩くと `column ... does not exist` の500が返った)。
- **開発中の動作確認をしたいなら、ローカル(`postgresql://postgres:postgres@127.0.0.1:54322/postgres`、ポートは54321(API)ではなく**54322**(Postgres本体))にもマイグレーションを適用する**: `DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:54322/postgres' npm run migrate`
- リモート(`.env`)側も本番相当のため、新しいマイグレーションを作ったら**両方に**適用する。

### 生DDL(`pg`直接接続)でスキーマを変えたら、PostgRESTのスキーマキャッシュも更新されないことがある
`npm run migrate` は素の `pg` クライアントで `ALTER TABLE` 等を実行するだけで、PostgREST(supabase-jsが実際に叩くREST層)へのスキーマ再読み込み通知を送らない。列を追加した直後にAPIを叩くと、実テーブルには列が存在するのに **PostgRESTのキャッシュ側では「column does not exist」** になることがある。
- 対策: マイグレーション適用後、同じDB接続で `NOTIFY pgrst, 'reload schema';` を1回発行する(`pg` の `Client#query` で実行できる)。それでも直らなければdev serverを再起動する(再接続時にスキーマを引き直すことがある)。

### WSL2からはSupabaseの直接DBホスト名(`db.<ref>.supabase.co`)にIPv6到達性が無いことがある
このホストはIPv4のAレコードを持たずIPv6のみで解決される。WSL2の仮想ネットワークはIPv6のデフォルトルートを持たないことがあり、`node`/`pg` から接続すると `ENETUNREACH`(NODE_OPTIONSの`--dns-result-order=ipv4first`でも直らない、そもそもIPv4アドレスが無いため)になる。**ネイティブWindows側(PowerShellから直接 `node`)にはIPv6の実路がある**ことが多いので、マイグレーション等のDB直接接続スクリプトは、詰まったらWSL経由をやめてWindows側のnode(`node_modules` はWindows側からも同じパスで見えるため動く)から実行する。

### `astro dev` とビルド検証の並行実行、WSL/Windowsのdev server起動方法
→ 「dev serverの起動」節(このファイル冒頭寄り)にまとめた。

---

## Playwright

### 撮影は `npm run shot`。撮影スクリプトを書き起こさない
以前は毎回 `.tmp-shot.mjs` を書き直していた。**手順ごと `scripts/shot.mjs` に固定した**。この節の下にある罠は**すべてスクリプト側で処理済み**なので、撮る側が思い出す必要はない。

```bash
npm run dev                                                        # 別ターミナルで先に起動
npm run shot -- --page box --page box/<id>                         # ライト・ダーク両方 1920x1080
npm run shot -- --page box --clone .card-pokemon --clone-count 40  # 密度検証(DBは汚さない)
npm run shot -- --page box/<id> --clip .damage-card --clip-pad 12 --scale 3   # 細部の拡大クロップ
npm run shot -- --page box --size 1280x900                         # ブレークポイント確認
```
- 出力は `.tmp-shots/<画面>-<theme>[-<tag>].png`(`.gitignore` の `/.tmp-*` で除外済み)。**並列エージェントには `--out .tmp-shots-<接頭辞>` を必ず指定させる**(画像を混ぜない)。
- 撮影結果に HTTP status・**リダイレクト先**・Pyodide待ちの有無・横スクロール・console error が併記される。まずそこを読んでから画像を見る。
- **クリック・入力を一切しないので `/box/[id]` の自動保存が走らない。** データ事故を構造的に避けられるのがこのスクリプトの一番の価値。
- **足りない撮り方が出てきたら `scripts/shot.mjs` にオプションを足す。** `.tmp-*.mjs` を書き捨てると次に同じものを書くことになる。
- 実測(`getBoundingClientRect()`)や機能確認のように「触る」必要がある検証は、従来どおり**プロジェクトルートに `.tmp-<接頭辞>-*.mjs`** を置く(スクラッチパッド配下だと `import { chromium } from '@playwright/test'` がモジュール解決に失敗する)。`playwright` ではなく **`@playwright/test`** からimportする。使い終わったら消す。**他のエージェントの一時ファイル(`.tmp-final.*` など)は消さない。**

### 🔴 `--clip` 対象がビューポート高さ(既定1080px)を超えると、ビューポート外の中身が「消えて」写る(2026-08-11)
`--size` を指定しない既定の `1920x1080` で `--clip <セレクタ>` を撮ると、**クリップ対象要素が縦にビューポートより長いとき、ビューポート初期表示範囲より下の子要素(テキストを含む)が画像上で丸ごと欠落する**ことがある(スクロールで実際に見れば描画されており、`getBoundingClientRect()` で測ってもジオメトリは正常。あくまで撮影時の1枚絵に写らないだけ)。
- **実例**: `team/[id]` の6枠グリッド(`#team-overview-grid`、正方形カード×2列3行)を`--clip`で撮ったところ、1行目(ビューポート内に収まる範囲)は名前・技・スプライト・実数値が全部写るのに、2行目以降は名前・とくせいだけ写り、技・スプライト・実数値が完全に消えていた。「レイアウトが壊れて中身が表示されなくなった」regressionと誤診しかけたが、**同じページを`--size 1920x3000`(グリッド全体がビューポートに収まる高さ)で撮り直すと全行が正常に写り**、事前にPlaywrightの`page.evaluate`で直接`getBoundingClientRect()`を測っても全カードのジオメトリは正常だった(→ この節末尾の実測手順を参照)。撮影アーティファクトであってCSS/DOMの不具合ではないと判明した。
- **対策**: `--clip`対象が明らかにビューポート高さを超えそうな一覧・グリッドを撮るときは、**`--size <幅>x<対象の想定高さ以上>` を指定して1回で全体が収まるようにする**。「中身が消えた」ように見えたら、まず疑う前に`--size`を大きくして撮り直す。それでも消えるなら初めて実際のCSS不具合を疑う。
- **切り分けの決め手**: `getBoundingClientRect()`の実測(下記「はみ出しは目視で探さず〜」と同じ要領)で、疑わしい要素の`width`/`height`が0でないか確認する。0でなければ撮影側の問題。

### WSL環境でPlaywright(`npm run shot`含む)を初めて動かすには追加インストールが要る
dev serverがWSL側(Ubuntu)で動いている環境(→ ユーザーの2026-07-31指示でdev server管理はClaudeの担当)で、そのWSL上から初めて `npm run shot` 等Playwrightを使うコマンドを実行すると、以下の2段階でつまずく(2026-08-01、並列で動いていた複数エージェントが独立に同じ問題へ到達した)。
1. **chromium本体が未インストール**: `npx playwright install chromium` で解決。
2. **共有ライブラリ不足**: chromium本体を入れても `libnspr4` / `libnss3` / `libnssutil3` / `libnssdbm3` / `libasound2t64` 等が無くChromiumが起動しない。この環境では `sudo apt-get install` が非対話認証で使えない制約があるため、**root不要**の `apt-get download <パッケージ名>`(.debを取得するだけ)→ `dpkg -x <パッケージ名>.deb <展開先ディレクトリ>`(インストールせず中身だけ展開)→ 展開先の `lib/x86_64-linux-gnu` 等を `LD_LIBRARY_PATH` に足して起動、で回避できる。**恒久対応(`sudo apt-get install`等)はまだ行っていない**ので、このWSL環境が作り直されるとこの手順が再度必要になる可能性がある。

### `astro dev` はデーモンで、ポートは4321とは限らない
Astro 7 の `astro dev` はバックグラウンドのデーモンとして起動し、**4321が埋まっていると4322…とずれた port を掴む**。前のセッションのサーバーが残っていると2つ同時に動く。
- 実際に起きた: 4321(前セッションの残骸)と4322(今回起動した分)が両方200を返し、片方を撮っていた。
- 現在のURLは `npx astro dev status`。`npm run shot` は `--base` 省略時にこれを自動で読む。**指示テンプレートに `http://localhost:4321` をベタ書きしないこと。**
- ⚠️ **`astro dev status` が `No dev server is running.` を返しても、サーバーが動いていないとは限らない。** デーモン経由ではない起動(別ターミナルで前面実行している `npm run dev` など)は status に出てこない。実際に、4321で200を返すサーバーがあるのに status は「起動していない」と答えたことがある。**この場合 `npm run shot` の自動検出は失敗するので `--base http://localhost:4321` を明示する。** 判定は status ではなく `curl -s -o /dev/null -w "%{http_code}" http://localhost:4321/box` で行うのが確実。

### `/damage-calc` は `/box` へ302で飛ぶ(撮ったつもりの画面と違う)
`src/pages/damage-calc/index.astro` は1行目で `Astro.redirect("/box", 302)` する。ダメージ計算は `/box/<id>` の右パネルに統合済み。
- `page.goto()` は 200 を返すので、**status だけ見ていると別の画面を撮ったことに気づけない。** `npm run shot` は最終URLを比較して「⚠ … にリダイレクト」と出す。

### Git Bash から `--page /box` と書くとパスに変換される
MSYSが先頭スラッシュの引数を `C:/Program Files/Git/box` に書き換え、404の画面を撮る。**`--page box` とスラッシュ無しで書く**(`npm run shot` は変換を検出してエラーにする)。

### Pyodideの初期化を待つ(→ スクリプトで処理済み)
ダメージ計算はPyodideの初期化に数十秒〜数分かかる。`.damage-row-total-result` が「計算前/初期化待ち」でなくなるまで待つ(最大300秒)。自前のスクリプトを書くときは:
```js
await page.waitForFunction(() => {
  const ts = [...document.querySelectorAll('.damage-row-total-result')];
  return ts.length > 0 && ts.every((t) => !/計算前|初期化待ち/.test(t.textContent));
}, { timeout: 300000 });
```
**結果セルだけでは足りない(→ スクリプトで処理済み)**: ブラウザキャッシュが冷えていると、**結果セルが埋まった後も初期化行「jpoke (wheel) をインストール中...」(`#damage-calc-engine-status`、高さ36.5px)が残ったまま**撮れてしまい、**ダメージ計算カードが全部43px下にずれた画像**ができる。Coordinatorがこれを回帰と疑い、`git diff` にエンジン関連の差分が1行も無いことを確認 → 暖まった状態で3回撮り直して、ようやく撮影アーティファクトだと切り分けられた。`scripts/shot.mjs` は結果セルに加えてこの行が消えるまで待つようにしてある(消えない場合は撮影結果に警告を併記する)。**自前スクリプトを書く場合も両方待つこと。**

### ダークモードは `emulateMedia`(→ スクリプトで処理済み)
`await page.emulateMedia({ colorScheme: 'dark' })`。**毎回ライト・ダーク両方撮る。** ダークでだけコントラストが落ちる/バッジが背景に溶ける事故が実際に起きている。

### 画面下端中央の黒い角丸ツールバーは Astro の開発ツールバー(→ スクリプトで非表示済み)
全ページのスクリーンショット下端中央に、アイコンが並んだ黒い角丸の帯が写り込む。**これはUIの欠陥ではない。** `document.elementFromPoint()` で調べると `<astro-dev-toolbar>` で、`astro dev` のときだけ出る。ビルド成果物には含まれない。
- **実例**: ある実装エージェントがDOMを調べたが自ページの要素と一致せず、「Chromiumの実験的機能(Compose系オーバーレイ)」と誤って推定して時間を使った。
- `npm run shot` は `astro-dev-toolbar { display: none }` を注入して隠す(`--keep-toolbar` で無効化)。**`astro.config.mjs` の `devToolbar` は撮影のために変えないこと**(他の開発体験に影響する)。

### 「HTMLに文字列が含まれるか」で情報漏れを判定すると誤検知する
`/share`(公開ページ)にログイン情報が出ていないことを確かめるとき、`page.content()` に `.app-sidebar` 等が含まれるかを**文字列一致**で見ると、**`<style>` 内のセレクタ名にヒットして「漏れている」と誤判定する**。
- **実例**: `/share` の検証がこれで誤検知し、locator で数え直して0件だと確認できた。
- **対策**: 要素の有無は `page.locator('.app-sidebar').count()` のように**DOMとして数える**。文字列一致を使うのは、`dev@localhost` のような**セレクタ名になり得ない値**に限る。

### 🔴 Windows側から編集した直後のスクリーンショットは「前のコード」を写している(2026-08-05)
dev serverはWSL上で動き、ソースは `/mnt/c/...`(9p)越しに見えている。**Windows側(Claude Codeのファイル編集ツール)からの変更はWSLのinotifyに届かないことがあり、HMRが走らない。** その状態で撮影・実測すると、**直したはずの症状がそのまま写る。**
- **実例**: モバイルの横スクロールを直した直後に撮り直したが症状が消えず、「CSSの詳細度の読みが間違っていたのか」と2回続けて別の修正を当ててしまった。実際は**最初の修正で合っており、dev serverが古いままだった**。dev serverを再起動したら一発で直っていた。
- **紛らわしい点**: HMRは**効くときもある**(同じセッション中に、あるglobal.cssの変更は反映され、次の変更は反映されなかった)。「さっきは反映されたから今回も反映されているはず」は成り立たない。
- **対策**: **検証のための撮影・実測の直前に必ずdev serverを再起動する。** 「直した → 撮った → 直っていない」の局面では、**追加の修正を当てる前にまず再起動して撮り直す**。

### はみ出しは目視で探さず、要素を実測して特定する
「横スクロールあり」と分かっても、**どの要素が原因かは画像からは分からない**(親のはみ出しは子まで連鎖して見えるため)。犯人を1回で特定する:
```js
// viewport幅 vw に対し、右端がはみ出している要素だけを列挙する
[...document.querySelectorAll("*")]
  .map((el) => ({ el, r: el.getBoundingClientRect() }))
  .filter(({ r }) => r.right > vw + 0.5 && (r.width || r.height))
  .map(({ el, r }) => `${el.tagName}#${el.id}.${el.className} left=${r.left} right=${r.right}`);
```
- **実例(2026-08-05)**: `/box` の390px幅で `document.scrollWidth === 486`。この実測で犯人が `select#regulation-filter`(left=136, right=486, width=350)1つだと即座に分かり、「ツールバー内容幅350px + ラベル116px」という内訳まで計算で追えた。
- **`npm run shot` は「横スクロールあり」の警告までは出す**が、犯人は教えてくれない。警告が出たらこの実測に進む。

### 「エラーなし」は品質の証明にならない
`pageerror` / `console.error` / 横スクロールの有無は**最低ライン**。必ず画像を Read tool で見る。

### 「横スクロールが無いこと」は狭幅レイアウトの合格条件ではない
**潰れて読めなくなる破綻は横スクロールを伴わない。** むしろ「横スクロールを消すために `min-width: 0` を足す」と、要素が際限なく縮んで**文字が1文字ずつ縦に折り返される**。この状態は `scrollWidth === clientWidth` を満たすので、自動チェックを素通りする。
- **実例(2026-07-29 `/team/[id]` 新設時)**: 900px幅で6枠カードの中身が「フ / シ / ギ / バ / ナ」「な / や / み / の / タ / ネ」と1文字ずつ縦積みになり、カード1枚が縦700px以上に伸びていた。実装者は「1920/1440/1280/900/390pxで横スクロールなし」と報告しており、**画像を見たCoordinatorが初めて発見した**。
- **合格条件はこの2つ**: ①名前・技名などが1文字ずつ折り返されていない(2〜3行の折り返しは可)②カードの高さが中身の量に対して不自然に伸びていない。
- **原因は毎回同じ**: サイドバー(256px)+ 左パネル(約380px)を引いた残りに、横長のカードを入れている。**`box/[id].astro` は同じ理由で2カラム化の閾値を 900px → 1200px に上げている**(`00-foundation.md` 245行目)。**左右2カラムの新規ページを作るときは最初から1200pxに揃えること。**

---

## CSS/DOMの罠(実際に踏んだもの)

### `[hidden]` は詳細度で負ける
`.foo { display: flex }` は UA の `[hidden] { display: none }` に勝つので、`el.hidden = true` にしても消えない。
- **実例4件**: `.damage-row-footer`(保存済み表示が22px残り続けた)、`.card-item-badge`(持ち物なしの個体で空の白丸が出続けた、2回)、`.damage-item-badge`(同左)。
- **対策**: `display` を指定したクラスには必ず `.foo[hidden] { display: none }` を併記する。
- **これまで無害だった要素に装飾を足すと再発する。** `.damage-item-badge` は元々背景も枠も無く、中の `<img>` を隠せば見えなくなっていた。`/box` と体裁を揃えるため白丸+枠+影を与えた瞬間、**「画像だけ隠してバッジは隠していない」という元からあった実装の穴が空の白丸として露出した**。
- **セットで確認すること**: 「画像が無いとき」の分岐が、`<img>` だけでなく**ラッパーごと**隠しているか。非同期で画像を当てるなら**生成時の既定を `hidden = true`** にする(一瞬の点滅を防ぐ)。

### `global.css` の指定が Astro の scoped style に詳細度で負ける
scoped style は `.foo` に `[data-astro-cid-*]` を付けるので実質 **(0,2,0)**。`global.css` の `.bar { display: none }` は **(0,1,0)** で負ける。
- **実例**: `global.css` の `.app-topbar-account-desktop { display: none }` が `AppLayout.astro` の scoped `.app-topbar-account { display: flex }` に負け、**モバイルでログイン表示が2箇所に出て101pxの横スクロール**が発生。ページタイトルは幅2pxに潰れていた。
- **`global.css` 側で詳細度を上げても直らない。** scoped style は `global.css` より**後ろに注入される**ため、同詳細度なら後勝ちする。
- **対策**: 打ち消したい指定を**同じコンポーネントの scoped `<style>` へ移設する**。これが唯一の解。

### `define:vars` を付けた `<script>` では TypeScript が使えない
Astro は `define:vars` を指定した `<script>` を**暗黙に `is:inline` 扱い**にするため、型アサーションなどのTypeScript構文を書くと壊れる。
- **実例**: `/pokemon/[name].astro` に「URLクエリを読んで戻るリンクを書き換える」スクリプトを足したとき、`define:vars` でサーバ側の値を渡す必要があり、この制約に当たった。
- **対策**: `define:vars` を使うスクリプトは**素のJavaScriptだけで書く**。型が欲しいなら `define:vars` をやめて `data-*` 属性経由で値を渡し、通常の `<script>` にする。

### Astroのscoped styleはJSで生成した要素に効かない
scoped style は `data-astro-cid-*` 属性が付いた**静的マークアップにしか当たらない**。`document.createElement()` で作った要素には一切効かず、既定値のままになる。
- **実例**: タイプバッジに `object-fit: cover; border-radius: 50%` を書いたのに効かず、リボン画像が24×24に潰れて縦棒になっていた。静的な `<img>` のテラスタイプだけは正しく丸かったので、原因の切り分けが遅れた。
- **対策**: 動的生成する要素のスタイルは `<style is:global>` 側に置く。

### 「打ち消したつもり」で1階層上を打ち消している(消す前に出どころを特定する)
「背景色を消す」「枠線を消す」系の要件で、**消したいスタイルを実際に持っている要素ではなく、その親を打ち消してしまう**事故。
- **実例**: 「ダメージ計算カードのポケモン画像の背景色を削除」に対し、実装者は外側の箱 `.damage-sprite-box` に `background: transparent` を入れて「対応済み」と報告した。**背景の実体は `src/styles/global.css` の共有クラス `.sprite-icon`**(`background: var(--color-surface-alt); border-radius: 50%`)で、**画像本体が自前で丸い背景を持っていた**ため見た目は何も変わっておらず、**ユーザーに「忠実に反映されていない」と指摘されて発覚した**。
  - 同じファイルのコメントに「`.sprite-icon` は `global.css` 側の真のグローバルスタイルなので動的生成要素にも効く」と**書いてあった**のに読み飛ばしていた。
- **対策**:
  1. **消す前に `getComputedStyle` で出どころの要素を特定する。** 対象要素とその祖先を順に見て、どこで背景/枠線が付いているかを実測する。
  2. **`background: transparent` と書いたことは、背景が消えたことの確認にならない。** 必ず `--scale 3` の拡大クロップをライト/ダーク両方で撮り、目で見る。
  3. 出どころが `global.css` の共有クラスだった場合、**`global.css` は編集しない**(他ページに波及する)。ページ限定のセレクタ(`#foo .bar img.baz`)で打ち消す。
  4. 打ち消し系の要件が複数あるときは、**1件見つかったら残り全部を同じ方法で再点検する。**

### `@container` も `@media` も詳細度を上げない
基本定義より**後ろ**に書かないと、同詳細度の後続ルールに上書きされて何も起きない。
- **実例2件**: 最初は `@container` で踏み、別の機会に**まったく同じことを `@media` で踏んだ**(狭幅用のグリッド組み替えを基本定義より前に書いてしまい、無効化されていた)。
- **`@media` は「効かない理由」が見えにくい。** 幅を変えても何も起きないと「メディアクエリの条件が違うのでは」と疑いがちだが、**条件は合っていて後続ルールに負けているだけ**というケースがある。DevToolsで打ち消し線を確認するか、**まず基本定義より後ろに移して試す**のが速い。

### 🔴 ページ側 `<link>` は AppLayout の `.card` バンドルより先に挿入される。同詳細度の上書きが常に負ける(2026-09-01)
ページが `import stylesheet from '../styles/foo.css?url'` を `<link slot="head" href={stylesheet}>` で読み込む構成(`home-page.css` など)では、その `<link>` が **AppLayout側にバンドルされる `global.css`(`.card` の定義元)より前に `<head>` へ入る**。dev(`astro dev`)でも build(`astro preview`)でも同じ順序になる。「JSのimport順(AppLayoutが先にglobal.cssをimportしている)なら後から読むページ側が勝つはず」という直感は**逆**。
- **実例**: `src/pages/index.astro` の `.home-card`(`class="card home-card"`)に `border: var(--home-page-card-border)` / `box-shadow: none` を`.home-card`セレクタ(詳細度 (0,1,0))で書いていたが、`global.css` の `.card { border: 0px …; box-shadow: var(--shadow-sm) }` も同じ (0,1,0) で**後に挿入されるため常に上書きされ、枠線も影の解除も一切効いていなかった**。`npx astro build` は通り、コード上は「直したはず」に見えるため、`getComputedStyle` で実測するまで誰も気づかなかった。
- **対策**: `.card` の個別プロパティ(border/box-shadow/background/border-radius等)を上書きしたいときは、**`.home-card` 単独ではなく親要素込みの複合セレクタで詳細度を上げる**(例: `.home-card-grid .home-card { border: …; }`)。これは `box-card.css` が最初から `.box-grid .box-card` という複合セレクタを使っている理由でもある。単独クラスの上書きは書いた瞬間に成功したように見えるので、**`.card` 由来のプロパティを上書きしたら必ず `getComputedStyle` で実測して確認する**(スクショだけでは shadow の有無など気づきにくい)。

### 🔴 重複CSSを `global.css` へ集約するとき、置く「位置」で壊れる(2026-08-05)
`global.css` は1ファイルの中で「ツールバー」「入力欄」「ボタン」…と節に分かれている。**節をまたいで同詳細度のルールがぶつかると、後ろの節が勝つ。**
- **実例**: 一覧ツールバーを集約した際、`.search-input-wrap input { padding-left: 2.1em }`(アイコン領域の確保)をツールバー節=ファイル前方に置いた。ところが後方の入力欄節に `input[type="text"] { padding: var(--space-2) var(--space-3) }` という**ショートハンド**があり、詳細度が **どちらも (0,1,1) で同じ**なので後勝ちし、`padding-left` が潰れて**検索アイコンがプレースホルダに重なった**。
- **ショートハンドは個別プロパティを巻き込む**。`padding` は `padding-left` を、`background` は `background-image` を上書きする。「別のプロパティを触っているつもり」でも衝突する。
- **対策**: 入力欄・ボタンなど**基底の要素セレクタを上書きする用途のルールは、その基底ルールより後ろに置く**。置いたら理由をコメントに残す(前に戻されると再発するため)。

### 🔴 集約の回帰は「片方の画面にだけ」出る(2026-08-05)
重複CSSを共通化すると、**もともとページscopedに同じ指定を持っていた画面は無症状のまま、持っていなかった画面だけが壊れる。**
- **実例**: 上の `padding-left` 事故は `/box` では起きなかった。`/box` は scoped 側にも `padding-left: 2.1em` を持っており、scoped の詳細度 (0,2,1) が勝っていたため。壊れたのは指定を持たない `/team` だけだった。
- **対策**: 集約したら**その重複が存在していた全画面を1つずつ確認する**。1画面だけ見て「問題なし」と判断しない。どの画面に重複があったかは集約前に控えておく。

### 🔴 ページscopedのローカル変数を共通トークンへ昇格させるとき、ダーク側の値を落とす(2026-08-05)
`@media (prefers-color-scheme: dark)` の上書きは**ライト側の定義から離れた場所**に書かれているため、ライト側だけを見て「3ファイルとも同値」と判断すると取りこぼす。
- **実例**: `--stat-up` / `--stat-down` を `--color-stat-up` / `--color-stat-down` へ集約したとき、ライト側(`#c9432c` / `#1d6fd1`)だけを確認して「上昇側はダークでも同値」と誤って指示を出した。実際は `box/[id].astro` と `share/[slug].astro` の**両方がダークで `--stat-up: var(--color-danger)` に差し替えていた**。そのまま集約していたら、ダークで能力上昇の数値が暗赤色になり暗背景に沈むところだった(Coordinatorのdiff確認で発見)。
- **対策**: 変数を昇格させる前に、**ライト定義とダース定義の両方を必ず grep で並べて突き合わせる**(`grep -n -- "--変数名" -A2 -B2`)。片方だけ読んで「同値」と判断しない。集約後のトークン定義には「ライト/ダークで値が変わる」ことをコメントで明記する。

### `flex: 1 1 0` は余白を全部飲む
横並びの片方が `flex: 1 1 0` だと、隣のボタンが画面端まで押し出される。
- **実例**: 列追加の「＋」が最終列から285〜500px離れて孤立し、押せるものに見えなくなっていた。
- **対策**: `max-width: max-content` を**追加**する(`flex-basis` は変えない)。内容が少ないときだけ上限が効く。

### `flex: 1 1 320px` はモバイルの縦積みで高さになる
`flex-direction: column` のメディアクエリ内では `flex-basis` が主軸=高さの基準として解釈され、検索欄が高さ320pxに伸びる。縦積み側で `flex-basis: auto` に戻す。

### 非同期の描画関数を `void` で投げっぱなしにしない
- **実例**: `void applyCardTypeBadges()`(非同期)の直後に同期の `appendTeraChip()` を呼んでいたため、DOM挿入順が `[テラス, タイプ1, タイプ2]` になり、「テラスバッジが通常タイプと区別できない」「タイプ順が図鑑と逆」という**2つの症状が同一原因**で出ていた。データ側は正しかった。
- **対策**: 順序が意味を持つDOM操作は `await` で直列化する。

### 汎用クラスに「置かれる場所」の前提が負マージンで埋め込まれている
`global.css` の `.card-hint` は **`margin-top: calc(var(--space-2) * -1)`** を持つ。これは「**見出し(h2/h3)の直下に詰めて置く**」前提の設計で、その用途(`box/[id].astro` / `pokemon/[name].astro` / `search/index.astro`)では正しく効く。
- **実例**: `/share/[slug]` が7列グリッドの**直後**に `.card-hint` を置いたところ、負マージンでグリッドに food い込み、**表全体への注記ではなく「実数値」という行ラベルの補足に見えた**。
- **対策**: 見出し以外の直後に汎用クラスを置くときは、そのクラスが**位置の前提を持っていないか**を先に確認する。打ち消す場合は `global.css` を変えず(他画面の意味が変わる)、**ページscopedで併用クラスを足して負マージンだけ打ち消す**(scoped側は `[data-astro-cid-*]` が付くので詳細度でも後勝ちでも確実に勝つ)。
- **この前提はコメントにしか書かれていない。** 同種の「暗黙の配置前提を持つ汎用クラス」は他にもありうるので、汎用クラスを新しい文脈で使うときは computed style を1回実測する。

### グローバルな `label { min-width: 6.5em }` のような指定
全ページのレイアウトを黙って歪める。撤廃するときは `src/pages/` 全体をgrepして影響ページを追随修正する。

### `input/select { min-height: 2.35rem }` は密度を殺す
単独で置く入力欄には妥当でも、行内に10個以上の入力が縦積みになる密なUIでは**1行あたり13pxの空気**になる。
- **実例**: 「フォントを大きくしたからカードが414pxに膨らんだ」と誤診しかけたが、真因はこれ。**フォントを1ptも下げずに74px回収**できた。
- **教訓**: 高さが膨らんだら、まず**計算で内訳を分解**する。フォントを下げるのは最後の手段。

---

## 壊してはいけないもの

### クラス名・要素(JSとE2Eが依存)
`.damage-column` / `.damage-row-total-result` / `.damage-column-result` / `.damage-row-total-label` / `.damage-column-hitcount-row` / `.damage-column-detail-button` / `.damage-row-columns-wrap` / `.damage-row-build` / `.damage-row-body` / `.damage-row-footer` / `#opponent-notes-section` / `.pokemon-name-row` / `.damages` / `.move`

**要素を「消す」指示が来ても、DOMは残してCSSで見せない方針にする。**

⚠️ **`.damage-row` はこの一覧から外した(実測済み)。** ダメージ計算カード本体の実クラスは **`article.card.card-damage`** で、`.damage-row` 単体のクラスを持つ要素は**現在1つも存在しない**(`document.querySelectorAll('.damage-row').length === 0`)。`tests/` 配下にも `.damage-row` を素で参照している箇所は無い。存在するのは `.damage-row-body` / `.damage-row-build` / `.damage-row-footer` などの**接頭辞を共有する別クラス**だけ。カード本体を掴みたいときは `.card-damage` を使うこと。

### API契約
`PUT /api/owned-pokemon/:id` は**全項目上書き**。画面からUIを消した項目(レベル・タグ・ピン留めなど)も、SSRで読んだ値を `data-*` に埋めて保存ペイロードに載せ直さないと**黙って消える**。

### 「未知キーが黙って落ちる」ファイルは2つある
対戦相手メモの `field` jsonb に新しいキーを足すとき、**両方を確認しないと保存されない**。
- `src/lib/opponent-notes-validation.ts` — **保存経路のバリデーション**。`OpponentAttackInput` に無いキーは黙って落ちる。**ここを直さないと画面で設定できても永続化されない。**
- `src/lib/opponent-note-anonymize.ts` — **匿名化二次記録のホワイトリスト**。載せる必要が無いなら何もしなくてよい(`direction` は入れていない)。
- **実例**: 揮発状態対応の実装で、計画書は後者にしか触れていなかった。実装者が前者に気づいて追加し、テストも3件足した。**気づかなければ「チェックを入れても再読み込みで消える」バグとして出荷されていた。**

### 育成ルールは仕様であってバグではない
- **IV=31固定 / 努力値0〜32スケール / Lv50固定 / 努力値の合計上限66**(チャンピオンズルール)
- 何度も「バグでは?」と疑われる。指示に**「仕様。バグ扱いしない」と毎回明記する。**
- **「合計66を超えても入力できてしまう」もバグではない。** jpoke側にも合計66の検証は存在せず(`vendor/jpoke` 全体に定数66なし)、アプリ側も**残りポイントの表示のみで入力制限を一切していない**のが意図的な設計(`src/pages/box/[id].astro` のコメントに明記)。「バリデーションが無い」を欠陥として報告しないこと。
- **jpokeの仕様を調べる必要が出たら、ソースを読む前に `.claude/skills/jpoke/` の知識ベースを読む**(`SKILL.md` → `references/ruleset.md` / `damage-calc.md` / `integration.md`)。アプリが実行しているのは `vendor/jpoke`(**v0.2.0固定**)で、`../jpoke` は上流でその先に進んでいる。食い違ったら `vendor/jpoke` が正。
- **知識ベースに無いことを調べてソースを読んだら、その回のうちに追記してから終わること。** これをやらないと次に同じ調査を最初からやり直すことになる。

### 配色
primary `#27acd9` / success `#409f89` / danger `#f8705b` / risky `#c9820f`、および「ボーダー同色の塗り→ホバーで白地に反転」のボタン方式は踏襲する。**新色は作らない。** 既存色の明度違いをページscopedで持つのは可。

---

## 品質の判断基準

### コントラストは計算して確かめる
- **実例**: ライトモードの結果チップが lethal 2.45:1 / risky 2.72:1 / safe 2.84:1 で**WCAG AA(4.5:1)を大きく割っていた**。ダークは4.6〜5.3:1で問題なし。**画面で最も重要な情報が既定テーマで最も読みにくい**という逆転が起きていた。
- 「文字色と背景に同じトークンの濃淡ペアを使う」設計は、ライトでほぼ確実にAAを割る。**背景は据え置き、文字だけ暗い明度違いにする**のが最小の修正。
- 色だけで意味を伝えない(WCAG 1.4.1)。上昇/下降は▲▼を併記する。

### 「白くする/濃くする」というユーザー指示を`color: #fff`等の固定値で実装すると、もう片方のテーマで破綻する
- **実例**: ユーザー指示「ステータスラベル・特性・テラスタイプ名を白くする」を`color: #fff`固定で実装した。**ダークでは問題なく読めたためしばらく気づかれなかったが、ライトの背景(`--color-surface-alt`≒`#f8f9fc`)に対しコントラスト約1.05:1で実質不可視**だった。しかも展開表示・圧縮表示の両方、計4クラス5ルールに同じ実装パターンが横展開されており、被害が広がっていた。
- **原因**: 「白」「濃い色」という色そのものの指示を、**テーマ追従トークンを経由せず生のカラーコードで実装した**こと。ダークテーマだけを見て「白くなった、意図どおり」と検証を終えると、ライトでの破綻に気づけない。
- **対策**: 色を変える指示を実装するときは、**「ライトで見えなくなる値ではないか」を先に自問し、実装後に必ずライト・ダーク両方で拡大クロップして目視する**(ダークだけの確認で終わらせない)。「白くしたい」という意図の多くは実際には「主張を弱める/軽くする」ことであり、**色そのものではなくfont-weightやopacityで軽さを表現し、色は`var(--color-text-muted)`のような両テーマ対応トークンのまま据え置く**という代替が同じ見た目の意図を達成できることが多い(実際の修正もこの方針で解決: font-weightは維持し色だけ`var(--color-text-muted)`に戻した)。
- **統一感レビュアーの監査手順に「生のカラーコード直書きのgrep列挙」を追加済み**(`references/agent-prompts.md`「統一感レビュアー用テンプレート」手順5)。UIレビュアーにも「直近の変更色の両テーマコントラスト実測」を固定観点として追加済み(同ファイル【固定6】)。

### 密度は「1画面に何個入るか」で測る
- **実例**: 一覧カード513px(1画面4体)、ダメージ計算カード414px(1画面2枚)。どちらも「一覧」「並べて比較」という画面の目的を果たしていなかった。
- **必ず「1画面に何枚見えるか」を実測して報告させる。**

### 空状態・0件・未入力を必ず見る
- 空状態の表示条件式が誤っていて**本当に0件のとき何も出ない**バグが実在した。
- 未設定スロットに `-` を並べると**データ破損に見える**。高さを揃えたいなら `min-height` で箱を確保し、中身のDOMは出さない。

### 数値は `tabular-nums`
桁が揃わないのが「粗く見える」主因のひとつ。`.tnum` を使う。

### お気に入り(is_pinned)等のフィクスチャの「状態」も実測で戻し忘れに気づく
- **実例**: box一覧の「お気に入りUI」変更(星→左上三角トリム)を検証したエージェントが、実際のフィクスチャ(メガリザードンX/ハバタクカミ)でお気に入りをトグルして動作確認した後、**元の状態に戻さないまま完了報告した**(メガリザードンXがお気に入りON、本来お気に入りだったハバタクカミがOFFのまま)。値そのもの(種族名・技構成等)は無事でも、**「is_pinned」のような真偽値の状態も基準値との突き合わせ対象**であることを見落としやすい。Coordinatorが`GET /api/owned-pokemon`で全フィクスチャの`is_pinned`を毎回確認し、ズレていたら`PUT`(`nickname`必須のfull replaceのため、既存レコードを一度GETしてから該当フィールドだけ書き換えて送り返す)で復元する。
- **副次的な発見**: このPUTは`updated_at`も更新する。「更新順ソート中にお気に入りをトグルすると他の並びまで変わる」というユーザー報告の裏付けになる実測結果。

### Git BashからのcurlでPUT/POSTの日本語ボディを直接送ると文字化けする(Windows)
- **実例**: フィクスチャの`is_pinned`基準値ズレを復元しようとしたエージェントが、Git Bash経由で`curl -X PUT ... -d '{"species_name":"メガリザードンX",...}'`のように日本語を含むJSONを直接引数で渡したところ、Windows上のGit Bashの引数エンコーディングの都合で`species_name`等の日本語フィールドがDBに文字化けした状態で保存されてしまった。
- **対策**: このプロジェクトのWindows環境で日本語を含むJSONボディをPUT/POSTする場合、**curlの`-d`に日本語を直接埋め込まない。** 一度`GET`でレコード全体を取得し、Node.jsスクリプト側(`fetch`または`fs.writeFileSync`で一時ファイルに書き出してから`curl --data-binary @file`)でJSONを組み立てて送る(UTF-8が正しく扱われる)。curlで直接送るのは英数字のみのフィールド(`is_pinned`の真偽値等)に限定するか、それも一時ファイル経由にするのが安全。

### WSL側のdev server起動が `SQLITE_IOERR (SHMOPEN)` で毎回落ちる
→ 「dev serverの起動」節(このファイル冒頭寄り)「🔴 WSL側とWindows側のdev serverを同時に動かすと…」にまとめた。**WindowsとWSLを同時に動かさないことが対策であり、「Windows側でdev serverを動かすこと自体」が原因ではない**(2026-08-07時点ではWindows側のbinding不備もあってWSL専用運用にしていたが、2026-08-12にbindingが解消され、単独ならどちらでも動く)。
